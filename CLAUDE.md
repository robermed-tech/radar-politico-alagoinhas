# Viratempo (ex-Avaz, ex-Radar Político) Alagoinhas — instruções para Claude Code

Este arquivo é lido automaticamente pelo Claude Code no início de cada sessão neste repositório. Ele existe para reduzir o número de idas e voltas com o Robério durante depuração.

## Contexto do projeto (resumo)

- **O produto chama-se VIRATEMPO** (nome definitivo, decisão do Robério em 21/08/26, com abertura declarada para ajuste futuro; substitui o "Avaz" de 10/08/26, que por sua vez substituiu o provisório "Radar Político"). A marca exibida em telas, PDFs, mensagens de WhatsApp e manifest é "Viratempo"; identificadores de código, tabelas e arquivos (`radar-app/`, `fetchRadar`, `radio_*`, `radar_theme`, nomes de repo/pasta) seguem com os nomes antigos de propósito — renomear identificador por causa de rótulo é troca sem ganho (a mesma regra da Rádio Escuta). **A marca é o VETOR OFICIAL desde 26/08/26** (`MARCA/SVG/Ativo 1.svg`, no Dropbox da agência), aplicado em `components/LogoViratempo.tsx`: `WordmarkViratempo` é o traçado inteiro, de UMA tinta só (`currentColor`), e `TiqueViratempo` é o "v" de abertura isolado numa caixa quadrada. Três coisas morreram junto com o desenho anterior e não devem voltar: o **corte "vira" + "tempo" em duas cores** (o arquivo da marca não tem esse corte, e refazê-lo à mão é inventar variante que a marca não tem); a **wordmark como TEXTO** em Space Grotesk (`.wordmark-vt` foi removida do index.css — fonte não é marca); e o **símbolo de varredura de radar** (`.vt-varredura`/`vt-girar`, também removidos). O **tagline "Radar do clima político" saiu de TODA a UI** (barra lateral, Login, convite e PDF) na mesma decisão. O tique sobrevive onde a palavra não cabe: favicon, ícone do app (`public/icons/icon-*.svg`) e cabeçalho do PDF. A caixa da marca é 5,10:1 e a altura de x ocupa ~52% dela (contra 28% do desenho antigo), então no MESMO box ela lê maior — ao posicioná-la, medir, não copiar o tamanho antigo.
- Sistema de monitoramento político via Instagram para a SECOM de Alagoinhas/BA (prefeito Gustavo Carmo).
- Pipeline ativo: Apify (scraping) → `agora.py` (Python) → Claude Haiku API → Supabase (destino único) → dashboard HTML + painel Avaz (Vite/React, ex-Radar Comando). **O Google Sheets saiu do fluxo em 01/08/2026** (ver bullet próprio em Comportamento); a planilha antiga ficou como arquivo morto, ninguém escreve nem lê nela.
- **`agora.py` é o agente ativo.** `radar_agente.py` está descontinuado — nunca editar esse arquivo.
- GitHub Actions: o workflow ativo é o **`agora.yml`** (3x/dia: 08h, 14h e 19h BRT; cron em UTC). `radar.yml` está desabilitado desde ~jun/2026 — não confundir os dois.
- Frontend admin: **`viratempo.surge.sh` (endereço ÚNICO desde 21/08/26)**. Os antigos `avaz.surge.sh`, `radar-politico.surge.sh`, `radar-comando.surge.sh` e `radar-politico-alg.surge.sh` servem uma página de redirecionamento (`radar-app/redirect-legado/`, publicada pelo mesmo CI) que preserva rota e parâmetros — não republicar o app neles. Cada troca de nome seguiu o mesmo procedimento: `--domain` novo no `deploy.yml` e o endereço anterior movido para os redirecionadores (a allowlist do Supabase Auth usa `https://*.surge.sh`, então subdomínio novo não exige mexer no Auth; domínio PRÓPRIO fora do surge.sh exige adicionar a URL na allowlist e, no Surge, HTTPS de domínio custom é recurso pago). Supabase: projeto `radar-politico` (ref `wtlhqyqxhuchzloodoyx`), tenant `alagoinhas` — o nome do projeto Supabase segue com o nome antigo (renomeá-lo mexe em remotes, secrets e CI sem ganho de produto). **GitHub: existem DOIS repositórios em migração inacabada — ver a seção "Dois repositórios GitHub" logo abaixo antes de qualquer push, dispatch ou leitura de run.**
- `agora.py` já lê `tenant_settings` do Supabase a cada execução (keywords, fontes, `climate_thresholds`, `notification_config`). Pendência real de configuração: os `score_weights` afetam só os índices calculados no frontend, não o score por post do modelo.

## Dois repositórios GitHub (migração inacabada — estado de 01/09/26)

A migração de 12/08 para a organização nova ficou no meio do caminho. Em 29/08 o CÓDIGO já está sincronizado (as duas `main` no mesmo commit, `6fab25d`) e a metade dos secrets do novo foi cadastrada; o que continua travando é o Actions da organização.

- **`robermed-tech/radar-politico-alagoinhas` (antigo) é quem OPERA o produto.** Todos os crons rodam nele — ÁGORA 3x/dia, heartbeat a cada 15 min, captação de rádio, monitor de créditos — e seguiam saindo `success` em 29/08. O deploy do painel por push em `main` também funciona nele (testado em 26/08: push → run `success` → `viratempo.surge.sh` atualizado, build byte a byte igual ao local).
- **`vaporfilmes-ops/radar-politico` (novo) recebe os commits, mas o Actions dele NUNCA executou um run.** Em 26/08, um `workflow_dispatch` do deploy criou o run e ele ficou preso em `queued` — com o GitHub "All Systems Operational". Diagnóstico: o repositório é PRIVADO e a organização não tem cobrança configurada (minutos do Actions em repo privado são pagos); resolve-se em Settings → Billing da organização. Consequência colateral: **o repo novo é privado, então leitura anônima da API devolve 404** — conferir run por lá exige token (é a mesma lição da memória "verificar via git, não WebFetch").
- **O run preso de 26/08 NÃO é cancelável, e isso é a prova do diagnóstico.** Medido em 29/08 (76h em `queued`): ele tem **0 jobs**, e as duas rotas se contradizem — `gh run cancel` responde "Cannot cancel a workflow run that is completed" e o `force-cancel` responde 409 "Cannot cancel a workflow run that has not been queued yet". Ou seja, a lista mostra `queued` mas o run nunca entrou em fila de verdade: não é fila lenta, é run que a organização não admitiu. Só `DELETE` o tira da lista; ficou lá de propósito, para comparar com o primeiro run que criar job quando o billing destravar.
- **Consequência prática: até a organização nova destravar, todo push vai para os DOIS remotes.** **O NOME dos remotes varia por checkout — rodar `git remote -v` antes de empurrar.** No worktree usado em 29/08, `origin` era o ANTIGO e `vapor` o novo (o inverso do que valia no checkout principal, onde o antigo se chama `legado`). Push só no novo deixa os crons rodando código defasado — foi exatamente o que aconteceu entre 12 e 26/08: o repositório antigo operou duas semanas SEM o rebrand de 21/08 (workflows como "Avaz — …" e alertas de WhatsApp com a marca velha), porque a `main` dele parou em `10cb949`. Em 26/08 as duas `main` foram igualadas, e em 29/08 continuavam iguais.
- **Secrets do repo novo (estado de 01/09/26): 15 cadastrados e o conjunto ESSENCIAL está COMPLETO (11/11).** Operação: `ANTHROPIC_API_KEY`, `APIFY_API_TOKEN`, `AUTOR_HASH_SALT`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` (do `.env` da raiz desta máquina — que EXISTE no Windows, ao contrário da nota antiga sobre o Mac); rádio: `GROQ_API_KEY`; Instagram: `IG_USERNAME`/`IG_PASSWORD`; alerta: `CALLMEBOT_APIKEY`; deploy: `SURGE_LOGIN`/`SURGE_TOKEN`. Mais quatro por paridade, sem consumidor em workflow: `APIFY_POST_ACTOR_ID`/`APIFY_COMMENT_ACTOR_ID` (identificadores públicos que o `radar.yml` escreve em texto) e `VITE_SUPABASE_KEY`/`VITE_SUPABASE_URL` (o `deploy.yml:38` as leva em claro). **Faltam só dois, e nenhum impede a operação: `IG_PROXY` e `IG_SESSION_JSON`** — os dois REDUZEM risco de bloqueio do Instagram (sem eles a coleta sai pelo IP do runner e faz login completo 3x/dia). O `IG_PROXY` sai do painel do Webshare no formato de `.env.example:17`; o `IG_SESSION_JSON` **não existe como cópia em lugar nenhum** (é o `dump_settings` do instagrapi, e o do legado é de 22/06, provavelmente já vencido): regera-se com login local, e entra por arquivo (`gh secret set … < arquivo`), nunca por colagem, porque é multilinha. **NÃO cadastrar** (a varredura de `secrets.*` nos workflows os acusa como faltando, e isso é ruído conhecido): os quatro `EVOLUTION_*` (servidor fora do ar desde 31/07 — quem entrega WhatsApp é o CallMeBot), os quatro `TWILIO_*` (nunca existiram nem no legado) e `GOOGLE_SA_JSON`/`GOOGLE_SHEET_ID` (só o `radar.yml`, desabilitado, do fluxo do Sheets morto em 01/08). **Conferir nome de secret é obrigatório**: erro de digitação é silencioso, o workflow recebe string vazia e falha adiante — o cruzamento entre `gh secret list` e os `secrets.*` dos workflows pega isso em segundos, e foi o que mostrou, em 01/09, que um `gh secret set` que parecia ter dado certo não havia gravado em repositório nenhum.
- **Os valores vieram do `.env` local, não dos secrets do legado, e podem estar defasados — `APIFY_API_TOKEN` é o suspeito.** O GitHub nunca devolve valor de secret, então não há como comparar: o `.env` da raiz foi modificado em **27/07** e o secret do legado em **28/07**, um dia depois, e 27/07 é a data da migração da conta Apify `radar_vapor` → `radarP_traffic`. O prefixo não distingue (`apify_api_` + 36 chars nos dois). O teste é o primeiro ÁGORA do repo novo: coleta com 403 lá e normal no legado = token da conta antiga, e o conserto é recadastrar pegando o valor em apify.com.
- **`GH_WEBHOOK_TOKEN`: o secret saiu do repositório antigo em 29/08 (24 → 23 secrets), e o PAT por trás dele tinha um consumidor que quase passou batido.** O secret era cópia morta (nenhuma referência em código ou workflow) e `gh api .../hooks` devolvia `[]` — **mas essa checagem só enxerga webhook do lado do GitHub**. O PAT clássico `apify-webhook-radar` (escopos `repo, workflow`) aparecia como *Last used within the last week*, e o que o usava era um **webhook do lado da APIFY**, criado em 26/05/26 (um dia depois do secret), disparando `ACTOR.RUN.SUCCEEDED` do ator `nH2AHrwxeTRJoN5hX` contra `.../actions/workflows/radar.yml/dispatches`. Ou seja: uso real toda semana, efeito ZERO — o `radar.yml` está `disabled_manually` desde jun/2026 (último run em 02/06, `failure`), então eram 120 disparos acumulados batendo em porta fechada. **O webhook foi apagado na Apify em 29/08 (`HTTP 204`, restaram 0 webhooks lá); os dados de recriação ficaram num arquivo de scratchpad, não no repo.** Lição que fica: **antes de revogar credencial, procurar o consumidor do lado de FORA do GitHub** — a lista de webhooks do repositório não prova que ninguém usa o token. Ordem certa quando isso acontecer de novo: apagar o webhook primeiro, revogar o PAT depois, senão o serviço externo fica acumulando 401 que ninguém investiga. Apagar o secret **não revoga a credencial**: a revogação do PAT é só pela interface (`github.com/settings/tokens`), porque o GitHub não expõe rota de API para listar ou revogar PAT do próprio usuário. **Não confundir com o `GH_DISPATCH_TOKEN`** (fine-grained, ~30/07, nos secrets do Supabase), que é outro token e é o que faz o botão GRAVAR e o "Enviar teste" funcionarem.
- **Quando o Actions do novo funcionar**, a ordem de encerramento é: completar os 10 secrets que faltam, repontar as Edge Functions, confirmar um ciclo completo de runs agendados no novo, e só então desligar os agendamentos no antigo. **O deploy do painel é o que destrava primeiro**: o `deploy.yml` só consome `SURGE_LOGIN`/`SURGE_TOKEN` (as `VITE_*` estão em texto no próprio arquivo), e esses dois já estão lá. Atenção a dois pontos já documentados que dependem do repo ser PÚBLICO e deixam de valer no novo enquanto ele for privado: o heartbeat de 15 min ("Actions ilimitado e grátis" só em repo público) e a issue de reserva do alerta de suporte (legível pela API sem token só em repo público).
- **Repontar as Edge Functions é configuração, não código — mas `GH_REPO` sozinho NÃO basta.** `gravar-radio` e `testar-alerta-suporte` já leem `GH_REPO` do ambiente, com **fallback no repositório antigo** (`index.ts:30` e `index.ts:28`), então o endereço se troca com `supabase secrets set GH_REPO=vaporfilmes-ops/radar-politico`. O que falta junto é o TOKEN: o `GH_DISPATCH_TOKEN` em uso é o PAT fine-grained chamado **`Radio Escuta`** (o único fine-grained da conta, sem expiração, secret do Supabase atualizado em 30/07), e token fine-grained é **repository-scoped** — o dele alcança o repo ANTIGO. Repontar o `GH_REPO` sem trocar o token faz o dispatch responder 404 e o botão GRAVAR / "Enviar teste" falharem em silêncio. **E o token novo pode esbarrar na organização**: fine-grained só alcança repositório de org que tenha habilitado isso em Settings → Personal access tokens → *Allow access via fine-grained personal access tokens*. Se a `vaporfilmes-ops` não tiver habilitado, criar o token não resolve — é a MESMA classe de bloqueio do billing (tela que exige o dono da org, fora do alcance da conta `robermed-tech`, que lá tem `push` mas não `admin`). Conferir as duas telas na mesma visita.
- **Deploy manual de emergência existe e foi usado em 26/08** (o primeiro deploy do `viratempo.surge.sh`, com o Actions do novo travado e o antigo ainda defasado): a máquina local está logada no Surge (`npx surge whoami` → robermed@gmail.com), o `.env` do radar-app tem as mesmas `VITE_*` do CI, e o roteiro é o do `deploy.yml` (build → `cp dist/index.html dist/200.html` → `npx surge --project dist --domain viratempo.surge.sh` → redirecionadores). Apagar o `CNAME` que o surge grava no dist antes de commitar.

## Decisões de produto vigentes (revisões de 24, 25 e 27/07/2026)

Vieram das reuniões com o cliente (PRs #37, #40, #41 e #55). Valem como estado atual — não desfazer sem pedido explícito.

### Revisão de 27/07 (PR #55)

- **Filtro 24h/7dias/30dias em toda tela que mostra comentário ou análise.** Um componente só: `components/PeriodoFilter.tsx` (rótulos, ordem e desenho vêm dele). Está em Clima, Feed, Aprovação, Mapa da Cidade, Pedidos, Análise por Perfil, Previsões e Alertas & Ações. Ao criar tela nova com esses dados, usar esse componente e não uma cópia local.
- **O recorte de comentários por período é feito cruzando `url_post` com os posts da janela**, nunca por `comments.data_comentario_ts` (backfill parcial, ~8% das linhas). Mesma abordagem do `agora.py::contar_comentarios_por_tema`.
- (SUBSTITUÍDO em 27/08, ver "Revisão de 27/08") **Estação Meteorológica**: o radar de varredura era a coluna do meio entre o card do clima e o de engajamento (`RadarStatusColumn`, grid de 6 colunas: 3 + 1 + 2). Hoje ele é selo na linha do título e a faixa nobre é veredito + engajamento + diagnóstico. A faixa horizontal (`RadarStatusBar`) continua no estado sem dados, onde não há os dois cards.
- **Box "Publicações analisadas" é preto fixo**, não acompanha tema claro/escuro, e toda fonte nele é bold e >= 12px. A diretriz global de tipografia do `index.css` rebaixa `font-bold` para peso 400 com `!important` — por isso existe `.peso-bold-total`, exceção **escopada só a esse box**. Só mudar a classe no componente não basta; foi assim que a primeira tentativa saiu preta e com texto em peso regular.
- **Velocímetro por tema** (`GaugeTema.tsx`): arco único em degradê com pontas arredondadas, arcos finos internos, ponteiro longo com cubo e o valor abaixo. É **SVG puro** — não reintroduzir ECharts aqui, a ClimaPage é a landing e o chunk é de ~1 MB. O ponteiro **vibra o tempo todo** (animação CSS de `transform`, que roda no compositor e sobrevive à rolagem). O ângulo de repouso é aplicado por `useEffect`, **nunca por `requestAnimationFrame`**: rAF não roda em aba oculta e a agulha ficava travada no zero apontando valor errado. A referência do cliente tem vermelho à esquerda; aqui o degradê continua verde à esquerda porque o ponteiro mede **% de negativos** e 0% é o melhor cenário. Foi adotada a forma, não a inversão de significado.
- **Mapa da Cidade**: cada barra e cada rótulo de bairro abrem a coletânea de comentários que definiu aquela posição no rank (`ComentariosBairroModal`). **Nome de bairro nunca aparece com underscore na tela** — `lib/format.ts::labelBairro` converte o slug (`riacho_da_guia`) para o nome legível (`Riacho da Guia`) em todo ponto de exibição; o slug cru continua sendo o valor usado para clique/match (eixo do gráfico, filtro), só o rótulo visual muda. Sem acento (o frontend só tem o slug, não `bairros.nome`), mas sem o traço.

### Revisão de 29/07 (refinamentos de tela pedidos pelo cliente)

- **Pedidos do Povo não tem mais o chip "Todos"** — a faixa de filtro é só de temas. O estado "todos" continua sendo o padrão (`temaSel = "todos"`, nenhum chip aceso = lista inteira) e clicar de novo no tema aceso volta para ele: sem isso, remover o botão prenderia o usuário no primeiro tema clicado.
- **`RadarStatusColumn` usa `h-full`.** O wrapper é o item do grid e estica com a linha, mas o card parava no próprio conteúdo (`minHeight` 320) enquanto clima e engajamento crescem com o texto — a base do radar ficava flutuando acima da dos vizinhos. Ao mexer nessa linha da Estação Meteorológica, a altura tem que vir do grid, não de um `minHeight` combinando com o conteúdo dos outros dois.
- **Previsões perdeu "Assuntos em alta · 24h" e "Estabilização pós-alerta".** `AssuntosEmAlta` e `PainelRecuperacaoIRT` saíram da `TemasPage`; `fetchTemasMonitorados`, `IRT_STATUS`/`IRT_TEND` e `TendenciaIcone` foram removidos junto como código órfão (o componente `AssuntosEmAlta.tsx` e a query em `lib/data.ts` continuam no repo, sem chamador). O backend segue calculando os dois. Não recriar sem pedido explícito.
- **O gráfico do Mapa da Cidade ocupa a tela toda**: altura `min(calc(100vh - 430px), n*60px)` com piso de 280px, e `barMaxWidth` de 18 para 30. O `min()` é o que importa: com altura fixa por viewport, um período com 3 bairros esticaria três barras dentro de 700px — o teto por barra evita isso, e o piso evita o oposto.
- **Os quatro cards de extremo da Análise por Perfil são BRANCOS** (o mesmo `bg-bg-1` dos outros cards, que segue o tema), com o **@ do perfil em corpo grande** (30/38px, peso 800) carregando a cor semântica: verde para elogio, vermelho para crítica, mais um fio vertical de 4px na cor do grupo. Isso **reverte o gradiente preenchido de 28/07** (`FUNDO_CRITICA`/`FUNDO_ELOGIO`, texto preto sobre degradê claro) — com quatro cards coloridos lado a lado o olho lia a faixa de cor antes de ler quem. A receita do degradê claro segue valendo onde ela está (box de engajamento, botões); o que mudou é que **estes cards** não a usam mais.
- **Todo comentário de cidadão aparece no MESMO box, `components/ComentarioBox.tsx`** — em **vidro claro translúcido** (`bg-bg-2`) desde 30/07, que **reverte o degradê chumbo→quase-preto** de 29/07: repetido cinco ou seis vezes seguidas, o box escuro virava uma parede preta no meio de uma página clara. O componente único continua; o que mudou foi a superfície, num lugar só. `bg-bg-2` é branco translúcido nos dois temas (0,55 no claro, 0,04 no escuro), então o box acompanha o tema em vez de ser uma ilha clara dentro do modo escuro. **Tinta de sentimento sobre o vidro claro tem tokens próprios** — `--sent-ink-pos`/`--sent-ink-neg`/`--sent-ink-link`, verde-700 e vermelho-700 no claro — porque o par de gráfico `#22C55E` mede 2,3:1 ali e até o `--success` do tema para em 3,22:1; os tokens novos medem 4,91:1 e 6,33:1 no pior caso. Registro histórico do que a versão escura exigia (chip com fundo quase sólido, laranja-200 para link) fica no git: nada disso vale mais.
- **Os comentários de subtema abrem DENTRO do card do tema clicado** (Previsões → "Dentro de cada tema"), não numa faixa no rodapé do painel. Lá embaixo ficavam a três cards de distância do subtema que os havia aberto. A lista rola por dentro (`max-h-[420px]`) para 15 comentários não esticarem a linha inteira do grid.
- **O chip do seletor de perfis não tem mais o selo com o número de publicações**, e o cabeçalho do perfil não tem mais o badge "Risco {nível}" (`calcIndices`/`NIVEL_*` saíram da `PerfilPage` como código órfão). O @ nos quatro cards de extremo caiu de 30/38px para 21/25px.
- (Histórico) O degradê do `RadarStatusColumn` (`linear-gradient(165deg,#475569,#0F172A)`) segue em uso no radar de coleta, no painel da antena e no card de gravar — só o box de comentário saiu dele. Antes cada tela tinha o seu: `bg-bg-2` claro na Análise do Clima e na Análise por Perfil, `bg-bg-1` no drill-down das Previsões, `.quote-box` translúcido no Feed e grafite chapado (`#1E293B`) nos dois modais de coletânea. Consumidores: `ComentariosBairroModal`, `EvidenciaComentariosModal`, `AlertaCrise`, `ApprovalPage`, `PerfilPage`, `TemasPage`, `FeedPage`, `PedidosPage`. Ao criar tela que exiba comentário, usar esse componente — não uma cópia local. A citação da **rádio** ficou de fora de propósito: é transcrição de fala com instante e aviso de transcrição automática, não comentário.
- **Sobre o degradê escuro (radar, antena, card de gravar), texto colorido de sentimento continua proibido**: o vermelho de gráfico `#EF4444` mede 2,01:1 na ponta clara `#475569`, e nem o `#FCA5A5` chega ao AA. Ali o rótulo precisa de chip com fundo quase sólido (`rgba(2,6,23,0.88)`, onde o `#FCA5A5` mede 10:1) e o link é laranja-200 (`#FED7AA`, 5,60:1). Isso vale para o que continua escuro — **não** para o box de comentário, que voltou ao vidro claro.
- **A coletânea do Mapa da Cidade não tem mais os filtros "Todos / Críticos / Favoráveis / Neutros".** A lista mostra tudo, do mais curtido para o menos; a decomposição por sentimento continua na barra do topo do modal e no chip de cada comentário, então saiu o controle, não o dado.
- **Na Análise por Perfil, o perfil selecionado e os KPIs dele ficam logo abaixo do seletor**, no topo da página. Antes vinham depois da tabela de ranking: clicava-se num chip no topo e o efeito do clique acontecia fora da tela.
- **O card "Tom das publicações de @perfil" (barra crítica/neutra/elogio) saiu** da Análise por Perfil. Os mesmos três números estão na tabela de ranking, nas colunas "publica criticando"/"publica elogiando". `fazNeutro` e `pctFazCritica` continuam em `lib/analisePerfis.ts`, sem consumidor nesta tela.
- **O painel da Escuta do Rádio usa a TORRE de antena** (`components/AntenaSinal.tsx`): a tela mostra a captação do que está no ar. **O item de menu, porém, fica com o rádio de mesa** (`NIcoRadio`) — a torre chegou a ocupar os dois lugares em 29/07 e o cliente pediu o receptor de volta no menu em 30/07. Os dois desenhos convivem de propósito: o ícone da barra lateral nomeia a seção, a antena de dentro da página é o estado da captação. `IconAntena` continua exportado e usado pelo painel. `AntenaStatusColumn` é gêmeo do `RadarStatusColumn` (mesmo degradê, mesmas medidas, mesma linha de status) — o cliente pediu "igual ao do radar", e igual quer dizer o mesmo card, não um parecido. As ondas acendem do foco para fora por **animação CSS de `transform`/`opacity`**, nunca `requestAnimationFrame` (a mesma razão do velocímetro: rAF para em aba oculta), e há uma camada fixa e apagada por baixo para o desenho continuar legível com `prefers-reduced-motion`.
- **"Captando" no painel da antena é gravação que DEU certo na janela, não rádio cadastrada.** Estação cujo stream falhou continua monitorada e não está captando nada — é a mesma distinção entre falha e silêncio que os cards de estação já fazem.
- **A cor do @ vem de `text-success`/`text-danger` (tokens `--success`/`--danger`), nunca do par `#22C55E`/`#EF4444` dos gráficos.** Sobre fundo branco o verde de gráfico mede 2,29:1 e reprova até o mínimo de 3:1 de texto grande; os tokens são resolvidos por tema (`#16A34A`/`#DC2626` no claro, `#4ADE80`/`#F87171` no escuro) e passam AA nos dois. Regra geral: cor de série de gráfico é para preenchimento, não para texto.

### Localidade: apelido de bairro que também é substantivo comum

- **"Conter a palavra" nunca prova que é o bairro.** Achado em 27/07: quatro denúncias sobre o *Centro de Testagem e Aconselhamento* (CTA, um serviço de saúde) viraram quatro críticas ao **bairro Centro**. O bairro real tinha 2 menções e aparecia com 6, sendo 67% negativas, o que o jogava para o topo do ranking de "mais crítico". Depois da correção: 2 menções, 50% negativo.
- `agora.py::_PALAVRA_COMUM_DE_LUGAR` lista **palavras**, não bairros, e é cruzada com os aliases reais de cada tenant. Quando o apelido é uma delas (`centro`, `cruzeiro`, `riacho`, `velha`, `barreiro`...), ele só resolve se aparecer como lugar: precedido de preposição (`no Riacho`, `bairro Centro`), no fim da frase, seguido de pontuação, ou seguido de marcador de lugar (`centro da cidade`, `centro de Alagoinhas`). `centro de testagem` e `centro cirúrgico` caem fora.
- **Alias mais longo primeiro.** A varredura seguia a ordem de inserção do dicionário, então `centro` podia vencer `centro da cidade` e `riacho` podia vencer `riacho da guia` — o apelido curto decidindo no lugar do nome completo. A ordenação mora em `normalizar_localidade`, e não em `carregar_bairros`, para nenhum chamador conseguir quebrá-la montando o mapa por conta própria.
- **A regra do normalizador NÃO serve para texto corrido.** O normalizador recebe o valor curto que o modelo extraiu ("Centro"); o reparo recebe o comentário inteiro, onde a palavra vem cercada de verbo e vírgula ("Barreiro estão só as crateras", "no Riacho esse ano"). Aplicar a mesma exigência nos dois produziu **3 falsos positivos em 7** na primeira tentativa, marcando bairro legítimo como erro. Por isso `_so_em_composto` (usado só pelo reparo) é mais conservador: só acusa quando **toda** ocorrência da palavra é cabeça de nome composto, e nunca acusa quando a palavra não aparece no texto (em "era Riachense" o modelo acertou Riacho da Guia sem escrever "riacho"; adivinhar contra ele apagaria dado bom).
- O prompt de comentários também foi ajustado: pede **bairro/povoado/praça/rua**, e diz explicitamente que nome de equipamento ou programa (posto, CTA, hospital, UPA, escola, creche, secretaria) **não é localidade**. A formulação anterior pedia "bairro/praça/rua/escola citado" e convidava o modelo a devolver o nome do serviço.
- **A genericidade é decidida pela ÚLTIMA palavra do apelido** (`_alias_extensivel`), não pelo apelido inteiro. `no centro` tem duas palavras e escapava da checagem, voltando a casar dentro de "no centro de cirurgias eletivas" e "no Centro Administrativo" — o mesmo erro do CTA por outra porta, achado ao varrer a base inteira. `centro da cidade` e `riacho da guia` terminam em palavra distintiva e seguem valendo por conteúdo.
- **Não existe resgate por preposição anterior.** Chegou a existir e abria justamente esse buraco: em "no centro de cirurgias" a preposição aprovava a linha antes de alguém olhar o que vinha depois. Como a entrada é o valor curto extraído, "no Riacho" já passa pelo fim de frase.
- Duas ambiguidades que precisaram virar regra na recuperação: **`_CONTEXTO_DE_FESTA`** (nome de santo precedido de "alvorada", "festa", "igreja", "padroeira" é o festejo, não o bairro — Alagoinhas tem Santa Terezinha e Santa Isabel, a confusão é estrutural) e **`_ALIAS_DE_MUNICIPIO_VIZINHO`** ("Até Catu colocou umas atrações" é a *cidade* de Catu; o apelido curto exige prova de lugar, `rua do catu` continua direto).
- **`_bairro_no_texto` é mais exigente que `normalizar_localidade`, de propósito.** O normalizador *resolve o que o modelo extraiu*; `_bairro_no_texto` *decide sozinha o que o modelo deixou passar*. Inventar bairro errado é pior que deixar em branco, então a dúvida resolve para `None`.
- Ferramentas (custo zero: só leem texto já gravado, nunca chamam o modelo):
  - `python agora.py --teste-localidade [N|0]` roda a bateria de casos com resposta esperada (`_CASOS_LOCALIDADE`) e varre a base (`0` = inteira) procurando **contaminação** e **captura perdida**, os dois erros de sinais opostos. **A varredura lê o texto inteiro, enquanto a produção alimenta o valor curto extraído — ela acha erro, mas não é taxa de acerto.**
  - `python agora.py --reparar-localidade [--dry-run]` desfaz atribuição vinda de nome composto.
  - `python agora.py --recuperar-localidade [--dry-run]` preenche bairro que o modelo deixou passar e o texto nomeia. Nunca sobrescreve bairro já atribuído.
  - **Sempre rodar o `--dry-run` e LER os textos antes de gravar.** A primeira versão do reparo apontou 7 linhas e 3 eram bairro legítimo; só a leitura pegou isso.
- Estado em 27/07 depois das três passadas: 51 bairros atribuídos (eram 38), contaminação 0, captura perdida 0. Sobram homônimos que nenhuma regra de texto separa sozinha e que já estão cobertos pelas duas regras acima.
- **Análise por Perfil usa a tela Relevância como critério** (`lib/relevancia.ts`, match por substring e sem acento, espelhando `agora.py::_motivo_relevancia`). Sem palavra ativa cadastrada, tudo passa — a tela não avisa mais disso (o aviso saiu na revisão de 27/07; quem sinaliza lista vazia agora é a própria tela Relevância). Os quatro cards de extremo mostram só "mais critica"/"mais elogia"/"recebe mais críticas"/"recebe mais elogios" (os `.menor` de `extremos()` continuam calculados mas não exibidos — os cards "menos" saíram por pedido do cliente), com piso de amostra (`MIN_AMOSTRA`) nos rankings de polaridade.
- **Análise por Perfil ocupa a largura cheia da tela** (revisão de 28/07) — o container tinha `mx-auto max-w-6xl`, um resquício que nenhuma outra página do painel usa (todas as demais são só `space-y-4 p-5`); sobrava moldura vazia dos dois lados em tela larga enquanto Clima/Feed/etc. iam de ponta a ponta. Não reintroduzir o max-width. O nome do perfil em verificação (cabeçalho logo acima dos KPIs, `@{autor}`) também subiu de `text-lg` para `text-[34px]/[42px] font-extrabold` — mesmo corpo do H1 da página — de propósito: é a confirmação visual de qual perfil está sendo lido, então precisa ser reconhecível num relance, não do mesmo tamanho do texto ao redor.
- **Cards e botões coloridos seguem a receita do card "Engajamento no período" (Estação Meteorológica): degradê CLARO (150deg, mesma família de cor do início ao fim) com texto quase preto (`#04242F` desde 26/08, antes `#1A0F02`) por cima — nunca degradê escuro com texto claro.** Foi a correção de 28/07: a primeira versão dos cards de extremo e do seletor de perfis usava fundo escuro com texto claro, e "fugia do padrão" do resto do painel. Vermelho de crítica é `#FCA5A5 → #EF4444`; verde de elogio é `#86EFAC → #22C55E`. Vale só para cor SEMÂNTICA (crítica/elogio, reação recebida); a mesma revisão de 28/07 tirou a cor por categoria do seletor de perfis (`CAT_GRADIENTE` foi removido) — identidade de perfil não é sentimento, e a paleta do painel reserva verde/vermelho só pra isso. Hoje `PerfilPage.tsx` usa um chip neutro pra todo perfil, igual pro Prefeito, pra Imprensa ou pra Oposição: degradê chumbo→preto (`FUNDO_PERFIL_BASE`) com um brilho translúcido branco por cima (`BRILHO_PERFIL`/`BRILHO_PERFIL_ATIVO`, mais forte no ativo), texto branco sólido, anel branco (`box-shadow`) marcando o selecionado. **O brilho translúcido tem seu próprio limite de contraste**: um brilho branco de até 24–46% de opacidade sobre a ponta clara do degradê antigo (`#475569`) derrubava o texto branco pra 4,13:1 (inativo) e 2,54:1 (ativo) — os dois abaixo do mínimo AA. A correção foi escurecer a ponta clara da base para `#334155` (chumbo, não cinza aberto) e também baixar o teto do brilho (14%/24%); qualquer brilho translúcido por cima de um degradê precisa entrar na mesma medição de contraste do degradê em si, não só o degradê sozinho. **Texto sobre o degradê é sempre preto SÓLIDO, nunca preto com alpha** — a mesma pegadinha do texto branco-com-alpha da versão anterior se repetiu ao contrário: `rgba(26,15,2,0.78)` passava na ponta clara e reprovava na escura (3,87:1). Todo elemento auxiliar sobre um degradê (badge, selo, chip) precisa do próprio fundo quase sólido, não translúcido — um chip a 14% de opacidade sobre o degradê do seletor de perfis reprovava AA em 3 das 4 categorias (3,80–4,05:1) porque o gradiente por trás varia demais para um alpha baixo compensar; a correção foi `rgba(26,15,2,0.88)` com texto claro por cima, isolando o chip do fundo. **Opacity reduzida no elemento inteiro (bg + texto juntos) é outra armadilha**: ela mistura tudo com o que está atrás na página, e como o texto já é quase preto sobre um fundo também escuro, o efeito líquido é reduzir o contraste do FUNDO colorido sem avisar — diferenciar estado ativo/inativo só por anel (box-shadow), nunca por opacity do botão inteiro. Ao trocar qualquer tom, medir contraste nas DUAS pontas do degradê, não só na mais escura: a ponta clara é sempre o pior caso na direção oposta (times passados: verde reprovou 4,03:1 na ponta clara com texto branco; vermelho reprovou 3,87:1 na ponta escura com preto-alpha).
- **`posts.tom_publicacao` mede o que o perfil DISSE; `posts.sentimento_post` mede o que o público RESPONDEU. Nunca trocar um pelo outro.** O prompt do `agora.py` diz, sobre o `sentimento_post`, que ele é "o IMPACTO na imagem do prefeito pela REAÇÃO dos comentários, NÃO o tom da caption": usá-lo como opinião do perfil faria um post elogioso da prefeitura que tomou uma enxurrada de críticas ser contado como crítica que a prefeitura fez contra si mesma. Os dois campos discordam com frequência, e é essa discordância que a tela Análise por Perfil mostra. Deduzir o tom pela categoria (oposição = crítica) continua proibido: é o atalho de polaridade por lado removido em 25/07.
- (REMOVIDO em 11/08/26) **A seção Influenciadores/Mapa de Influência saiu da Análise por Perfil** (pedido do cliente, print com X vermelho sobre o card inteiro — ranking por score composto + os três contadores Aliados/Imprensa/Oposição). `InfluencersSection` (`pages/InfluencersPage.tsx`) fica no repo sem chamador, como `AssuntosEmAlta`. Não recriar sem pedido explícito. As regras históricas do card (contorno laranja = quem mais publicou, descrição abaixo do gráfico) só valem se ele voltar.
- **O feed não tem mais "Comentários nas publicações sobre a gestão" nem "Temas das publicações sobre a gestão" na Análise por Perfil** — os dois cards saíram na revisão de 28/07 (pedido do cliente). Não recriar sem pedido explícito; se voltar, o `temas` (useMemo) e `labelTema`/`TEMA_LABEL` precisam voltar junto, foram removidos como código órfão.

### Tom da publicação (`tom_publicacao`, migration 010)

- Valores: `critico` | `favoravel` | `neutro` | `nao_classificado`. O default é **`nao_classificado`, nunca `neutro`**: "não medido" e "medido e deu neutro" são coisas diferentes na contagem. Há CHECK constraint no banco.
- **Quem classifica o tom é UMA chamada dedicada (`PROMPT_TOM`), que vê só a legenda. Nunca reintroduzir o campo no `PROMPT_TRIAGEM` nem no `PROMPT_SISTEMA`.** A primeira versão fazia isso, para economizar a chamada extra, e quebrou de um jeito instrutivo: os três posts da inauguração do Hospital Regional (obra estadual, com o presidente) saíam `neutro` pelo classificador dedicado e `favoravel` com confiança 95 pela triagem. Causa: `triar_post_rapido` injeta `[LADO: ALIADO]` e a nota "elogio ao que foi entregue = POSITIVO", que é orientação para classificar **comentários** e vazava para o tom. Ou seja, o atalho de polaridade por lado voltava pela porta dos fundos, e o mesmo post recebia tom diferente conforme caísse no tier rápido ou no profundo. O classificador dedicado não sabe de quem é a conta nem vê os comentários: não tem como deduzir por lado.
- **O tom é calculado uma vez por post, e depois reaproveitado.** Ele depende só da legenda, que não muda; reclassificar a cada execução seria pagar 3x por dia pela mesma resposta. `analisar_com_agora` lê os toms já gravados e só chama o modelo para post novo (o log diz "N tom novo (M reaproveitados)"). Em regime isso sai mais barato que a versão "sem chamada extra" que foi removida.
- Efeito colateral bom: como `--teste-tom` chama a mesma função da produção (`classificar_tom_publicacao`), o harness mede exatamente o que o pipeline faz, por construção. Na versão anterior ele media outra coisa.
- **O portão vem antes de tudo**: "esta publicação emite juízo sobre a gestão municipal?" Sem isso o classificador marcava como `favoravel` qualquer notícia boa sobre a cidade — inauguração do Hospital Regional (obra estadual), festa de Santa Terezinha, comércio crescendo. Medido em 27/07: com o portão, o post do Hospital Regional passou de `favoravel` para `neutro` tanto na conta do prefeito quanto na do opositor.
- **Autopromoção de político não é juízo sobre a gestão.** Político contando a própria agenda, apresentando a equipe ou descrevendo seu método ("a gente caminha, escuta as ruas") é `neutro`. É a mesma regra do comentário: falar bem de um lado não é falar mal do outro. Sem ela, `@soulucianoalmeida` saía `favoravel` por descrever o próprio trabalho.
- Confiança abaixo de `CONFIANCA_MIN_TOM` (60) conta no total mas não entra em nenhum lado. Gêmeo no frontend: `analisePerfis.ts::CONFIANCA_MIN_TOM`. Se mudar num, mudar no outro.
- **O upsert nunca rebaixa um tom já gravado.** O pipeline reprocessa os mesmos posts a cada execução; sem a guarda em `gravar_no_supabase`, uma triagem que falhasse numa rodada apagaria a classificação boa da rodada anterior.
- Ferramentas (custo só Anthropic, zero crédito Apify):
  - `python agora.py --teste-tom [N] [--oposicao|--imprensa|--governo]` mede numa amostra real **sem escrever nada**. O que olhar: se todo post de oposição sair `critico` e todo post de governo sair `favoravel`, o modelo está deduzindo pelo perfil em vez de ler o texto.
  - `python agora.py --reclassificar-tom [N] [--dry-run] [--refazer]` preenche a base. Idempotente: só toca em `nao_classificado`, a menos que venha `--refazer`.
- Medição de 27/07 na base real (214 publicações classificadas): 25 críticas, 63 favoráveis, 126 neutras. Por perfil, `@soulucianoalmeida` 11 críticas/0 elogios, `@alagoinhas24h` 7/2, `@prefeituraalagoinhas` 0/20, `@alagonews` 0/17, e quatro perfis de oposição com 0 críticas (só agenda) — o placar que confirma que não houve dedução por lado.
- **A coleta Apify não é o único caminho para exercitar o pipeline.** Em 27/07 o teto mensal já estava em 101,2% ($29,34 de $29,00), então `python agora.py` encerra em "Nenhum post coletado" antes de chegar na análise. Para validar mudanças no módulo 4/5c sem depender disso, dá para alimentar `analisar_com_agora` + `gravar_no_supabase` com posts e comentários lidos do próprio Supabase: da análise em diante é o mesmo código de produção, e só a origem dos posts muda. Foi assim que a contaminação do tom pela triagem apareceu.

### Revisão de 01/08 (prints com traço vermelho)

- (SUBSTITUÍDA em 04/08, ver "Revisão de 04/08" abaixo) **Pedidos do Povo: os temas eram BOTÕES GRANDES coloridos** com box moldurado na cor do tema — aprovados em 01/08 e trocados pela régua de volume na prévia de 04/08, a pedido explícito do cliente ("retire o colorido"). A regra de 29/07 sobrevive às duas versões: sem chip "Todos", padrão = lista inteira, clicar de novo no tema aceso volta para ela. `ListaPedidos` continua sendo o componente dos cards.

- **A tabela "Ranking dos perfis monitorados" saiu da Análise por Perfil** (`BarraLados` e o import de `CAT_COR` saíram junto como órfãos). Os números continuam em `lib/analisePerfis.ts`, resumidos nos cards de extremo e nos KPIs do perfil selecionado. Não recriar sem pedido explícito.
- **Previsões (ordem vigente, 2ª revisão do dia): "Linha do tempo do clima" no topo → Subindo/Caindo → "Dentro de cada tema" no rodapé.** A 1ª revisão de 01/08 tinha subido o "Dentro de cada tema" ao topo; horas depois o cliente pediu a troca entre ele e a linha do tempo (print com traços vermelho/rosa ligando topo e rodapé, ordem confirmada por pergunta direta).
- **Vitrine de comentário exige confiança >= 70** (`lib/sentimento.ts::CONFIANCA_MIN_DESTAQUE`/`vozDestacavel`, usada nas "Vozes da população" da Aprovação). É deliberadamente mais alta que o `CONFIANCA_MIN` (50) da contagem: no agregado um comentário fronteiriço é 1 voto em centenas; em destaque ele vira citação com a chancela da tela. Confiança 0/ausente (linha anterior ao campo) NÃO passa na vitrine, ao contrário da contagem. Caso de origem: dois elogios a vereador de oposição ("vcs tem que virar prefeito e vice urgente") gravados como positivo com confiança exatamente 50 apareceram em "Vozes que aprovam".
- **O mesmo caso reforçou a regra 1 do `PROMPT_COMENTARIOS`**: desejar o opositor NO CARGO ("tem que virar prefeito") e exaltá-lo com desdém genérico ("o resto é resto") são NEUTRO — a palavra "prefeito" no desejo de que o opositor assuma não é menção à gestão atual. A reprodução isolada (temp 0) já dava neutro, mas a produção gravou positivo: na fronteira de confiança 50 a composição do lote muda o resultado, então o padrão precisou virar exemplo obrigatório no prompt (confiança subiu para 65-80 depois disso). `--reparar-sentimento-oposicao` rodado em 01/08 com o prompt novo: 58 mudanças (padrão dominante: positivo/negativo -> neutro em posts de oposição).

### Revisão de 04/08 (novas diagramações — prévia com abas aprovada)

Quatro páginas rediagramadas sobre a linguagem mestre, com prévia estática aprovada antes da implementação (`Desktop/aprovacao-novas-diagramacoes.html`). Ajuste pedido no OK do cliente: o ranking de perfis mantém **total de seguidores E evolução** — só a cor por categoria saiu.

- **Pedidos do Povo: os temas são uma RÉGUA DE VOLUME, sem cor por tema** (substitui os botões coloridos de 01/08 — pedido explícito: "retire o colorido"). Nome + contagem + barra proporcional em chumbo; o laranja é só do tema SELECIONADO (laranja = interação). A linha ativa acende e a lista filtra no lugar — o box moldurado saiu. Cards de pedido neutros (chip de tema neutro, "revisar" no token `--warning`); `temaColors.ts` continua existindo para `PostChips` e `RadioPage`, mas os Pedidos não o usam mais.
- **Os planos de crise são RECORTADOS pela janela do `PeriodoFilter`, no servidor** (05/08, achado na avaliação do produto). O filtro 24h/7d/30d da Alertas & Ações só afetava o briefing: `fetchCrisisPlans` trazia sempre os 20 de maior `score_risco`, sem recorte de data. Como 39 dos 70 planos da base eram de junho, a tela exibia crise de dois meses atrás sob o cabeçalho "últimas 24 horas", com o rótulo "situações que precisam de atenção" e o botão de alertar secretário ao lado, ou seja, convidando a acionar gente por incidente encerrado. O corte é por `gerado_em` e **no servidor** (`&gerado_em=gte.`): filtrar no cliente depois do `limit=20` esconderia crise recente de score menor sempre que os 20 mais graves fossem antigos. `dias` entra na `queryKey`, como já era no briefing. Efeito medido: 24h → 0 planos, 7d → 6, 30d → 11, contra 12 sem recorte (a mais velha de 10/06). Dois acréscimos que vêm junto: cada card mostra a **idade** do alerta (`idadeDoAlerta`, "há 3h" / "há 1 dia"), porque dentro da mesma janela urgência é outra; e existe um **estado vazio próprio** ("Nenhum alerta de crise nas últimas 24h"), senão a seção sumia sem explicação quando a janela não tinha plano. Frase com preposição vem de `periodoLabelCom` (o gênero muda: "nas últimas 24h" e "nos últimos 7 dias"). Harness: `radar-app/alertas-dev.html` + `src/dev-alertas.tsx`.
- **Alertas & Ações: fundo neutro com ESPINHA de severidade** (o fundo lavado de laranja saiu — quando a página inteira é cor de alerta, nada se destaca). Resumo por severidade no topo (críticos/altos/total com `ContadorAnimado`), crítico sempre primeiro (`ORDEM_NIVEL` + `score_risco`), título curto = o tema do plano. **"Alertar Secretário" existe no próprio card** (`AlertarSecretarioPlano`), reusando o `EnvioSecretario` do dashboard — mesmo canal, contato, `message_log` e Histórico; a mensagem leva nível, pavio, `risco_se_ignorar` e o link do post. As cores de nível seguem `NIVEL_COLOR` (sistema paralelo de RISCO, não a marca).
- **Fontes: lista agrupada por categoria, SEM chips coloridos** (`FILTRO_COLOR` pintava GOVERNO de verde e OPOSIÇÃO de vermelho — verde/vermelho são de sentimento). Três grupos com cabeçalho e contagem + grupo "Outros" (filtro fora das três categorias não pode sumir do agrupamento) + "Outras plataformas" (YouTube). LED laranja/cinza (o sinal da Rádio Escuta) no lugar do selo.
- **Análise por Perfil**: o seletor é um **trilho rolável agrupado** por Governo/Imprensa/Oposição (`grupoDaCategoria` — os chips continuam neutros; o anel do ativo fecha em laranja da marca por fora do respiro `var(--bg-page)`); o cabeçalho do perfil e os 4 KPIs são **um cartão único** (quem à esquerda, números à direita, `ContadorAnimado`; `KpiStat` saiu desta página mas segue usado por ApprovalPage/CommandCenter); os 4 extremos moram num card só ("Quem puxa a conversa") como tiles compactos com fio de sentimento **absoluto** (border-left mudaria a largura do tile). `RankingSeguidores` perdeu o `CAT_COR` (removido): barra neutra em chumbo, sparkline na marca, categoria em texto na linha; total, sparkline e saldos por janela continuam, e ganho/perda vira texto nos tokens `--success`/`--danger`.
- **Prévia 2 (mesmo dia, mesmo fluxo) — Feed, Aprovação, Mapa da Cidade e Previsões** (`Desktop/aprovacao-novas-diagramacoes-2.html`): **Feed** em duas colunas no `xl` (metade da rolagem), espinha de sentimento no lugar da borda colorida, filtros com contagem; `PostChips` dá NOME à reação ("reação crítica") com tinta `--sent-ink-*` e o chip de tema ficou neutro. **Aprovação**: número + Histórico do IAD **fundidos num cartão único**, com `GaugeAprovacao` local — **verde à DIREITA de propósito** (mede % de APROVAÇÃO, 100 = ótimo; é o espelho do `GaugeTema` e um não substitui o outro). **Mapa**: KPIs no padrão, azul decorativo removido, gráfico ECharts intacto (cor da barra = % negativo é dado). **Previsões**: ordem das seções intacta, curva da linha do tempo na MARCA (série única não compara nada), Subindo/Caindo com barra de intensidade proporcional à maior taxa da própria lista, nos tokens de sentimento. O chip de tema da pauta da Rádio Escuta também ficou neutro — **`temaColors.ts` não tem mais consumidor de UI** (permanece no repo).
- **Prévia 3 (mesmo dia) — Configuração, Relevância, Histórico de Alertas e Login** (`Desktop/aprovacao-novas-diagramacoes-3.html`): **Admin** sem reestruturação, só os pesos do score virando barras deslizantes na marca (`accentColor`) com valor na display. **Relevância** em **nuvem de pílulas** (clicar alterna ativa/desativada; ✕ remove via `ConfirmaModal` com a dica de preferir desativar) — a lista segue sendo do cliente. **Histórico de Alertas** em linha do tempo **agrupada por dia**, espinha chumbo, chips neutros de canal (o verde/azul decorativo saiu — canal não é sentimento) e chip de tema na marca. **Login/Aceitar Convite**: painel de marca em chumbo quente com o sol da marca flutuando (a cena da Estação Meteorológica); tile da logomarca e botão do convite corrigidos para tinta escura sobre a marca. **GlossaryPage está órfã** — nenhum menu roteia até ela; ficou fora do redesign de propósito (remover ou reintegrar é decisão de produto pendente).
- **O fluxo que valeu para todas: mockup estático de aprovação primeiro** (com abas quando são várias páginas, tema claro/escuro alternável e nota tracejada explicando a proposta em cada uma), implementação só depois do OK — e o OK pode vir com ajuste, que entra na implementação e fica registrado aqui.

### Coleta parada desde 27/07: teto da Apify, não bloqueio do Instagram (06/08)

- **A causa da "Última coleta não trouxe posts novos" era o TETO MENSAL DA APIFY**, não bloqueio do Instagram nem token expirado: `service_status` mostra 101,1% (US$ 29,33 de US$ 29,00), **travado nesse valor desde 27/07** — a Apify barra novos runs ao atingir o limite, então o consumo para de crescer e a coleta devolve 0 posts em toda execução. Recarregar é em apify.com/billing (o teto vem de `maxMonthlyUsageUsd` do plano, e o ciclo é o da assinatura, não o dia 1º).
- **O banner agora NOMEIA essa causa quando ela é conhecida**: com `service_status.apify` >= 100%, o aviso passa de três hipóteses ("bloqueio do Instagram, token expirado ou limite do Apify") para "Coleta parada: créditos da Apify esgotados", com o consumo e o que fazer. O dado já existia no painel (aba Monitor de coleta) e o aviso o ignorava. Só vale para o estado de coleta vazia: com o pipeline PARADO o problema é outro (não rodou), e crédito não explica. Valor monetário com **vírgula decimal** — "US$ 29.33" se lê como outro número em português.
- **Enquanto a Apify não for recarregada, o sistema não pode virar ruído** (decisão de 06/08, o cliente avisou que não recarregaria naquele momento). Duas mudanças, mesma lógica nos dois lados: **(1) o heartbeat** roda a cada 15min e, com a dedup padrão de 60min, mandaria um alerta por hora para sempre — e, com o WhatsApp fora do ar, uma issue por hora no repositório. Quando `service_status.apify` >= 100%, ele dispara `heartbeat_apify_sem_credito` com `janela_dedup_min=DEDUP_CREDITO_MIN` (1440 = 1x/dia) e mensagem que nomeia o serviço e o valor; sem esse dado, cai no texto genérico e na dedup de 60min. **(2) o banner** dispensa por CAUSA (`apify-sem-credito`) e não por execução, senão quem já sabe do problema fecharia o mesmo X 3x/dia. Volta sozinho quando o consumo cair abaixo do teto ou quando a coleta vazia passar a ter outra causa — os quatro cenários foram conferidos.
- **Crédito da API Anthropic recomposto em 06/08** pelo Robério. São dois serviços distintos e ambos param o produto de jeitos diferentes: sem Anthropic o pipeline **analisa** com defaults (incidente de 01/08); sem Apify ele não **coleta** nada. O alerta de créditos Apify já existe em `verificar_creditos_apify` (WhatsApp a partir de 80%), e depende do canal Evolution, que estava fora do ar em 31/07 — por isso o teto estourou sem ninguém ser avisado.

### Revisão de 06/08 (responsivo mobile + revisão ortográfica)

- **O dashboard não rola de lado no celular.** O `main` (App.tsx) ganhou `overflow-x-hidden`: com só `overflow-y-auto`, qualquer conteúdo mais largo que a tela fazia a página inteira "andar" para a direita e a esquerda no celular. Conteúdo largo legítimo (tabelas de ranking, monitor de coleta) continua rolando DENTRO do próprio card via `overflow-x-auto` — nunca na página.
- **A causa real do estouro era a linha de "Temas que merecem atenção"** (ClimaPage): badge de nível, contadores e botão são todos `shrink-0`, e no celular sobravam ~30px para o texto do tema — quebrava palavra por palavra (uma por linha) e ainda forçava a largura da página. No mobile o texto vai para uma linha própria de largura cheia (`flex-wrap` + `order-4 w-full`); no `sm+` a ordem volta ao layout de uma linha. Linha nova de lista com badge+texto+ações repete esse padrão, não o flex de linha única.
- **O aviso "Última coleta não trouxe posts novos" tem X de fechar** (`PipelineHealthBanner`, pedido do cliente em 06/08). A dispensa vale por EXECUÇÃO — o localStorage guarda o `executado_em` dispensado — então rodada nova que continue vazia faz o aviso voltar: cada execução vazia é um aviso novo. O estado "Radar parado" NÃO tem X de propósito: ele avisa que os números da tela estão defasados, e dispensável viraria um painel desatualizado em silêncio. No celular o carimbo "Última execução" desce para linha própria (antes era `shrink-0` na mesma linha e espremia o título numa coluna de poucas palavras).
- **Concordância do período centralizada no `PeriodoFilter`**: além de `periodoLabel`, existem `periodoFrase` ("nas últimas 24h" / "nos últimos 7 dias") e `periodoArtigo` ("as últimas 24h" / "os últimos 7 dias"). Texto novo que ponha a janela no meio de frase usa esses helpers — era comum colar "nos" fixo na frente e a tela exibir "nos últimas 24h".
- **Plural por concatenação é proibido em texto de UI**: `item{"s"}` gerava "items" (o certo é "itens"); "pauta(s) vieram", "captação(ões) falharam", "perfil(is)" e "crise(s) real(is)" viraram plural dinâmico com verbo concordado ("1 pauta veio" / "3 pautas vieram").
- **A pílula ativa do nav mobile usa a MESMA tinta escura do menu desktop** (`#04242F` desde 26/08, antes `#1A0F02`) — estava `text-white` sobre `var(--brand)` (2,24:1, reprovado no AA), a mesma regra de contraste de sempre que só o desktop seguia.
- **Acentuação nos textos GERADOS**: os três prompts com regra de estilo (`PROMPT_SISTEMA`, `PROMPT_BRIEFING`, `PROMPT_CACADOR`) agora exigem português do Brasil com acentuação/ortografia corretas e avisam que o corpo sem acento do próprio prompt não é estilo a imitar; o exemplo de diagnóstico do briefing foi acentuado e perdeu o travessão interno (contradizia a regra duas linhas acima). Era por isso que "saude publica", "gestao" e "critica" chegavam sem acento à tela. Textos já gravados se corrigem sozinhos quando o briefing do período for regerado (3x/dia). Critérios de classificação intocados — a triagem só devolve campos categóricos e não foi mexida.
- **Harness**: `radar-app/responsivo-dev.html` + `src/dev-responsivo.tsx` (mesmo padrão do icones-dev, fora do bundle) montam o banner de saúde, a lista de temas, a faixa do radar e a linha de topo da Rádio Escuta REAIS com as queries pré-populadas no cache do React Query, sem login — é onde se mede overflow em 375px sem depender do Supabase. `TemasEmCrise` ficou exportada da ClimaPage por causa dele.
- **Rádio Escuta no celular (2ª rodada de 06/08)**: os três cards da linha de topo apareciam CORTADOS na borda da tela. Causa: item de grid tem `min-width:auto`, e a linha do formulário de programa (input de nome + hora + min + ✕, os três últimos `shrink-0`) definia um min-content de ~470px — a coluna única do mobile ficava mais larga que a tela. Correções que andam juntas: `min-w-0` nos três itens do grid (RadioPage), formulário com `flex-wrap` e o campo nome em `basis-full sm:basis-0` (linha própria no celular), botões Editar/Pausar/Remover da lista em `w-full sm:w-auto` (linha própria — na mesma linha deixavam ~40px para o nome, uma letra por linha), cabeçalhos dos dois cards escuros com `flex-wrap`.
- **O teto dos cards escuros da Rádio Escuta agora é a var `--radio-altura-max` (index.css): `none` abaixo de lg, `470px` no lg+** — `superficieRadio.ts::ALTURA_MAX` consome a var. O teto existe para a LINHA lado a lado do desktop; empilhado no celular ele só espremia a lista de estações numa fresta de ~50px sob o formulário. O desenho aprovado do desktop (três cards em 470px) continua idêntico.
- **Plural entre parênteses saiu de TODA a UI**: "estação(ões)", "cadastrada(s)", "ativa(s)", "escolhida(s)", "monitorada(s)", "assunto(s)", "crítica(s)"/"elogio(s)"/"neutra(s)", "fonte(s)", "comentário(s) negativo(s)" e "post(s) avaliado(s)" viraram plural dinâmico com concordância real (inclusive na mensagem de WhatsApp do Alertar Secretário). Texto novo com contagem segue esse padrão, nunca "(s)".
- **Frentes de instabilidade mostram o rótulo do tema** (`TEMA_LABEL[f.tema] ?? f.tema`), não o slug cru "saude" do boletim.
- **O radar da Estação Meteorológica fica OCIOSO quando o pipeline está com problema** (pedido do cliente em 06/08: dizia "Radar em varredura" com o pipeline parado e "0 itens coletados hoje" ao lado). O critério de "em varredura" deixou de ser só "existe fonte ativa cadastrada": agora é fonte ativa E pipeline saudável, pela MESMA régua do banner de saúde — `PipelineHealthBanner.tsx::pipelineComProblema`, exportada e consumida por `useRadarKpis` (queryKey `["pipeline-health"]` compartilhada com o App, zero requisição extra). Banner aceso ⇒ radar ocioso, os dois indicadores nunca discordam. `health` sem leitura não acusa problema (o radar não abre em âmbar por falta de dado). O RadarSweep do Monitor de coleta da Configuração (AdminPage) segue no critério antigo de propósito: lá a pergunta é "há fontes configuradas para varrer", não "o sistema está coletando agora".
- **Reparo dos textos JÁ GRAVADOS em ai_briefings**: `python agora.py --reparar-acentos-briefings [N] [--dry-run]` acentua diagnóstico, alertas, oportunidades e recomendações das linhas existentes (uma chamada Haiku temp 0 por linha; validador NFD rejeita item a item qualquer mudança além de diacríticos/cedilha — reescrever dado histórico é pior que exibi-lo sem acento). Existe porque os prompts corrigidos só valem para texto novo e, com a coleta vazia, o main() aborta antes de regenerar briefing. **A máquina local não tem credenciais do backend** (não há `.env` na raiz; o CLI supabase não está instalado/logado — a nota antiga de "CLI logado nesta máquina" não vale neste Mac): o reparo roda pelo workflow `reparo-acentos.yml`, que usa os secrets do repo, dispara UMA vez sozinho no push de si mesmo e depois fica em "Run workflow" (sempre com um dry-run de auditoria antes do passo que grava, e regenerando semana/mês via `--testar-briefings-periodo` ao final).
- **O defeito de acentuação NÃO era só dos briefings: `posts` tinha 155 textos sem acento** (resumo, queixa_dominante, elogio_dominante), e é o Feed "O que o povo diz" que os exibe (`lib/resumo.ts::resumoProsaPost` cai em resumo/queixa/elogio) — 65% dos resumos longos. Achado em 06/08 varrendo TODAS as tabelas de texto gerado, não só a que o cliente apontou. Também saíram sem acento os quatro textos FIXOS de `agora.py::ABORDAGEM_POR_CLUSTER` (gravados em `boletins`): eles são texto EXIBIDO, então vão acentuados e sem travessão, ao contrário dos prompts, que são instrução para o modelo. `crisis_plans` tinha 2 itens e `radio_topics` está vazia.
- **A linha entre CORRIGIR e REESCREVER foi fixada em 06/08.** Entram no corretor: diacríticos, cedilha, crase, erro de grafia inequívoco ("Empatetico", "empaticocom", "juridiquese"), concordância que a acentuação resolve ("expoe" com sujeito plural vira "expõem"), "mal/mau", artigo faltando ("com a gestão") e travessão (proibido em texto gerado). **Não entram**: sugestões de reescrita que mudam a formulação da análise (coordenação "indiferente ou descaso", concordância de particípio distante, mistura de tempos verbais, ordem dos termos) e nome de pessoa cuja grafia não dá para confirmar ("Pro Edenia"). O texto é registro histórico do que a análise disse; reescrevê-lo é pior que exibi-lo imperfeito. Duas rodadas de revisão adversarial (4 + 3 lentes) sobre o antes/depois produziram 21 achados aplicados e 8 recusados por esta regra, com veredito final de zero acento ou crase indevidos.
- **O corretor determinístico virou `acentuar_textos.py` e cobre as duas tabelas.** `posts` é atualizado por **PATCH na url** (filtro `url=in.("…")` com a URL percent-encoded, validado contra o REST), **nunca por upsert**: um upsert com só as três colunas de texto apagaria o resto da análise do post. **PROTEGIDOS blinda trecho de classe gramatical divergente e roda ANTES dos n-gramas e do dicionário**, devolvendo o trecho acentuado à mão no fim — foi o teste de regressão dos briefings que exigiu isso: juntar os dois corpora fez "a maioria dos comentários do período **critica** a gestão" (verbo) virar "crítica à gestão", porque nos posts 16 das 17 ocorrências de "critica" são substantivo. Regra que ficou: ao ampliar o corpus, **rodar o corpus antigo de novo e exigir diff vazio** (ou só melhorias) antes de publicar.
- **O reparo via modelo falhou por CRÉDITO ESGOTADO da API Anthropic** ("credit balance is too low", o mesmo incidente de 01/08 — descoberto em 06/08 pela sonda do workflow; o run saía VERDE com efeito zero porque cada linha degrada em log e os logs do Actions não são legíveis sem token). Duas peças nasceram disso: **(1) `.github/scripts/diagnostico_reparo.py`** — sonda camada por camada (Anthropic, Supabase, contagem de briefings sem acento) que publica o veredito numa ISSUE de rótulo `reparo-acentos` (issue de repo público é legível pela API sem token; única, fechada quando tudo passa) e falha o job enquanto restar briefing sem acento; a falta de crédito é AVISO, não falha, porque existe a peça 2. **(2) `.github/scripts/acentuar_briefings.py`** — acentuação SEM modelo: mapa determinístico (correções exatas de grafia + n-gramas para os ambíguos + dicionário) derivado do corpus real das 8 linhas sem acento, conferido ocorrência por ocorrência ("critica" verbo fica sem acento, "esta semana" fica pronome, crase só onde a regência pede) e validado por revisão adversarial de três lentes antes do push. Só toca linha com diagnóstico longo sem NENHUM acento (idempotente; as demais linhas nem entram, porque os ambíguos só foram auditados neste corpus) e mantém o validador NFD. **O crédito foi recomposto pelo Robério ainda em 06/08**, então o reparo por modelo e a análise do pipeline voltam a funcionar; a sonda do workflow confirma no primeiro run ("Anthropic: OK"). O caminho determinístico CONTINUA sendo o principal do reparo, e a falta de crédito segue tratada como AVISO e não como falha: ele não depende de saldo nenhum e é o que garante o resultado no próximo incidente.

### Revisão de 07/08 (Divisão da Conversa — Share of Voice)

- **Tela nova "Divisão da Conversa"** (`pages/SovPage.tsx` + `lib/sov.ts`), aprovada pelo fluxo mockup-primeiro (`Desktop/aprovacao-share-of-voice.html`): quem domina a conversa — gestão, oposição ou imprensa. A fatia de cada lado é a conversa GERADA (comentários nos posts do lado), não o número de posts; "voz por publicação" mostra exatamente essa diferença (na base real a oposição publica 1/3 da imprensa e gera quase o mesmo volume). Cinco blocos: barra da divisão com veredito dinâmico (inclui delta contra a janela anterior de mesma duração), voz por publicação, evolução semanal (5 semanas ancoradas no post mais RECENTE — com a coleta parada, ancorar em hoje esconderia a curva; o rótulo carrega as datas reais), ranking de perfis e o quadrante voz × reação. Sem ECharts (chunk de 16 kB), barras em CSS puro. **TEMPORARIAMENTE admin-only (11/08/26, pedido do cliente)**: é atualização do SaaS ainda em teste, então o item de menu e a rota ficam atrás de `SO_ADMIN`/`RequireAdmin` no App.tsx até o cliente liberar — é só gate de UI (os dados que a tela lê, posts/comments, continuam visíveis a qualquer usuário nas demais telas; não há RLS a mexer, ao contrário da Rádio Escuta).
- **O lado é o `filtro` do cadastro de Fontes** (`mapaDeLados`), com fallback pela categoria do post para handle fora do cadastro — perfil removido das Fontes continua tendo posts na base, e sumir com eles distorceria a fatia dos outros. Cores: laranja da marca = GESTÃO (identidade, não sentimento — a mesma regra que tirou verde/vermelho da tela Fontes); oposição e imprensa em dois tons de chumbo por tema. **A reação negativa é SEMPRE na tinta vermelha `--sent-ink-neg`, nunca verde por faixa** (ajuste de 07/08 na primeira leitura do cliente: a 1ª versão pintava verde abaixo de 50% para dizer "cenário bom", e verde colado no rótulo "Reação negativa" lia como contradição — o juízo bom/ruim vem do valor, a cor só nomeia o que o número conta). Os cards também ganharam **legenda fixa explicando o referente do número**: é a fatia dos comentários que critica a GESTÃO entre os que tomam partido, inclusive nos cards de imprensa e oposição — a leitura errada "84,8% falam mal da imprensa" veio da primeira revisão, e o significado estava só num tooltip de hover (invisível em print e TV); mesma solução da legenda fixa do IAD de 28/07. A legenda também explica o **"sem sinal"** (segunda dúvida da mesma leitura): quando menos de `MIN_VOTOS_IAD` comentários tomam partido, o card não exibe percentual — o texto usa a constante, não um "10" solto, para acompanhar mudança de piso.
- **A reação negativa é SEMPRE "dos que tomam partido" (votos = pct×total), nunca a média de `pct_neg` ponderada por `comentarios_total`.** Achado da auditoria de 07/08 que precedeu a implementação (pedido do cliente: conferir se os dados traduzem a realidade de cada comentário): a média ponderada incluía posts SEM NENHUM comentário analisado (gravados 0%/0%), e a reação negativa da gestão na janela de 30d caía de 43,5% (contagem comentário a comentário) para 14,4% — um número que mentia a favor do cliente. Com votos como peso, post sem medição não puxa a média. Piso `MIN_VOTOS_IAD` por lado e por perfil ("sem sinal" no lugar do percentual — 6 comentários não sustentam um número).
- **A auditoria validou o resto** contra a tabela `comments` INTEIRA (3.572 linhas, paginada — o PostgREST devolve no máximo 1000 por chamada, e uma leitura sem `offset` produz amostra enviesada em silêncio): a fatia por agregado diverge < 2,1pp da contagem por comentário; os `comentarios_pct_*` dos posts batem 130/130 com o denominador documentado (indeterminados diluem); a amostra cega por estrato confirmou os rótulos, com um vazamento residual conhecido — ~17 elogios a opositor gravados como `positivo` (≤ 1pp no agregado; o reparo de 01/08 pegou 58, estes ficaram).
- **Números da auditoria que valem registro** (base 01/06–03/08, 2.507 comentários válidos de cidadãos): dos que tomam partido, 21,6% aprovam / 78,4% rejeitam a gestão na conversa toda; nos posts do PREFEITO (`@gustavoascarmo`) 69,7% aprovam; nos da PREFEITURA institucional 82,5% REJEITAM. O contraste pessoa × instituição é o achado — a conta institucional concentra cobrança de serviço (vacina, São João, saneamento 92% de rejeição por tema), a pessoal concentra a militância. **Conversa não é pesquisa de opinião**: quem comenta em página política é mais crítico que a população; os números medem a narrativa pública, não intenção de voto.
- Harness: `radar-app/sov-dev.html` + `src/dev-sov.tsx` (proporções reais, lado sem votos → "sem sinal", período vazio), sem login e sem rede.

### Revisão de 06/08 (créditos dos dois serviços, Relevância dentro de Fontes, relatório em PDF)

- **O número do crédito da Apify tem UMA fonte de escrita: `lib/creditos.ts`.** O cliente apontou a mesma linha de `service_status` exibida de dois jeitos: o banner de saúde dizia "US$ 29,33 de US$ 29,00" e a Configuração dizia "$29.33 de $29.00 · 101%". Conferido contra o banco: **o dado está certo** (29,3303 / 29,00 = 101,1%), a divergência era **só de exibição** — ponto decimal contra vírgula (em português "US$ 29.33" se lê como outro número), "$" contra "US$", e o percentual arredondado para inteiro num lado e gravado com uma casa no outro. Pior: a 101% a Configuração ainda chamava de "quase esgotados", contradizendo a barra cheia ao lado e o banner que já dizia "esgotados". A correção não foi acertar os dois textos à mão (foi assim que divergiram): `fmtUSD`, `fmtPctCredito`, `frasePctConsumo`, `nivelCredito` e `tituloCredito` moram em `lib/creditos.ts`, o cartão é um só (`components/CartaoCredito.tsx`, usado pela Apify e pela Anthropic) e o banner consome os mesmos helpers. **Percentual sempre com uma casa** — é a precisão com que o backend grava (`round(pct,1)`). A partir de 100% o título é "esgotados" e entra a linha "Consumo acima do teto contratado", porque a barra satura e sozinha ela esconderia o excedente. Harness: `radar-app/creditos-dev.html` + `src/dev-creditos.tsx` renderiza banner e os dois cartões LADO A LADO com o mesmo dado, nos quatro níveis — se a divergência voltar, aparece ali sem precisar de login de admin.
- **A Anthropic passou a ter medidor, ao lado do da Apify** (Configuração > Monitor de coleta). Os dois serviços param o produto de jeitos diferentes e só um era medido: sem crédito na Apify a coleta volta com 0 posts; sem crédito na Anthropic o pipeline **coleta, analisa com `_DEFAULTS_ANALISE` e o run sai VERDE** (incidente de 01/08). **O número é o CONSUMO ESTIMADO deste pipeline, não o saldo da conta**: a Anthropic não publica saldo por chave (não há equivalente ao `/users/me/limits` da Apify), então `agora.py::registrar_uso_anthropic` soma os tokens de cada resposta (`usage`) pelo preço de tabela do modelo (`_PRECO_ANTHROPIC`, USD por 1M de tokens; modelo desconhecido cai no preço de um Sonnet, que erra para MAIS — custo subestimado faz o alerta chegar tarde). A tela diz isso com todas as letras; exibir um "saldo" que ninguém mediu seria pior que não ter número.
- **O acumulado é MENSAL e mora na própria linha de `service_status`**: cada run soma o próprio custo ao que já estava lá e zera quando vira o mês (comparação pelo `atualizado_em` em UTC). Guardar só o custo do run daria uma barra que sobe e desce sem relação com teto nenhum; guardar em tabela nova exigiria migration, e **não há como aplicar migration a partir daqui** (o MCP do Supabase responde sem permissão e não existe `.env` com senha do banco) — o desenho foi escolhido para não precisar de DDL.
- **O teto vem de `ANTHROPIC_BUDGET_USD`** (default 25) e **não é limite técnico**: a API não corta em valor nenhum. Ele é a referência da barra e do alerta. **Lido em tempo de chamada** (`_teto_anthropic()`), a mesma regra de credencial dos outros arquivos.
- **A barra e o WhatsApp compartilham o limiar de 80%**, como já era na Apify: `registrar_uso_anthropic` dispara `alerta_suporte.disparar("anthropic_creditos", ..., janela_dedup_min=720)`. O rodapé do cartão mostra o **último disparo registrado em `alerta_historico`** (com o provedor que entregou, ex. `whatsapp:callmebot`), então o admin vê na tela que a mensagem saiu — ou que nunca saiu. Não confundir `alerta_historico` (avisos automáticos de saúde) com `message_log` (envio manual ao secretário, que alimenta a página "Histórico de Alertas").
- **A contabilidade nunca derruba uma chamada**: `_contabilizar_uso_anthropic` roda dentro do proxy `_MessagesMonitorado` em `try/except` mudo. Derrubar uma análise já paga por causa de um campo de `usage` ausente seria o pior negócio possível. Os campos de cache entram com o multiplicador certo (leitura ~0,1x, escrita ~1,25x) mesmo o pipeline ainda não usando prompt caching — para o dia em que passar a usar, o número não ficar errado em silêncio.
- **Relevância virou ABA dentro de Fontes e saiu da barra lateral** (pedido do cliente): as duas telas respondem à mesma pergunta — o que o radar olha —, uma pelo perfil e outra pela palavra. `RelevanciaPage.tsx` foi **removida** (página sem rota é código morto); `KeywordsSection` mora agora em `FontesPage.tsx`. Abas, e não os dois cards empilhados, porque cada lista é longa o bastante para empurrar a outra para fora da dobra. A regra do produto não mudou: **a lista de palavras é do CLIENTE** e o sistema nunca adiciona nem "melhora" nenhuma.
- **Relatório do clima em PDF** (card "Relatórios" na Análise do Clima): escolhe-se 24h/7d/30d e o arquivo abre NA TELA, num `ModalShell`, com botão de baixar. Três decisões que valem registro: **(1) o PDF é escrito à mão** (`lib/pdf.ts`, ~250 linhas, zero dependência) em vez de jsPDF (~350 KB no chunk de uma página) — a mesma regra que manteve o velocímetro em SVG puro; as fontes-base usam `WinAnsiEncoding`, então todo acento do português sai correto (o arquivo inteiro é montado como string ASCII com os bytes >127 escapados em octal, e é isso que faz a tabela `xref` ser calculável sem biblioteca). **(2) O resumo escrito é montado por CÓDIGO, não pelo modelo**: o relatório precisa sair inclusive no dia em que a API está sem crédito, que é justamente quando o briefing não foi gerado; o diagnóstico da IA entra como seção A MAIS quando existe. **(3) Os números são os MESMOS da tela** — `agrupar` saiu da `ApprovalPage` para `lib/agrupamento.ts` e é consumido pelos dois, porque um PDF que contradiz o painel de onde saiu é pior que não ter PDF. O recorte de comentários cruza `url_post` com os posts da janela (a regra de 27/07), o piso de `MIN_VOTOS_IAD` vale igual (sem sinal, o PDF **não** imprime IAD) e o arquivo é montado no navegador, sem subir para servidor nenhum — ele carrega @ e texto de cidadãos reais, o mesmo dado que a retenção da migration 009 protege. O período do relatório é escolhido DENTRO do card e não herda o filtro do topo da página, de propósito. Harness: `radar-app/relatorio-dev.html` + `src/dev-relatorio.tsx`. **O impresso carrega a identidade do painel desde 11/08/26** (pedido do cliente): banda de cabeçalho na marca de ponta a ponta (`#62C2CA` desde 26/08) com a logomarca (desde 26/08 o TIQUE do arquivo oficial, por `pdf.ts::tique`; era aro + miolo por Bézier em `pdf.ts::circulo/anel`) e TODA tinta sobre a banda em `#04242F` (a regra auditada dos botões de marca — nunca branco sobre a marca), chip do período em fundo escuro quase sólido (a receita dos chips sobre degradê), tiques laranja nos títulos de seção (o section-label em papel), KPIs e citações em painel off-white `#EDF3F4` com fio da marca, faixa da marca de 4,5pt no topo de TODA página (`pdf.ts::faixaTopo`, prependada antes do conteúdo, então a banda da página 1 a cobre) e quadradinho da marca no rodapé (`pontoRodape`). Tinta do texto na família do petróleo (`#04242F`/`#5B737D` desde 26/08; era o chumbo quente `#26282D`/`#5C6068`), trilhos em `#D9E4E6`; verde/vermelho seguem só em sentimento. Verificação sem browser: empacotar `relatorio.ts` com esbuild (stubs só de `data` e `PeriodoFilter`), gerar o PDF em Node e auditar os comandos do stream + xref byte a byte — foi assim que o layout foi conferido.
### Revisão de 07/08 (Pedidos do Povo: retórica política não é pedido)

- **O campo `pedido` só registra demanda de serviço público ou ação administrativa que a gestão pode EXECUTAR.** Caso de origem: "Ele tem que entregar e as chaves 🔑 da prefeitura e dar tchau" virou o pedido "entregar as chaves da prefeitura" (confiança 85) na tela Pedidos do Povo. O traço comum da contaminação é o DESTINATÁRIO: exigir a saída do gestor, punição política (cassação, conselho de ética), voto ou volta de outro político ("volta Paulo Cézar", "fazer uma limpa na câmara") e conclamação a protesto/manifestação não são pedidos — são opinião política em forma imperativa, que o sentimento já captura. O `PROMPT_COMENTARIOS` diz isso com o exemplo das chaves, e o schema inline de `montar_prompt_comentarios` repete a proibição.
- **`normalizar_pedido` é a safety net pós-modelo** (pedir no prompt não garante — a mesma lição do `completar_frase` da rádio). Os padrões `_PEDIDO_RETORICO` casam contra o PEDIDO extraído (curto, via `_norm`), nunca contra o texto do comentário — a mesma separação normalizador/reparo da localidade. **A dúvida resolve para MANTER**: só cai retórica inequívoca. Fronteiriços legítimos ficam de propósito: "exonerar todos os cargos comissionados", "trocar secretario de saude" e "demolir o viaduto" são demandas literais no texto de origem; "votar redução tarifa" é a câmara votando projeto (só "votar em" é eleitoral); "manifestar-se sobre a maternidade" pede comunicação da gestão (`manifestacao` não casa `manifestar`); "voltar com a micareta" é retomada de evento (`^volta\b` não casa o infinitivo "voltar").
- Medição de 07/08 na base real: 569 pedidos gravados lidos um a um, **8 retóricos confirmados (1,4%)**, e o filtro anula exatamente esses 8 — zero falso positivo nos 561 legítimos. Bateria de 29 casos em `_CASOS_PEDIDO`.
- **Segunda leitura de 07/08 (revisão dos fronteiriços, pedida pelo Robério): mais 12 caíram, por três motivos que não são retórica.** (1) **Cobrança sem objeto executável** ("fazer trabalho direitinho e perfeito", "realizar ações para mudar cenário atual", "cumprir promessas de campanha", "melhorar gestão municipal") — o pedido precisa nomear a AÇÃO e o OBJETO; juízo de modo não orienta ninguém. (2) **Pedido inventado de constatação ou eco do post** — "Alagoinhas n ta preparada 😢" virou "preparar Alagoinhas para eventos" debaixo de um post sobre **El Niño** (o modelo leu "eventos" como festa), e "Parabéns Luciano, a cidade está abandonada" (ironia sem demanda nenhuma) virou "melhorar gestão municipal". A caption do post foi decisiva nesses casos: ler só o comentário não bastou. (3) **Pedido dirigido ao VEÍCULO de imprensa, não à gestão** ("melhorar qualidade do jornalismo", "fazer matérias sobre Alagoinhas", "dar créditos ao criador", "publicar video do prefeito"). A lista `_PEDIDO_SEM_DEMANDA` cobre os três motivos; **ironia é indecidível por regex** ("cancelar programação do São João" sob o anúncio da festa é deboche; um futuro pedido genuíno de cancelamento vem com outra string), então os casos de ironia auditados entram como FRASE EXATA e quem cobre o futuro é o prompt. Sobreviveram de propósito, pela mesma régua: "consertar a cidade" e "investir em melhorias da cidade" (objeto material + demanda real de realocar a verba no texto), "trocar secretario de saude" ("pedi para sair… ou alguém mude urgente" — executável pelo prefeito), "divulgar o link no site" (comunicação DA gestão) e "continuar levando os óculos de Rui Barbosa" (pergunta com objeto específico; conf 55 já a marca para revisão na tela). Total das duas leituras: **20 de 569 (3,5%)**; bateria com 50 casos.
- Ferramentas (custo zero: só leem o pedido curto já gravado, nunca chamam o modelo): `python agora.py --teste-pedidos [N]` (bateria + varredura da base com o texto de cada suspeito; sem N varre tudo) e `python agora.py --reparar-pedidos [--dry-run]` (anula SÓ o campo `pedido`; sentimento, tema e localidade da linha ficam — a crítica que a retórica carrega já está contada no sentimento). O reparo da base rodou pelo `reparo-pedidos.yml` (mesmo padrão do reparo-acentos: dispara uma vez no push de si mesmo, usa os secrets do repo, dry-run auditável antes de gravar e verificação final que FALHA o job se restar pedido retórico).

### Revisão de 11/08 (estado vazio da Análise do Clima prendia o usuário)

- **O estado "Sem dados no período" da Análise do Clima (`ApprovalPage`) renderiza o cabeçalho COM o `PeriodoFilter`.** Antes ele voltava só título + card de aviso, e o aviso mandava "ampliar o período no seletor acima" — seletor que não estava na tela. Com a coleta parada (teto da Apify), a janela padrão de 7 dias fica vazia enquanto a de 30 dias tem 169 posts: a tela abria vazia e não havia como sair dali (o "30 dias" que o cliente clicou era o de OUTRA página — cada tela tem estado local de período). Regra geral: **estado vazio por janela precisa manter o controle que troca a janela** (a ClimaPage já fazia certo; TemasPage é vazio de histórico, caso diferente). A frase do vazio nomeia a janela via `periodoFrase` ("Sem dados nos últimos 7 dias"), e a dica de ampliar só aparece quando `dias < 30` — em 30d vazio ela vira "Nenhuma publicação coletada nos últimos 30 dias". Harness: `radar-app/aprovacao-dev.html` + `src/dev-aprovacao.tsx` (coleta parada: posts só entre 14 e 25 dias atrás; e base vazia), sem login e sem rede.

### Revisão de 03/08 (linha visual única dos pop-ups)

- **TODO pop-up do painel é o `components/ModalShell.tsx`** — casca única, referência aprovada pelo cliente (comunicado escuro): caixa azul-marinho profunda com fio de luz na aresta superior, tile de ícone à esquerda, chip de contexto em caixa alta, título grande, painéis internos um degrau mais escuros (`ModalPainel`) e botão primário em pílula clara (`ModalBotaoPrimario`). Consumidores: `ComentariosBairroModal`, `EvidenciaComentariosModal`, `PublicacoesModal`, `EnvioSecretario` e `ConfirmaModal`. Ao criar tela com pop-up, usar essa casca — não um `fixed inset-0` local, que é como as cinco versões anteriores divergiram.
- **A casca é escura FIXA nos dois temas**, e o jeito de conseguir isso é ela aplicar `theme-dark` na própria subárvore: assim todo conteúdo interno que usa tokens (`bg-2`, `line`, `txt-*`, `sent-ink-*`) resolve nos valores escuros **já auditados de contraste**, e nada reimplementa cor de texto de comentário. É por isso que o `ComentarioBox` continua sendo o mesmo componente dentro e fora do modal, sem prop nem variante. A alternativa que ficou pelo caminho — hex fixos no componente do modal e um contexto próprio de "superfície escura" — duplica a tabela de contraste que os tokens já resolvem.
- **`window.confirm` não é mais usado** (`ConfirmaModal` cobre remover rádio e excluir usuário): era o único box fora da linha visual, e o nativo não diz a consequência da ação. O botão destrutivo segue a receita semântica do painel (degradê claro vermelho + tinta `#04242F`), nunca fundo escuro com texto claro.
- **Botão dentro do modal leva cor de texto explícita.** `<button>` não herda `color`, então o preto do user-agent sumia no fundo escuro (bug do "Copiar" pego no preview).
- **A caixa do diálogo tem `:focus-visible { outline: none }` — e isso NÃO é remover indicador de foco.** Ela tem `tabindex="-1"` e recebe foco programático só para ancorar o teclado; sem a regra, o `:focus-visible` global (laranja 2px, index.css) desenhava uma moldura em volta do pop-up inteiro toda vez que ele abria pelo teclado. Os controles de dentro continuam com o anel, que é onde a navegação por Tab precisa dele.
- **A casca embute a acessibilidade uma vez, para todos**: `role="dialog"` + `aria-modal` + `aria-labelledby`, Esc fecha, clique no véu fecha, rolagem da página travada, foco inicial na caixa, **Tab preso dentro do modal** (sem isso o foco passava do último botão para os controles da página coberta pelo véu) e **foco devolvido a quem abriu** ao fechar. Botão de fechar novo herda tudo isso de graça; um `fixed inset-0` local não herda nada.

### Coleta pelo Instagrapi: sessão morta e o fallback pago (01/09/26)

- **"Sessão carregada" NUNCA significou "autenticada", e isso custou meses de Apify em silêncio.** `set_settings` só desserializa cookies e dados de dispositivo; ele não fala com o Instagram. O `IG_SESSION_JSON` do repo legado é de **22/06** e já estava invalidado: a v1 (`i.instagram.com`) respondia `LoginRequired`, tudo caía no endpoint PÚBLICO (`www.instagram.com/api/v1/users/web_profile_info`), o público devolvia **429 em 14 de 14 perfis**, a Apify assumia e **o run saía VERDE**. Custo medido em 01/09: posts US$ 0,156/run e perfis US$ 0,0322/run, 3x/dia = **~US$ 17/mês contra um teto de US$ 29 (58%)**, para fazer o que a via gratuita deveria fazer.
- **`_sessao_viva` (coletor_instagram.py) é a checagem que faltava**: uma chamada autenticada (`account_info`) logo depois de carregar a sessão. `LoginRequired`/`ChallengeRequired` = sessão morta → loga, dispara `alerta_suporte.disparar("instagram_sessao_invalida", …, janela_dedup_min=1440)` e levanta `SessaoInvalida`, que os três módulos (posts, comentários, perfis) já tratam caindo para a Apify. **Qualquer outra falha é INCONCLUSIVA e deixa passar** — 429 ou queda de rede na própria checagem não provam sessão morta, e confundir as duas é o mesmo erro de "falha de consulta não é ausência de dado" que este arquivo já documenta em outros pontos. A dedup é de 1 dia porque a condição dura até um humano gerar sessão nova.
- **Sessão recusada NÃO tenta o login completo, de propósito.** Um login negado 3x/dia é o caminho mais rápido para o Instagram endurecer a conta. Renovar é ação humana, e o alerta pede exatamente isso. **Cuidado com a armadilha que já aconteceu**: a primeira versão do detector punha o `raise` DENTRO do `try` do caminho 1, o `except Exception` de lá o engolia, o código seguia para o caminho 3 e o login era tentado assim mesmo (run 33470485272, que terminou em `PleaseWaitFewMinutes`). Por isso hoje cada caminho tem DOIS blocos: sessão **ilegível** (JSON quebrado) permite tentar arquivo/login; sessão **lida e recusada** para o run.
- **Duas correções que vieram junto e só valem quando a sessão for renovada** — sem elas, sessão nova continuaria batendo em porta fechada: (1) os caminhos 1 e 2 criavam o cliente com `usar_proxy=False`, então o `IG_PROXY` contratado nunca era usado e as requisições saíam do IP do runner (corrigido: quem decide é `_novo_cliente`, e `_NO_PROXY` já desliga sozinho sem proxy configurado); (2) `user_id_from_username` resolve pelo endpoint público, então `_info_do_perfil` passou a tentar `user_info_by_username_v1` primeiro, com fallback para o antigo. **Medição que fecha o diagnóstico: ligar o proxy NÃO mudou nada** (429 idênticos, 14/14), o que descartou o IP como causa e apontou a sessão.
- **A linha de log que antecede o alerta é ASCII de propósito**: um `UnicodeEncodeError` de console (cp1252 no Windows) ali mataria justamente o aviso que a função existe para dar.
- **DESFECHO (01/09/26): a conta de coleta foi REMOVIDA pelo Instagram, e a via gratuita acabou.** O login recusado não era senha nem IP: o Instagram respondeu *"We can't find an account with …"* — conta de raspagem derrubada, o que também explica a sessão invalidada por volta de 22/06 e todos os sintomas em cadeia. **Não existe sessão a renovar.** Os quatro `IG_*` foram apagados do repositório legado e os três do novo (0 restantes nos dois): sem `IG_USERNAME`/`IG_PASSWORD`, `disponivel()` (`coletor_instagram.py:360`) devolve false e os módulos 1a/2a nem são executados — sem tentativa, sem 429 e, principalmente, **sem o alerta diário pedindo uma renovação impossível**, que é o que o detector faria a partir de agora. O serviço de proxy (Webshare) perdeu o único consumidor e deve ser cancelado. **A Apify passa a ser o caminho único e assumido**, ~US$ 17/mês contra um teto de US$ 29 — que é o estado real desde ~22/06, agora explícito. **Não criar outra conta de coleta por impulso**: conta nova raspando é derrubada em meses e traz de volta o mesmo ciclo, com mais um bloqueio no caminho. Se a economia voltar a importar, o caminho durável para SEGUIDORES é a Graph API (Business Discovery), que não usa conta de coleta nem proxy; ela não substitui posts e comentários. **O que fica de valor permanente**: o detector (que faz a próxima falha silenciosa aparecer no mesmo dia) e as duas correções de caminho — proxy nos caminhos 1 e 2, e a v1 antes do endpoint público —, que voltam a valer se um dia houver conta de novo.

### Comportamento

- **O Google Sheets saiu do fluxo (01/08/2026).** O Supabase é o único destino: os módulos 5 (abas Radar/Comentarios_Analisados) e 5b (Briefing_Diario) foram removidos do `agora.py`, junto com `conectar_sheets`/`garantir_aba`, o import do `gspread` e os secrets `GOOGLE_SHEET_ID`/`GOOGLE_SA_JSON` dos workflows ativos (`agora.yml`, `retroanalise.yml`). A memória contextual (módulo 3) passou a agregar os últimos 7 dias direto da tabela `posts` do Supabase — mesmo conteúdo que a aba Briefing_Diario recebia (score médio, narrativa e queixa dominantes por dia). As abas Feedback e Padroes (alimentação manual, sem uso) morreram junto; se voltarem, é como tabela no Supabase, nunca reintroduzindo a planilha. Sem Supabase configurado o `main()` agora **aborta cedo** (antes gastava crédito gravando só no Sheets); a falha ao carregar o histórico de posts suspende os alertas do run (o fallback de dedup via Sheets deixou de existir). Scripts legados (`radar.py`, `radar_agente.py`, `run_pipeline.py`, `gerar_relatorio.py`) ainda importam `gspread`, mas estão fora do fluxo desde jun/2026 e `gspread`/`google-auth` saíram do `requirements.txt`.
- **Alertas POLÍTICOS são só manuais.** O disparo automático de WhatsApp pelo agente está desligado por padrão (`agora.py::_auto_dispatch_ativo`; religa via `tenant_settings.notification_config.auto_dispatch_whatsapp = true`). A detecção e o laço IRT continuam rodando; o envio ao secretário é feito pelo card "Alertar Secretário" do dashboard e fica registrado em `message_log` (colunas `tema`/`mensagem`/`sent_by_nome`, migration 006), que alimenta a página "Histórico de Alertas". **Isso é sobre conteúdo político (crise, tema em alta) — não confundir com o Alerta de Suporte** (seção própria mais abaixo), que é sobre a SAÚDE DO PIPELINE (parou, deu erro) e É automático de propósito: o cliente não está lá pra apertar um botão quando o próprio painel de monitoramento está fora do ar.
- **Relevância e Fontes são páginas da barra lateral, abertas a qualquer usuário** (não voltar para dentro da Configuração). O cadastro de fontes é unificado numa tela só: Instagram → `monitored_sources` (pipeline atual); YouTube → `sources` (nasce pausada). Não recriar as abas "Fontes (coleta)" e "Notificações" — foram removidas de propósito.
- Quem pode escrever o quê (migration 007): `relevance_keywords`, `monitored_sources` e `sources` aceitam qualquer autenticado do tenant. **Continuam admin-only**: `tenant_settings` (pesos de score e limiares de clima), `secretaries` e `profiles` — os limiares de clima ficam restritos para o cliente não maquiar os próprios números. Ao mover qualquer tela de config para fora do admin, lembrar que **só mudar a UI não basta**: sem ajustar a policy, a escrita falha silenciosamente no RLS.
- A seção **Narrativas** foi removida da UI (sidebar + página). O backend continua gerando os dados; não reintroduzir a tela sem pedido.
- **Influenciadores** não é mais item de menu, e desde 11/08/26 a seção (Mapa de Influência) também saiu de dentro de "Análise por Perfil" — ver o bullet REMOVIDO na revisão de 27/07.
- **Ranking de seguidores** (25/07) vive dentro de "Análise por Perfil": quem tem mais e menos seguidores, o total de cada conta e o saldo de ganhos/perdas por janela (última coleta, 24h, 7 dias). Série em `profile_metrics` (migration 008), gravada por `agora.py::gravar_metricas_perfis` a cada run e também sob demanda com `python agora.py --seguidores` (grátis via Instagrapi; `--com-apify` libera o fallback pago). O painel se atualiza sozinho a cada minuto. **O Instagram publica só o TOTAL de seguidores, nunca quem entrou ou saiu** — a tela fala em *saldo líquido* de propósito; não prometer lista de quem deixou de seguir.
- **Escuta do Rádio saiu do "V2 fora de escopo" e está implementada** (29/07/2026) — ver a seção própria mais abaixo. É **admin-only** (com RLS), como a Configuração; desde 11/08/26 a Divisão da Conversa também está admin-only, mas só temporariamente e só na UI (ver bullet da tela).

### Escuta do Rádio (29/07/2026, migration 011)

Coleta e transcrição de rádios locais, análise das pautas e envio ao secretário. Spec de desenho em `RADAR_ESCUTA_RADIO.md`. Peças: `coletor_radio.py` (captura), `radio_analise.py` (pautas), `agora.py::_contexto_radio/analisar_radio/teste_radio/expurgar_pii_radio`, `radar-app/src/pages/RadioPage.tsx`, `lib/radio.ts`, `components/AlertaRadio.tsx`.

- **É admin-only nos DOIS lados.** Item de menu escondido por `isAdmin` + página em `RequireAdmin` + RLS admin-only em `radio_transcripts`/`radio_topics`, **inclusive no SELECT**. As rádios moram em `sources` com `platform='radio'`, então a policy de select de `sources` também foi estreitada (`platform <> 'radio' OR is_admin()`) e a tela Fontes filtra `platform !== "radio"`: sem isso a estação apareceria na Fontes, que é aberta a qualquer usuário. Esconder o menu sem mexer no RLS seria cosmético (a lição da 007, ao contrário).
- **O cadastro de rádio fica DENTRO da tela Escuta do Rádio**, e não na tela Fontes, exatamente porque a Fontes é aberta a todos. Isso é uma exceção consciente ao "cadastro de fontes unificado numa tela só".
- **O ator já transcreve.** `radarp_traffic/radio-transcriber` grava o stream ao vivo e transcreve com Groq Whisper large-v3. Não existe serviço de transcrição nosso. Mas o `groqApiKey` é **required no input** e o ator não guarda a chave: sem o secret `GROQ_API_KEY` no ambiente e no Actions, a coleta encerra antes de chamar a Apify (de propósito, para não queimar um run que falha na largada).
- **O ator grava em TEMPO REAL**, então captura de 30 min é um run de 30 min: cada minuto de captura é um minuto pago na Apify. Daí três decisões: `durationMinutes` default 30 e não 60; `concurrency` = nº de estações (o run dura UMA captura, não N); e o timeout de espera nunca é o default de 300 s dos outros coletores — 300 s abandonaria toda captura acima de 5 min **depois** de já ter gasto o crédito.
- **A espera pelo run é PROPORCIONAL ao áudio gravado (`_timeout_da_espera`), nunca uma folga fixa.** A conta antiga era `duracao*60 + 600`, e os 600 s não bastaram: medido em 31/07, um run de 4 estações × 30 min levou 41min51s (711 s de overhead), o coletor desistiu aos 2400 s e **o run terminou SUCCEEDED 102 segundos depois** — crédito pago, transcrição pronta no dataset, zero bloco gravado. A folga hoje é `FOLGA_BASE_SEG` (600) mais `FRACAO_OVERHEAD` (0,5) do tempo de gravação. Duas coisas que a conta antiga ignorava: o overhead do Whisper cresce com o tamanho do áudio, e **`concurrency` tem teto de 4**, então acima de 4 estações o ator grava em LOTES e o run dura `duracao × nº de lotes`. Esperar demais custa um job ocioso; esperar de menos joga fora crédito já gasto.
- **`TETO_ESPERA_SEG` (195 min) fica DELIBERADAMENTE abaixo do `timeout-minutes` do step "Captar rádios" (200, job 230).** Se o Actions matar o job primeiro, o processo morre sem imprimir o id do run, e o resgate passa a exigir garimpar o console da Apify. É a mesma regra dos três lugares que guardam o teto de 120 min de duração: números que divergem fazem o job ser abortado no meio de uma gravação já paga.
- **`python agora.py --adotar-radio <run_id> [--dry-run]` recupera um run que já terminou** mas cujo resultado se perdeu (desistência por timeout, job abortado). O run_id sai no próprio log da desistência. Vale enquanto o dado existir na Apify — **retenção de 3 dias**, a mesma janela dos clipes. Ele reaproveita `_processar_brutos`, o mesmo caminho da produção: um resgate com implementação própria divergiria do pipeline justamente no dia em que importa. Enxerga o cadastro inteiro (`_fontes_todas`, ativas ou não), porque o bloco a recuperar pode ser de estação pausada depois da captação.
- **O envio de WhatsApp loga o CORPO da resposta, não só o status.** Um 404 da Evolution API é ambíguo entre "instância inexistente ou desconectada" e "este número não tem WhatsApp" (nos DDDs > 30 o JID brasileiro perde o nono dígito). Em 31/07 cinco testes seguidos do Alerta de Suporte renderam a mesma linha "HTTP 404" e nenhuma pista. Vale para `alerta_suporte._enviar_whatsapp`, `agora._enviar_whatsapp` e os steps de aviso ao grupo no `agora.yml`/`radio.yml` — nestes o `curl` mantém o `|| true` (o aviso não pode derrubar o job que já falhou), mas status e corpo passam a aparecer no log, com `::warning::` quando não entrega. Antes o `-s` somado ao `|| true` fazia canal quebrado e aviso entregue parecerem a mesma coisa.
- **O teto de duração é 120 min e vive em TRÊS lugares que precisam subir juntos** (30/07): `coletor_radio.coletar_e_gravar` (`min(120, ...)`), `DURACAO_MAX` da Edge Function e o `timeout-minutes` do step do `radio.yml` (130, com o job em 140). Subir o teto sem subir o timeout do workflow faz o job ser abortado no meio da gravação, e a captação inteira vira crédito gasto sem transcrição nenhuma — o pior resultado possível, pior que recusar o pedido. O limite do GitHub é 6 h por job, então a folga existe; o que não pode é os números divergirem.
- **Custo medido**: o único run de rádio bem-sucedido até 30/07 fez 10,3 min por US$ 0,14, ou ~US$ 0,014 por minuto de run. É o número que a tela usa para estimar o custo das durações longas. 120 min ficam na casa de US$ 1,65 por estação, contra um teto mensal de US$ 29.
- **Captação longa (>= 90 min) não foi testada na transcrição.** O maior bloco já transcrito com sucesso tem 10 minutos, e ninguém verificou como o ator fatia áudio de duas horas para o Whisper do Groq. A tela avisa isso na hora de escolher, em vez de deixar a pessoa descobrir depois de pagar a captação inteira. Antes de tratar 120 min como rotina, o teste honesto é uma captação de 90 min numa estação só.
- **Captura só na faixa horária do programa** (com `TOLERANCIA_MIN` de 20 min para atraso do cron). Fora do horário o ator gravaria a grade musical. **Fonte sem nenhum horário cadastrado fica FORA da captação automática** (revisão de 03/08): a regra do produto é gravar só no horário pré-determinado do programa ou sob demanda pelo botão GRAVAR — não existe gravação 24h nem "a cada execução". O fallback antigo ("sem horário captura sempre") foi removido de propósito; a tela de cadastro mostra "só grava sob demanda" nessas estações.
- **Uma rádio tem VÁRIOS programas** (`sources.config.programas`, lista de `{nome, hora_inicio, duracao_min, dias}` — revisão de 03/08, pedido do cliente: a mesma estação tem programas em horários diferentes). Cada programa é uma janela de captação independente: a duração da captura é a do programa NO AR (não a de outro horário da mesma rádio), o nome gravado em `radio_transcripts.programa` é o do programa que abriu a janela, e a trava anti-duplicidade é **por janela**, não por dia — senão a captura da manhã bloquearia a da tarde. O formato legado (campos soltos na raiz do config) continua lido por `coletor_radio._programas_de` e `lib/radio.ts::programasDe`; a tela grava sempre no formato novo. No formulário, cada programa tem seus próprios dias da semana; **`dias` ausente significa "todos os dias"** — o formulário carrega isso como os 7 chips acesos, nunca como seg-sex (mudaria o significado em silêncio ao salvar). A grade de crons do `radio.yml` (meia em meia hora, 06h-22h30) cobre qualquer horário cadastrado sem ajuste manual.
- **A rádio NÃO entra no IAD nem no clima.** Meia hora de locutor é um formador de opinião, não meia hora de opinião popular; somar ao IAD deixaria um único programa fabricar tempestade. É a mesma armadilha do "perfil político não é população" já resolvida nos comentários. O placar da tela é apresentado como **pressão da mídia**, e `voz='ouvinte'` é destacada à parte porque é o trecho mais próximo da voz do cidadão.
- **A janela não para na última palavra-chave: ela vai até uma FRONTEIRA de fala** (`_estender_ate_fronteira`). Depois de estabelecer o assunto, o locutor passa a falar em "eles", "isso", "essas pessoas" — e o corte fixo em `CONTEXTO_SEG` entregava meia fala ao modelo. Achado em 30/07 na pauta do Hospital Dantas Bião: 30 s de fatos (o desmentido do governador, a acusação a "representantes do povo", a inauguração com Lula e o prefeito) ficaram fora da janela e, portanto, fora da análise. A extensão para em publicidade/vinheta (`_FRONTEIRA_DE_BLOCO`), em pausa maior que `GAP_MAX_SEG` ou no teto de `EXTENSAO_MAX_SEG`.
- **Estender exige refundir E um teto DURO na janela final** (`JANELA_MAX_SEG`). Medido no mesmo dia: com extensão de 150 s cada intervalo encostava no seguinte, a fusão encadeava, o próximo estendia de novo, e um bloco de 30 min virou **uma janela de 11 minutos** — truncada em `MAX_CHARS_JANELA`, com a pauta do hospital sumindo dentro dela. O teto por intervalo é respeitado individualmente e a soma escapa dele; por isso o corte é aplicado depois da fusão. Efeito da correção no bloco real: 1 pauta virou 2 (a inauguração do Hospital Litoral Norte estava perdida) e o resumo passou a citar o desmentido do governador e quem foi acusado.
- **A citação é pedida em FRASES INTEIRAS, até 400 caracteres.** Citação cortada no meio faz o card parecer que a fala acabou ali, e quem lê conclui que não houve mais nada dito sobre o assunto — foi a queixa que originou esta revisão. O corte defensivo em `normalizar_pauta` subiu para 500 pelo mesmo motivo: cortar ali reintroduziria o problema que o prompt evita.
- **A frase é fechada por código, não por pedido ao modelo** (`completar_frase`). O prompt pede citação em frases inteiras, mas pedir não garante: numa medição de 30/07 uma das duas citações voltou terminando em "com profissionais comprometidos", sem pontuação. A extensão copia o próprio texto da janela até o primeiro `.!?`, no máximo 160 caracteres; se a citação não for encontrada no texto (o modelo parafraseou, o que o prompt proíbe), fica como veio — completar o que não se achou seria inventar fala. Depois disso, 3 de 3 citações do bloco real terminam em frase completa.
- **`intervalo_da_citacao` casa início e fim SEPARADAMENTE** (prefixo e sufixo). Derivar o fim do tamanho do prefixo que casou dava um terço da frase quando a citação inteira não casava — o que é o caso comum, porque o modelo reproduz a pontuação de um jeito e o Whisper de outro. Medido: a citação de 60 palavras ia até 834,9 s e o `ts_fim` saía 809,1 s.
- **O harness `--teste-radio` imprime resumo e citação INTEIROS.** Ele existe para julgar a qualidade da análise; truncar em 120 caracteres escondia exatamente o defeito procurado (citação terminando no meio da fala).
- **O portão de relevância vem ANTES do modelo, e tem dois estágios.** Medido na saída real do ator: 90% da grade é música e publicidade (as 357 palavras da 93 FM eram letra de música, dedicatória de ouvinte e comercial de laboratório, com zero política). Mandar transcrição crua ao modelo é pagar Haiku para classificar letra de música. Os dois estágios existem porque a regra da imprensa exige a âncora do município **no mesmo texto** da palavra genérica, o que é correto para post (autocontido) e errado para fala ao vivo: ninguém repete "Alagoinhas" a cada frase. Então `candidato` (qualquer keyword cadastrada) localiza o **segmento** e `gate` (a regra inteira, com âncora) decide sobre a **janela** de ~2 min em volta. A regra não foi afrouxada, só medida num texto maior — "em Catu a prefeitura de lá..." continua sendo descartado.
- **`radio_analise.py` não reimplementa NENHUMA regra**: recebe do `agora.py`, via `Contexto`, o gate de relevância, o `CRITERIO_TOM_PUBLICACAO`, o vocabulário de temas e o normalizador de localidade. Foi assim de propósito, porque PROMPT_TRIAGEM e PROMPT_COMENTARIOS passaram um mês divergindo por terem cada um a sua cópia do mesmo critério.
- **A seção chama-se "Rádio Escuta"** (era "Escuta do Rádio" até 30/07). O id da rota, os nomes de arquivo e as tabelas continuam `radio`/`RadioPage`/`radio_*`: renomear identificador de código por causa de rótulo de tela é troca sem ganho.
- **Cada citação tem um CLIPE de áudio no card** (migration 012, `radio_topics.audio_clip`, bucket privado `radio-clipes`). O instante `mm:ss` sempre esteve lá para permitir conferência, mas conferir exigia abrir o áudio na Apify — ninguém faz.
- **O clipe tem MEDIDOR DE NÍVEL em dBFS (30/08/26), e ele MEDE — não anima.** `components/MedidorNivel.tsx` lê o áudio que está tocando por um `AnalyserNode` (envelope de pico com ataque imediato e queda de 90 dB/s) e mostra escada de 20 segmentos de -60 a 0 dBFS, número em dígitos tabulares e **retenção de pico** (o traço que salta para o máximo e cai 14 dB/s depois de 800 ms parado). A distinção é o ponto: o painel já tem o `SinalVivo.OndasEq`, que é um SÍMBOLO de "tem áudio aqui" e não mede nada; um segundo desenho animado sem dado seria a mesma figura ocupando o lugar de uma medição. **Silêncio é dito, não desenhado**: ao pausar, a escada apaga e o número vira travessão, porque medidor congelado no último valor afirma um nível que não está mais saindo pelo alto-falante (verificado: pausa → `aria-valuenow` -60 e "— dB"). **Cor: teal na faixa normal e ÂMBAR nos últimos 6 dB.** Verde e vermelho seriam o óbvio num medidor e continuam proibidos — aqui eles são SENTIMENTO, e um medidor vermelho leria como "reação negativa" na tela que mede reação; âmbar já é a cor de atenção do painel. Segmento apagado usa `var(--line)`, nunca um cinza fixo: um claro a 13% some no card branco do tema claro, e a escada vazia deixaria de existir justamente no estado que ela precisa mostrar. **Três armadilhas que já estão resolvidas e não devem voltar:** (1) o `<audio>` precisa de `crossOrigin="anonymous"`, senão o grafo entrega SILÊNCIO e o que se perde não é o medidor, é o áudio — o Storage do Supabase responde `Access-Control-Allow-Origin: *` e atende preflight, então o modo CORS vale para a URL assinada (medido); (2) **UM `AudioContext` para a página inteira** (`contextoDeAudio()`), porque o navegador limita a ~6 e a tela tem um player por citação: com um contexto por player, a sétima citação conferida falharia sem explicação — e por isso o desmonte desliga o analisador em vez de fechar o contexto, que calaria os outros; (3) sem Web Audio ou com o áudio recusado, o componente **cai no `<audio controls>` nativo** em vez de mostrar medidor parado. O transporte próprio (tocar/pausar, régua de posição com `accentColor` na marca, tempo) existe porque o controle nativo não aceita nada dentro dele e o medidor empilhado por fora seriam duas linguagens coladas. Harness: a seção "Medidor de nível do clipe" do `radio-dev.html`, com um WAV de fala sintetizado no navegador e um estouro aos 9s — medido ali, o medidor chega a -1 dBFS e acende 2 segmentos âmbar.
- **A ESCALA do medidor foi validada contra uma pauta REAL em 30/08/26, e é isso que fixa o limiar da zona quente.** Clipe da pauta "Entrega do Hospital Litoral Norte" (357 KB, 22,30 s), lido pelo próprio medidor no harness com o áudio servido em mesma origem — 70 amostras ao longo do clipe deram **pico -14 dBFS, média -21,1 dBFS, zero amostras em silêncio e ZERO segmentos âmbar**. A leitura que importa: fala de rádio comprimida vive entre ~-42 dB (as pausas entre palavras) e ~-14 dB (os picos), então a escada de -60 a 0 usa a maior parte da própria extensão e a zona quente dos últimos 6 dB **não dispara em material normal**. Se o alarme fosse a -12, toda pauta piscaria âmbar e o aviso não significaria nada; **ao mexer em `DB_QUENTE` ou em `DB_MIN`, medir de novo contra um clipe real antes**, porque o número certo depende do material, não da teoria. O mesmo teste confirmou de lado o recorte do clipe: 22,30 s contra os 21,48 s da citação (767,48 → 788,96) são exatamente os 0,4 s de `FOLGA` em cada ponta, ou seja, o clipe continua sendo a frase citada e nada além. **Áudio de cidadão não entra no repo nem fica em `public/`**: o clipe baixado para o teste foi apagado ao fim, e a URL assinada descartada — é a mesma regra da retenção de 90 dias.
- **O clipe é a FRASE CITADA, do começo ao fim, e nada além dela.** É a regra, não uma preferência: o áudio existe para conferir a transcrição que está na tela, então ouvir outra coisa derrota o propósito. Os limites saem de `radio_analise.intervalo_da_citacao`, que acha o intervalo da frase nos segmentos do Whisper; `radio_clipes.janela_do_clipe` só acrescenta `FOLGA` de 0,4 s em cada ponta, porque `ffmpeg -c copy` alinha o corte no quadro MP3 (~26 ms) e sem folga a primeira e a última sílaba saem cortadas. Duas versões erradas ficaram pelo caminho: `ts_fim` gravava o fim da JANELA (~2 min de programa) e o recorte usava margem fixa (4 s antes, 20 s depois) em torno do início.
- **O casamento da citação é feito no texto CONTÍNUO dos segmentos, nunca dentro de um segmento isolado.** O Whisper corta em trechos de 3 a 5 s e qualquer frase real atravessa vários: procurar as 6 primeiras palavras dentro de um segmento não acha nada e cai no padrão (a janela inteira). `_mapa_de_segmentos` concatena o texto guardando o offset de cada segmento, e a posição encontrada é mapeada de volta. Se nada casar, devolve o padrão recebido — instante inventado num alerta que vai ao secretário é pior que instante aproximado, porque ele confere no áudio.
- **`_gravar_clipes` RECALCULA o intervalo em vez de ler o gravado**, e devolve o `ts_fim` corrigido ao banco: pauta anterior a 30/07 tem o fim da janela lá, e recortar por aquele valor daria um clipe que não é a frase. `python agora.py --clipes-radio [N] [--refazer]` (ou o `radio.yml` com `apenas_clipes` + `refazer_clipes`) reprocessa o que já existe.
- **Por que recortar, e não apontar para a Apify.** Três medições de 30/07 fecham a questão: o key-value store guarda o bloco inteiro (28,6 MB para 30 min); **não aceita Range** (pedir 100 KB devolveu os 28,6 MB), então um proxy baixaria tudo a cada seek; e a **retenção de dados do plano é de 3 dias**, contra 90 das pautas — um player apontando para lá estaria quebrado na maioria dos cards. `audio_clip` nulo é dito na tela como "sem áudio para conferir", nunca como um player que não toca.
- **O clipe é dado pessoal e sai no expurgo junto com o texto.** É o único artefato em que a pessoa é reconhecível pela VOZ; apagar a transcrição e deixar o áudio seria pior que não apagar nada. `expurgar_pii_radio` apaga os arquivos do bucket e zera `audio_clip` antes de expurgar a transcrição. O bucket é privado e o acesso é por URL assinada de vida curta, com policy de SELECT `is_admin()` em `storage.objects` — sem ela o `createSignedUrl` do navegador falha e o botão não abre nada.
- **A URL assinada é pedida no CLIQUE, não na montagem da lista**: trinta pautas na tela dispararia trinta assinaturas para ouvir zero ou uma, e a assinatura emitida junto com a página poderia já ter expirado na hora do clique.
- **O runner do GitHub NÃO traz ffmpeg** (medido em 30/07: o step registrou "ffmpeg ausente" e saiu sem recortar nada). O `radio.yml` instala com `apt-get` antes dos steps que recortam; sem isso a pauta é gravada e o card fica sem o botão de ouvir, em silêncio. A máquina do Robério também não tem — daí o comando avisar em vez de falhar.
- **Whisper alucina sobre música** (saiu "Suzy Allison Dance The Two Step" de uma letra em inglês). Transcrição não é citação literal confiável: toda citação exibida ou enviada leva o instante `mm:ss` (`ts_inicio`, achado no segmento que a contém, com fallback no início da janela — nunca instante estimado) e o aviso de que é transcrição automática.
- **"Não captada" ≠ "sem assunto".** Estação falha sozinha (a Rádio Boa voltou `RECORDING_FAILED` enquanto três gravaram, porque a URL cadastrada era uma página de player e não um stream). O `status` é gravado por estação, bloco com falha NÃO entra na análise, e a tela distingue os dois casos. Tratar falha como silêncio faria o painel afirmar que não houve pauta onde houve falha técnica.
- **Falha do modelo NÃO marca o bloco como analisado** (30/07). `analisar_janela` devolve `None` quando a chamada falha e `[]` quando o modelo leu e não achou pauta; só o segundo caso marca `analisado_em`. Antes as duas situações eram `[]` e o bloco era marcado de qualquer jeito: uma captação paga de 15 min ficou com zero pautas e sem chance de reprocessamento quando a API da Anthropic recusou por falta de crédito. O áudio já foi pago, e só o reprocessamento recupera o valor dele; o upsert de `radio_topics` é idempotente por `(transcript_id, ts_inicio, assunto)`, então reanalisar não duplica nada.
- **Interesse sem o "por que" volta a false** (`normalizar_pauta`): a pergunta do cliente é "por que interessa", e interesse sem justificativa é afirmação sem lastro na tela.
- **O box de envio é o MESMO componente do dashboard.** `components/EnvioSecretario.tsx` foi extraído do `AlertaCrise` e os dois consomem ele; o cliente pediu "igual ao que já existe", e a única forma de continuar igual é não haver duas cópias. Envio segue **manual**, cai em `message_log` e aparece no Histórico de Alertas sem migration nova.
- **Retenção de 90 dias** para transcrição bruta e `segments` (`RETENCAO_RADIO_DIAS`, `expurgar_pii_radio`), mais curta que os 180 dos comentários: quem liga para a rádio e se identifica no ar nunca escolheu falar com o sistema, e o valor analítico já foi extraído para `radio_topics`. Resumo, tema, tom, localidade e instante sobrevivem.
- **`coletor_radio.py` e `radio_analise.py` leem env em tempo de CHAMADA, não no import.** O `agora.py` chama `load_dotenv()` **depois** dos imports, então constante lida no import fica vazia quando as credenciais vêm de `.env`. O sintoma é escrita que retorna 0 sem erro nenhum, e só aparece na máquina local (no Actions as variáveis são de ambiente de verdade). Isso já custou um diagnóstico falso durante a implementação. Não voltar a usar constante de módulo para credencial nesses arquivos.
- **A captação tem workflow PRÓPRIO (`radio.yml`), e o `agora.py` só ANALISA.** O ator grava em tempo real, e o step do `agora.yml` tem `timeout-minutes: 20` (job 30): capturar dentro do pipeline derrubaria o ÁGORA inteiro — Instagram, clima, alertas e briefing — no dia em que o horário de um programa coincidisse com uma das três rodadas. O `radio.yml` tem `workflow_dispatch` com `dry_run` e `ignorar_janela` para teste manual, e analisa no fim também, para a pauta aparecer logo depois do programa em vez de esperar a próxima rodada do ÁGORA. **O cron NÃO tenta bater com o `hora_inicio`** (revisão de 03/08): o agendador do GitHub atrasou 1h52 a 2h55 nos três últimos disparos das 10:00 UTC. A estratégia é uma **grade uniforme de meia em meia hora cobrindo 06h-22h30 de Alagoinhas** (crons `9-23` e `0-1` UTC, minutos :07/:37) e o coletor **espera a janela abrir** (`--aguardar 45`, `minutos_ate_abrir`): cada chegada cobre 65 min (45 de espera + 20 de tolerância), então **qualquer horário cadastrado fica coberto sem tocar nos crons** — manhã, tarde ou noite; só programa de madrugada exigiria linhas novas. Run sem janela por abrir sai em segundos, e o Actions é grátis (repo público, mesma conta do heartbeat). A trava `_ja_captada_na_janela` (por janela de programa, não por dia) + `concurrency: escuta-radio` garantem **uma captura paga por janela**: o primeiro run que a alcança grava, os demais saem em segundos. **A janela é avaliada na hora local do tenant** (`RADAR_TZ`, default `America/Bahia`, fallback UTC-3 fixo): o coletor usava `datetime.now()` naive, e no runner UTC a janela "07:00" abriria às 04:00 de Brasília — bug latente que nunca disparou porque as estações estavam cadastradas sem `hora_inicio`.
- **Gravação sob demanda (30/07): o botão GRAVAR do painel dispara o `radio.yml`, e o caminho passa por uma Edge Function.** `components/GravarAgora.tsx` (topo da tela) → `lib/radio.ts::gravarAgora` → `supabase/functions/gravar-radio` → `workflow_dispatch`. O disparo **não pode sair do navegador**: acionar o workflow exige um token com permissão de Actions, e esse token no bundle daria a qualquer visitante do painel o poder de queimar crédito da Apify. A função confere `profiles.role = 'admin'`, valida as estações contra o tenant (o RLS protege a leitura, não o que a chave de serviço faz) e corta a duração em 60 min. **Pendência de configuração**: o secret `GH_DISPATCH_TOKEN` (PAT fine-grained, só "Actions: read and write") precisa ser cadastrado com `supabase secrets set`; sem ele a função responde 503 com a instrução, e o botão diz isso na tela em vez de falhar em silêncio.
- **O MESMO botão para a captação (30/08/26): GRAVAR vira PARAR quando há algo no ar.** Três coisas sustentam isso, e nenhuma é cosmética. **(1) O estado vem da APIFY, não do GitHub.** O `workflow_dispatch` é disparar-e-esquecer (o GitHub responde 204 sem id de run) e **cancelar o job NÃO interrompe a gravação**: o ator roda na infraestrutura da Apify e continua gravando e cobrando — é o caso já medido em que o coletor desistiu por timeout e o run terminou `SUCCEEDED` 102 s depois. Por isso a Edge Function ganhou as ações `estado` e `parar` (mesmo endpoint, mesma checagem de admin), que listam os runs `RUNNING` do ator e abortam por `POST /v2/actor-runs/{id}/abort`; cancelar os jobs do `radio.yml` é o passo SECUNDÁRIO, para o runner não ficar esperando um run que já morreu. **(2) Parar DESCARTA o áudio, e a tela diz isso antes.** O ator só transcreve no fim do bloco, então bloco abortado não vira pauta nenhuma: os minutos já consumidos não voltam, e parar evita só o que falta. Confirmação pelo `ConfirmaModal` (a casca única do painel), com essa consequência escrita. **(3) O `INPUT` do run é lido SÓ no servidor**, e dele saem apenas `durationMinutes` e os nomes das estações — esse mesmo objeto carrega o campo `groqApiKey` (que a Apify guarda cifrado) e o resto da configuração do run, e nada disso tem por que chegar ao navegador. **Sem o secret `APIFY_API_TOKEN` na função o painel não sabe o que está no ar e NÃO oferece PARAR** (`indisponivel: true`, degradação silenciosa): melhor só GRAVAR do que um PARAR que não para. Visualmente os dois estados **não se parecem** — GRAVAR é a pílula teal chapada com tinta escura; gravando, o botão vira o MEDIDOR da captação (trilho petróleo, avanço em teal por `width`, equalizador do `SinalVivo` e o tempo que falta em `tnum`), com tinta clara medida nos dois lados da fronteira do avanço (18,13:1 no trilho e 8,80:1 no cheio, pior caso). Um toggle que só troca a palavra convida ao clique errado, e aqui o clique errado ou queima crédito ou joga fora áudio pago. **Vermelho continua proibido** como cor de controle: neste painel vermelho é sentimento. Harness: `radar-app/radio-dev.html` + `src/dev-radio.tsx` (cinco cenários, com a Edge Function dublê — o ciclo inteiro roda sem login e sem rede; o dublê troca a PROPRIEDADE `functions` do cliente, porque no supabase-js v2 ela é um getter que devolve um cliente novo a cada acesso e remendar `.invoke` na instância deixa o pedido vazar para produção). **Pendência de configuração: `supabase secrets set APIFY_API_TOKEN=...` e o redeploy da função** — sem os dois o botão segue só gravando, como antes.
- **O PARAR foi testado de ponta a ponta em 30/08/26, com captação paga de verdade, e os números confirmam o desenho.** Roteiro: `APIFY_API_TOKEN` cadastrado na função (`supabase secrets set`) e `gravar-radio` publicada na versão 8; 15 min disparados na DIGITAL FM 96.3 pelo mesmo `workflow_dispatch` que o botão usa; PARAR clicado na tela pelo Robério aos ~2 min. Resultado medido: o run da Apify terminou **`ABORTED`** (04:32:41 → 04:34:57 UTC, 136 s) e o job do `radio.yml` terminou **`cancelled`** — as DUAS metades, que é o ponto: só cancelar o job deixaria o ator gravando os 15 min inteiros. **Custo: US$ 0,0035 contra os ~US$ 0,21 do bloco completo**, ou seja, parar aos 2 min evitou 98% do gasto do run. Outros três fatos que só o teste real deu: o run aparece na Apify **33 s** depois do disparo (o estado "Iniciando…" dura menos do que o minuto que eu estimava); a leitura do `INPUT` devolve `durationMinutes` e o nome da estação corretos; e o campo `groqApiKey` **está** no `INPUT` do run — mas a Apify o guarda CRIPTOGRAFADO (`ENCRYPTED_VALUE:<...>:<...>`, medido em 30/08), então o que vazaria devolvendo o `INPUT` cru seria um texto cifrado, não a chave. O filtro no servidor continua certo por princípio (o navegador não precisa do interior do run para desenhar um contador, e a criptografia é da Apify, não nossa), mas **não** é ele que impede um vazamento de chave em claro — a frase anterior deste arquivo dizia isso e estava errada. O token do `.env` local enxerga o ator (`/v2/acts/radarp_traffic~radio-transcriber/runs` → HTTP 200), então ele não é de conta órfã — o que ainda não prova que seja idêntico ao secret do repo legado, coisa que só o primeiro ÁGORA do repo novo decide.
- **O card de gravar é QUADRADO e fica no canto superior direito**, na mesma linha da antena e dos indicadores (`lg:grid-cols-6`: antena 1 + KPIs 3 em 2×2 + gravar 2, com `ml-auto max-w-[380px]` e `minHeight` 340 — medido, 380×388 em telas de 1280 e 1680). A primeira versão era uma faixa retangular de largura cheia acima de tudo, e empurrava a página inteira para baixo por causa de um controle de uso eventual.
- **A escolha lista o CADASTRO inteiro, não só as ativas, e rádio pausada pode ser gravada sob demanda.** `active` governa a captação automática no horário do programa; recusar uma estação cadastrada por estar pausada seria negar um pedido explícito de gravar agora. Por isso `coletor_radio._fontes_por_id` (usada só pela gravação sob demanda) não filtra `active`, e a Edge Function também não — mas a captação automática segue lendo `_fontes_ativas`, então pausar continua tirando a estação do horário. A tela marca a pausada com um selo, para ninguém achar que ela também grava sozinha.
- **`sources` NÃO tem coluna `tenant_id`** — só `profiles` tem. O isolamento entre clientes neste produto é por **projeto do Supabase**, não por coluna nessa tabela. A primeira versão da Edge Function filtrava `sources` por `tenant_id`, o PostgREST devolvia `42703` (coluna inexistente), o erro era engolido e o botão acusava "nenhuma das rádios escolhidas existe neste cadastro" com quatro rádios cadastradas na tela ao lado. Antes de filtrar por tenant em qualquer tabela, conferir se a coluna existe lá.
- **Falha de consulta não é "nenhuma linha encontrada".** Tratar as duas do mesmo jeito foi o que mandou procurar o problema no cadastro, que estava certo, em vez de na consulta, que estava errada. Toda leitura da Edge Function checa `error` antes de olhar o tamanho do resultado.
- **`--estacoes` implica ignorar a janela horária.** `coletar_e_gravar(somente_ids=...)` liga `ignorar_janela` sozinho: quem apertou GRAVAR está pedindo agora, e exigir as duas coisas faria o botão não fazer nada justamente fora do horário do programa, que é quando alguém aperta um botão de gravar. Estação pedida que não está ativa é logada como pedido não atendido, nunca como "nada a coletar".
- **O card "Rádios monitoradas" (cadastro) não se mistura com o box de gravar.** Lá se define quais estações existem e em que horário elas gravam sozinhas; no box do topo se pede uma captação avulsa. São perguntas diferentes e continuam em lugares diferentes — mas desde 30/07 ficam **lado a lado na linha de topo**, e não a uma rolagem de distância: o cadastro saiu do card claro no rodapé (`CadastroRadios`, removido) e virou `components/RadiosMonitoradas.tsx`, no lugar onde estavam os quatro indicadores. Os KPIs desceram para a linha seguinte, em 4 colunas — o espaço do topo é de controle, e eles são leitura. A linha ficou antena (1 col) + cadastro (3) + gravar (2).
- **Os dois cards escuros da Rádio Escuta usam a MESMA superfície, importada de `components/superficieRadio.ts`.** O cliente pediu o cadastro "com o mesmo layout do Gravar agora", e duas cópias das constantes só ficam iguais enquanto ninguém mexe numa delas (a lição do `PeriodoFilter` e do `ComentarioBox`). Igual quer dizer o mesmo degradê, as mesmas superfícies internas e a mesma tinta.
- **Os dois cards têm teto de altura (`ALTURA_MAX`, 470px), e o teto é o MESMO nos dois.** Os comentários já diziam que a lista rola por dentro "para o card não crescer com o número de estações", mas `flex-1` sem teto não faz isso: com quatro rádios a linha ia a 565px e empurrava indicadores e pautas para fora da dobra — o mesmo defeito que tirou o "Gravar agora" da faixa de largura cheia. Teto diferente entre os dois deixa faixa vazia embaixo do mais baixo, porque o grid dimensiona a linha pelo item mais alto.
- **Editar é operação de primeira classe no cadastro de rádio** (`lib/radio.ts::updateRadio`). Antes, mudar o horário do programa exigia remover e cadastrar de novo, o que perdia o `peso` e o estado de ativação. O formulário é UM só e alterna entre cadastrar e editar (dois formulários lado a lado deixariam a dúvida sobre qual está valendo); o item em edição ganha anel laranja em vez de mudar de fundo, para continuar legível. O `peso` viaja junto no update mesmo sem campo na tela — gravar o `config` sem ele apagaria a audiência a cada edição de horário. A edição passa pela mesma `validarStream` do cadastro: uma URL de página de player colada aqui faria a captação falhar depois de já ter sido paga.
- **`--brand` (index.css) é um hex ÚNICO, `#62C2CA` (26/08/26, o teal do manual da marca; foi `#F79641` de 04/08 a 26/08 e `#FF6A2B` entre 03 e 04/08), igual nos dois temas desde 31/07.** A troca de 04/08 recalibrou a FAMÍLIA inteira, não só o hex: derivados de marca (`--brand-glow`, meshes, sombras rgba, sol do Login, série da linha do tempo, destaque do Mapa de Influência) foram para a família dourada do `#F79641`, e os tons de TEXTO re-medidos (ver bullet do `--brand-text`). O laranja de risco/alerta continua intocado (sistema paralelo, bullet abaixo). Até então o token trocava de tom entre o claro (`#FB923C`) e o escuro (`#EA580C`), e cada botão que precisava do "mesmo laranja" nas duas telas replicava um degradê de dois stops pra disfarçar a troca — só que aí o próprio botão passava a ter dois tons dentro de si, e cada cópia do degradê saía ligeirinho diferente da vizinha (a queixa do cliente em 31/07: o filtro de período e o "Mais citados" do Feed liam como dois laranjas distintos lado a lado, mesma tela). A correção foi na raiz: um hex só no token, e **todo botão de marca usa `var(--brand)` ou a classe `bg-brand` chapados — nunca mais um degradê de dois tons pra representar a marca.** Isso zerou os degradês hardcoded em `PeriodoFilter`, `App.tsx` (pílula do menu), `superficieRadio.ts` (`FUNDO_LARANJA`, usado por Gravar Agora e Rádios Monitoradas), o card "Engajamento no período" (ClimaPage), o botão "Alertar Secretário" (AlertaRadio) e a logomarca de Login/Aceitar Convite.
- **Tinta sobre `var(--brand)` é sempre `#04242F` (o petróleo do manual), nunca branco.** Branco sobre `#62C2CA` mede 2,08:1 e reprova o AA (mínimo 4,5:1); a tinta escura mede 7,77:1 — e o par teal/petróleo é o que a própria identidade define, então a regra deixou de ser uma escolha nossa. Antes da paleta nova a tinta era `#1A0F02` sobre `#F79641` (2,24:1 branco, 8,44:1 escuro); a regra é a mesma, o par é que muda. Isso já era a regra nos cards de degradê (reunião de 24/07), mas o app tinha **~15 botões `bg-brand text-white`** que a ignoravam (abas e botões de salvar em AdminPage, BairrosPage, CommandCenter, FontesPage, PedidosPage, RelevanciaPage, TrendsPage, EnvioSecretario) — todos com contraste reprovado, mais visível ainda no tema claro. Corrigidos para `text-brand-ink`, um token novo no `tailwind.config.ts` (`brand: { DEFAULT: "var(--brand)", ink: "#1A0F02" }`, mesmo par que `lime.ink` já usava). `bg-brand/10`, `bg-brand/5` e `border-brand` (tinta normal por cima, não texto sólido) não tinham esse problema e não foram tocados.
- **`brand.2` e `skycard` (tailwind.config.ts) viraram alias de `var(--brand)`, não mais hex hardcoded.** Eram cópias estáticas do laranja de antes da unificação — uma delas (`skycard.DEFAULT: #FB923C`) presa ao tom do tema escuro mesmo usada em telas de tema claro (foco de input em Login/Aceitar Convite). Ficaram como alias pra não haver uma terceira fonte de laranja no app; os usos (`text-brand-2`, `focus:border-skycard`) não precisaram mudar de nome.
- **O laranja de "atenção/alerta" (`#F97316` e primos, em `chartTheme.ts`, `temaColors.ts`, bandeiras de risco, ícones de alerta) é um sistema PARALELO ao `--brand`, de propósito — não confundir os dois.** `index.css` já documentava isso ("Marca — laranja, reservado para interação/marca, nunca estado de alerta"). A unificação de 31/07 tocou só botões de marca/interação; threshold de gráfico, chip de tema "Obras", ícone de sino de alerta e o destaque do Mapa de Influência continuam no laranja de risco, sem relação com este token.
- Ferramentas:
  - `python agora.py --radio --estacoes <id1,id2> --duracao 30` — mesma captação sob demanda que o botão dispara, pela linha de comando.
  - `python coletor_radio.py --autoteste` e `python radio_analise.py` — testes das funções puras (janela horária, janelas, instante, normalização). Zero rede, zero token.
  - `python agora.py --radio-dry-run [--agora]` — captura e loga sem gravar; `--agora` ignora a faixa horária (teste manual).
  - `python agora.py --radio` — captura e grava. `python agora.py --analisar-radio [N] [--dry-run] [--refazer]` — analisa os blocos pendentes.
  - `python agora.py --teste-radio [N]` — analisa transcrições já gravadas e imprime, **sem escrever**. O que olhar: se uma estação só produz um tom, o modelo está deduzindo pelo microfone em vez de ler a fala.
  - `python agora.py --expurgar-radio [N] [--dry-run]`.
- Validação de 29/07 contra transcrição real e sintética: as 3 estações de música geraram **0 janelas e 0 chamadas ao modelo**; numa transcrição com fala política o modelo separou crítica de locutor, crítica de **ouvinte** e elogio (creche entregue) na MESMA estação, extraiu bairro e pedido, e **não** gerou pauta da menção a Catu que estava dentro da mesma janela.

### Revisão de 11/08 (correções P1 da avaliação de UI/UX)

- **`chartTheme.ts::chartInk` fala a família da página** — QUENTE de 03/08 a 26/08, FRIA desde então: eixo = a própria tinta `txt2` do tema (`#3E5B66`/`#A9C0C6`), grade e trilho (`#E1EBED`/`#183541`/`#1B3A46`), tooltip na COR DO CARD (`#FDFEFE`/`#0C3240`), nunca branco puro. Os valores anteriores eram o slate azulado de antes do redesign de 03/08 e o gráfico parecia colado de outro produto dentro do card creme — o mesmo defeito que trocou o mesh escuro de índigo para laranja. Contrastes medidos no comentário da função (7,88/7,36 eixo claro; 9,24 escuro; 14,52/15,24 tooltip). O neutro de série acompanhou: `COLOR_SENTIMENT.neu` (`#ABA598`) e `COR_OUTROS` das Previsões (`#A29C90`) são cinzas quentes de luminância equivalente aos azulados que substituem. **Os slates de superfície escura FIXA (ModalShell, cards da Rádio Escuta, weather.ts) ficaram** — foram auditados para aquelas superfícies, não são gráfico.
- **Toda página carrega com `components/EsqueletoPagina.tsx`**: H1 real da página (o topo não salta quando o dado chega) + blocos de vidro com brilho de varredura (`.esqueleto-bloco`, index.css — o brilho anda por TRANSFORM no ::after, nunca background-position: repaint por quadro é a mesma armadilha do rAF; `prefers-reduced-motion` desliga o brilho e mantém os blocos). Substituiu os nove "Carregando…" de texto puro que colapsavam a tela inteira a cada troca de página. Componente ÚNICO (a lição do PeriodoFilter): página nova usa ele, nunca um "Carregando…" local. Os carregamentos de SEÇÃO (dentro de card: Rádio Escuta, abas da Configuração) continuam texto curto de propósito — ali o layout em volta já está de pé.
- **Box de engajamento da landing com concordância real**: "comentário analisado"/"comentários analisados" e "1 publicação"/"N publicações" (saía "1 publicações", contra a regra de plural dinâmico deste arquivo).

### Revisão de 21/08 (VIRATEMPO: rebrand + modelos aprovados na prévia)

Fluxo mockup-primeiro de sempre: prévia estática com abas e tema alternável (`Desktop/aprovacao-viratempo.html`, também publicada como artifact privado), aprovada pelo Robério com um ajuste: **no céu adaptativo, as FOTOS de clima continuam sendo o fundo do card**; rebrand mantido "com possibilidade de alteração futura"; todos os demais modelos aprovados. Endereço novo confirmado: `viratempo.surge.sh`.

- **Rebrand**: além do bullet da marca no topo deste arquivo, os textos de WhatsApp dos alertas (AlertaRadio/AlertaCrise, agora.py) trocaram "— Avaz" por "· Viratempo" (o travessão saiu junto, pela regra de texto), o PDF do relatório imprime "VIRATEMPO" com o subtítulo "RADAR DO CLIMA POLÍTICO" (corpo 19, era 21: o nome é mais longo), o arquivo baixa como `viratempo-clima-*.pdf` e o `alerta agora` do agora.py perdeu o "post(s)" (plural dinâmico). Nome de workflow, manifest, títulos e deploy.bat acompanharam.
- **Horizonte (céu adaptativo)**: a condição do clima tonaliza a ATMOSFERA do hero via dois tokens novos em `weather.ts` (`heroGlow`, a cor do radial do canto que era laranja fixo, e `heroWash`, lavagem diagonal de alfa baixo), com transição de 700ms. As camadas ficam SOB a foto e sob o conteúdo: as fotos, o fade, o `inkFoto`/`veuFoto` e toda a medição de contraste de 04/08 ficaram intocados. **Os chips de contagem NÃO voltaram ao hero**: a decisão de 25/07 (contagem vive no box de engajamento) continua valendo mesmo com a prévia tendo mostrado chips — o ajuste do cliente foi sobre as fotos, não sobre reabrir aquela decisão.
- **Mesa de comando**: virou o miolo da própria landing, não uma página nova — `CommandCenter.tsx` segue órfão (junto de CrisisCenter/TrendsPage/AssistantPage) e não foi roteado. No `xl` o diagnóstico (2/5) e os temas que merecem atenção (3/5) sentavam lado a lado em grade bento (mesmo racional das duas colunas do Feed; abaixo do xl e quando só um existe, empilha como antes, e os cards ganharam `h-full` para a linha não ficar dentada) — **essa dupla foi desfeita em 27/08**: o diagnóstico subiu para a faixa nobre e os temas passaram a dividir a linha com as sugestões (ver "Revisão de 27/08"). O diagnóstico ganhou o carimbo de origem "análise gerada pela IA". O tile da Divisão da Conversa ficou de FORA de propósito: a tela é admin-only temporária (11/08) e um tile na landing furaria o gate.
- **Trajetória do clima** (TemasPage): o card "Linha do tempo do clima" mudou de rótulo para "Trajetória do clima" (identificador `TimelineClima` fica) e os dias notáveis que o modelo JÁ calculava (`marcados`, que estavam sem consumidor visual desde 25/07) voltaram como NÓS na curva: vermelho = pico ou virada para pior, âmbar = alívio (a cor do "estabilizado"), marca = último ponto, com legenda fixa embaixo. O rótulo do último ponto é **"última leitura", nunca "hoje"**: com a coleta parada o último dia com dado pode não ser hoje. Balões de texto continuam proibidos (o tema do dia segue no tooltip, que é a parte da decisão de 25/07 que permanece). O traçado se desenha na entrada (animação nativa do ECharts, roda uma vez). O rótulo do último nó fica à ESQUERDA do ponto: centrado em cima ele cortava na borda do grid.
- **Termômetro de temas em FAIXAS** (TemasPage, `TermometroFaixas`): ranking dos temas tingido pela temperatura da crítica, na receita semântica auditada (degradê claro da família do sentimento + tinta `#04242F`, `RECEITA_FAIXA` com 6 degraus), volume em barra neutra que cresce por `scaleX` (`.vt-crescer-x`, index.css), tendência por tema e decomposição (neg/neu/pos) abrindo NO LUGAR ao clicar. Entra entre Subindo/Caindo e Dentro de cada tema — **a ordem de 01/08 fica intacta**, e ele NÃO substitui nem os velocímetros da ClimaPage (decisão de 24/07) nem a régua neutra dos Pedidos (04/08: cor semântica não cabe lá, o dado dos Pedidos é volume).
- **Harnesses**: `radar-app/temas-dev.html` + `src/dev-temas.tsx` (TemasPage real com 35 dias de daily_themes sintéticos contando uma história de crise e alívio, sem login e sem rede); o dev-clima ganhou o cenário D (chuva + briefing, para ver o céu adaptativo e a grade bento). Atenção ao verificar por screenshot no painel embutido: com a aba OCULTA o rAF congela e a captura sai de quadro rasgado — a "linha branca" atravessando o gráfico que apareceu na verificação era artefato de captura, não defeito (confirmado bisectando até o código original).
- **Pendência de infraestrutura**: o repositório remoto novo (`vaporfilmes-ops/radar-politico`) ainda estava sem os secrets do Actions migrados em 12/08. Desfecho em 26/08: os secrets do Surge foram cadastrados lá, mas o problema real era outro (o Actions da organização nunca executou nada) — ver a seção "Dois repositórios GitHub" no topo.

### Revisão de 26/08 (paleta da marca + vetor oficial no SaaS)

Modelo aprovado pelo Robério na prévia de dois caminhos (canvas "Viratempo · Paleta Nova"), depois de ele mesmo editar o modelo A na tela. Venceu o **modelo A ("Maré")**: base clara continua sendo o padrão, o teal ocupa o lugar exato que o laranja tinha, e a marca entra sozinha.

- **A paleta é a do manual: teal `#62C2CA` (interação/marca), petróleo `#04242F` (tinta e superfície escura) e preto.** O mecanismo não mudou — hex único nos dois temas, tinta escura sobre o preenchimento, verde/vermelho reservados a sentimento —, só a cor. Toda a família derivada foi recalibrada e re-medida: `--brand-text`, `--gradient-hero`, `.metric-accent`, glow, mesh dos dois temas, `--estacao-*`, `--ceu-*`, `FUNDO_ESCUTA`, `chartInk`, o neutro de série (`COLOR_SENTIMENT.neu`, `COR_OUTROS`) e a casca dos pop-ups.
- **A tinta neutra virou fria nos dois temas**, pelo MESMO argumento que a tinha tornado quente em 03/08: no claro `#04242F`/`#3E5B66`/`#5B737D` (14,76 / 6,62 / 4,57 sobre a página), no escuro `#EAF2F3`/`#A9C0C6`/`#7E979E` (12,79 / 7,64 / 4,71 sobre o vidro do card). Creme quente sobre petróleo é que passaria a destoar.
- **Superfícies escuras que tinham hex próprio foram para o petróleo com LUMINÂNCIA CASADA**, não com um tom "parecido": a casca do `ModalShell` (`#14203A→#08232E`, `#0C1527→#061A22`, `#0A111F→#05151C`) e o chip de perfil (`#334155→#1F4552`, `#1E293B→#0F2C35`, `#020617→#010C10`). O motivo é prático: as duas superfícies têm medição de contraste registrada (o brilho translúcido do chip reprovava AA em 28/07 e só foi resolvido escurecendo a ponta clara), e casar a luminância mantém aquelas medições de pé em vez de exigir refazê-las.
- **Ganho colateral: cor de marca e cor de ALERTA deixaram de ser vizinhas.** O laranja de risco (`#F97316` e primos, em `chartTheme.ts`/`temaColors.ts`/bandeiras) continua no sistema paralelo dele, mas agora também está separado no espectro — antes os dois laranjas conviviam na mesma tela e só não se confundiam porque estavam documentados. O `#FED7AA` de AVISO sobre a superfície escura (GravarAgora, RadiosMonitoradas) ficou âmbar de propósito, junto com `#FCA5A5`/`#86EFAC`: é estado, não marca.
- **O PDF ganhou o tique de verdade.** `pdf.ts::tique()` desenha o glifo do arquivo oficial com operadores de caminho do PDF, a partir de uma tabela gerada UMA vez do SVG (largura 1, origem embaixo, y para cima — o PDF conta ao contrário do SVG). O aro de radar saiu do cabeçalho e a tagline saiu junto. Se o arquivo da marca mudar, **gerar a tabela de novo em vez de ajustar número à mão**.
- **Os ícones do app estavam na era "Radar Político"**: `public/icons/icon-*.svg` ainda eram o radar VERDE (`#22c55e` sobre `#0d1117`), nunca tocados nos dois rebrands anteriores. Agora são o tique teal sobre petróleo, e `manifest.json`/`index.html` levam `#04242F` como cor de tema.
- **O título de página encolheu junto** (Inter 600 / 27px, era Space Grotesk 800 / 34px): veio de três edições que o Robério fez no próprio canvas depois de aprovar o modelo, e valem para as 17 páginas com rota, não só para a tela editada — o padrão de 28/07 existe para elas não divergirem. O peso do menu ficou em 800: só o item ATIVO em 500 deixaria o selecionado mais leve que os vizinhos, invertendo a hierarquia.
- **Verificado no navegador** (harnesses `clima-dev`, `temas-dev`, `aprovacao-dev`, `relatorio-dev` e a tela de Login, que não pedem credencial), com `tsc --noEmit` e `npm run build` limpos.

### Revisão de 27/08 (Estação Meteorológica: modelo "Direção A")

Rediagramação da tela de ENTRADA, pelo fluxo mockup-primeiro de sempre: seis direções desenhadas num canvas de aprovação (o layout atual mais A a F, cada uma com a contrapartida escrita ao lado), e o Robério escolheu a **Direção A**. O que mudou é a DISPOSIÇÃO; nenhum dado, número ou regra de cálculo foi tocado.

- **Estado final do dia, depois de três rodadas de pedido do Robério: faixa nobre em 6 + 3 + 3 (veredito · radar · engajamento) e o DIAGNÓSTICO em banda de largura cheia logo abaixo dela.** A ordem de leitura da Direção A sobrevive inteira (quanto → por quê → temas → sugestões → termômetro); o que mudou foi onde cada bloco mora. O diagnóstico saiu da faixa porque, com o radar de volta, quatro cards dividiam doze colunas e a análise ficava numa tira de 278px: parágrafo de prosa em coluna estreita vira torre de linhas curtas. O radar cresceu junto (210px de diâmetro, contra 132 da rodada anterior e 235 do padrão do componente) e os corpos do card subiram (rótulo 16→18px, estado 21→24px, LED 12→14px), com o respiro em volta do desenho caindo de 36 para 24px para o card não ditar a altura da faixa. **A `AntenaStatusColumn` da Rádio Escuta, gêmea desta desde 29/07, ficou nos corpos antigos** — o pedido foi sobre este card; se as duas tiverem que voltar a casar, é lá que se mexe.
- **A faixa nobre passou a ser "quanto · com que lastro · por quê"**, em `lg:grid-cols-12`: veredito (5), engajamento (3) e diagnóstico (4) — proporções da primeira rodada, ver o bullet acima para o estado final. Era clima (3) · radar de coleta (1) · engajamento (2) numa grade de 6. As três razões, em ordem de peso: o radar é estado do SISTEMA e ocupava a coluna central da faixa mais nobre da tela, separando dois números que se qualificam um ao outro (a aprovação e o tamanho da amostra); o diagnóstico, que responde à pergunta que a tela existe para responder, só aparecia depois de ~420px de cartões; e a 1440px o herói já quebrava em duas linhas, então a faixa custava ~480px de altura, não os 320 do `minHeight`.
- **Sem diagnóstico a linha fecha em dois cards (7 + 5), não em três com um vão.** É o caso de tenant novo e de semana/mês sem histórico. O card de "análise ainda não disponível" continua aparecendo quando o período é semana/mês; no "dia" ele some, porque o backend gera na hora.
- **O radar chegou a virar selo na linha do título** (`RadarStatusChip`), mas **voltou para a faixa nobre no mesmo dia**, a pedido do Robério, no lugar que tinha desde 27/07: entre o clima e o engajamento. A faixa ficou em **4 + 2 + 3 + 3** (veredito, radar, engajamento, diagnóstico) e o selo saiu do cabeçalho junto — os dois ao mesmo tempo diriam o mesmo estado duas vezes na mesma tela. `RadarStatusChip` fica exportado sem consumidor; `RadarStatusBar` segue no estado "período sem dados". **O radar agora recebe o diâmetro por prop (`tamanho`)**: a coluna tem 183px e o card recorta o que passa disso, então a página passa 132, que é exatamente a largura útil. O padrão do componente (235) é a medida gêmea da antena da Rádio Escuta e continua valendo lá; até aqui a Estação Meteorológica desenhava 235 numa caixa de 135 e exibia um radar cortado na borda. **As cores de estado não mudaram** (teal `#62C2CA` varrendo, âmbar `#F59E0B` ocioso): chegaram a ser unificadas no teal e o Robério pediu o âmbar de volta na mesma conversa — ocioso é estado de atenção e a cor é o que avisa de longe.
- **O caminho para "O que o povo diz" saiu do card do clima e foi para o card de diagnóstico.** O card do clima deixou de ser um botão: ele não tinha afordância visível (o chip que a tinha foi junto), e card inteiro clicável sem sinal é pior que um alvo menor e declarado. Ter os dois seria dizer a mesma coisa duas vezes em lugares diferentes. O clique no diagnóstico é coerente com o que ele é: a leitura da IA leva à evidência que ela resume.
- **Temas e sugestões agora dividem a linha seguinte (7 + 5, no `xl`).** As sugestões viviam no rodapé da página, a uma rolagem inteira dos temas que as motivam. A grade abre no `xl` e não no `lg` porque a coluna de sugestões é texto corrido: em 5/12 de uma tela de 1024px ela viraria uma tira estreita. O termômetro por tema fecha a página, como aprofundamento.
- **O número do IAD desceu de 208px para 132px** (e o "%" de 72 para 46), o ícone do clima de 104 para 96px, o nome da condição de 36 para 34px. O número deixou de ser o único bloco da faixa: no corpo antigo ele empurrava lastro e diagnóstico para fora da primeira leitura. **O nome da condição ganhou linha própria** abaixo do par número+ícone, então não disputa mais largura com o número — o `clamp` por container-query continua lá para o caso estreito. A legenda fixa ("Aprovação da gestão nos comentários analisados no período") passou a FECHAR o card, no rodapé, em vez de vir colada no número. No estado "sem sinal" ela não aparece: explicaria um número que não existe. A foto de céu foi de 52% para 46% da largura do card, que estreitou junto.
- **No card de engajamento o número subiu para logo abaixo do rótulo** (fechava o card antes) e o chip "Ver publicações" desceu para o rodapé. A leitura da faixa é da esquerda para a direita: o segundo número da linha precisa estar na mesma altura do primeiro para se comparar com ele. A frase virou "mais confiável é a leitura ao lado", que é onde a leitura está agora.
- **A tela de LOGIN mudou na mesma leva** (canvas próprio, editado pelo Robério na mão): o painel de marca saiu do degradê `FUNDO_ESCUTA` para o **petróleo chapado `#04242F`** (o import de `superficieRadio` saiu do arquivo); a **wordmark subiu de 38 para 55px e saiu do branco para o teal** — `var(--brand)` aplicado no contêiner, porque o traçado é `currentColor` e o componente não recebe `style`, e NUNCA a classe `.text-brand`, que resolve por `--brand-text` (escuro no tema claro, ~3,3:1 sobre este fundo, contra 7,9:1 do teal); a manchete caiu de peso 800 para 400 e a linha de apoio de 500 para 400; o link virou "Primeiro acesso ou esqueci a senha" (perdeu o "— definir senha", e com ele o travessão) e o rodapé virou "Acesso restrito a usuários cadastrados". **Sobre o peso**: no canvas o valor escrito foi 100, mas a Inter é carregada em 400/500/600/700/800, então o navegador resolveu para 400 e foi 400 o que ele aprovou na tela. Peso declarado que não existe no arquivo não afina nada, só cai no vizinho mais próximo em silêncio; para um traço realmente mais fino é preciso acrescentar o 200/300 na URL da fonte. A tela de Aceitar Convite NÃO tem este painel e não foi tocada.
- **A wordmark da SIDEBAR subiu de 26 para 30px e saiu da tinta do tema para o teal da marca** (27/08, via o `painel-viratempo.json` que o Robério entregou — um retrato do painel em forma de configuração). A cor é `style={{ color: "var(--brand)" }}` no contêiner, a mesma regra do painel do Login: o traçado é `currentColor` e a classe `.text-brand` resolveria por `--brand-text` (escuro no tema claro). Os DEMAIS deltas do mesmo JSON foram avaliados e **recusados por pergunta direta**: subtítulo novo ("Clima político de Alagoinhas, lido pelos comentários") não entra em tela nenhuma (mantida a decisão de 21/08 de marca sem tagline), fundos de página ficam como estão (`#F1F5F6`/`#05202A`), e a janela padrão continua POR TELA (Clima 24h, Feed 7d, Mapa/Pedidos 30d — o `periodo: 1` global do JSON era default de configurador, não pedido). Cards sólidos, régua de KPIs em toda página, Aprovação em dois blocos e SOV visível eram modelagem do configurador, não estado desejado.
- **Existe uma ferramenta de ajuste da marca e dos elementos das páginas**: `radar-app/marca-dev.html` + `src/dev-marca.tsx` (harness Vite só de dev, mesmo padrão do icones-dev — fora do bundle, sem login, sem rede). Duas camadas, na mesma tela: **(1) marca** — réplicas com as classes reais das seis superfícies onde ela vive ou pode viver (sidebar desktop, nav mobile — hoje sem marca, a superfície existe para PROPOR e o placeholder "sem marca" é clicável —, painel de marca e logo compacta do Login, Aceitar Convite e banda do PDF), com variante wordmark/tique/nenhuma, altura, cor, alinhamento e margens por superfície; **(2) elementos** (pedido do Robério em 27/08: "todos os elementos clicáveis") — a réplica do Login é a página INTEIRA (painel + formulário, manchete, apoio, features, rótulos, botões, link e rodapé) e todo texto ou botão das réplicas é clicável: o clique abre texto, tamanho, peso, cor e margens daquele elemento no painel (registro `ELEMENTOS`, com os valores reais do app como padrão); **(3) páginas internas** (mesmo dia) — as 10 telas de dados como WIREFRAMES gerados do próprio retrato painel-viratempo.json (`PAGINAS`: título, KPIs ativos com valores de exemplo, blocos na grade de 12), uma por vez via chips; título, rótulo/valor/legenda de KPI e título de bloco são elementos clicáveis como os demais, e BLOCO ganha dois controles a mais no painel: largura em colunas e visível/oculto (`BLOCO_DEFAULTS`; a Divisão da Conversa da landing já abre como oculto, o estado real). Tema claro/escuro, persistência em localStorage e exportação de JSON no espírito do painel-viratempo.json: `marcaPaginas` por superfície + `elementos` só com o que foi mexido. O fluxo é o mockup-primeiro de sempre: **a ferramenta NÃO escreve no app**, o JSON exportado é que vira pedido de mudança. Regras embutidas nela em vez de deixar violar: a cor da banda do PDF é travada em `#04242F` (nunca branco sobre o teal) e a cor da marca entra pelo contêiner (`currentColor`), nunca pela classe `.text-brand`. A réplica do PDF é visual (o arquivo real é desenhado por `relatorio.ts`/`pdf.ts`).
- **Verificado no navegador** (harness `clima-dev`, em 1440px e 375px): faixa nobre em 568/334/451px com os três cards na mesma altura, linha de temas+sugestões em 801/568, número em 132px, nenhum elemento estourando a largura em nenhuma das duas medidas. `tsc --noEmit` e `npm run build` limpos.

### Alerta de Suporte (31/07/2026)

Aviso automático (WhatsApp e/ou SMS) para o número que o admin cadastrar em **Configurações > Alerta de Suporte** (aba nova em `AdminPage.tsx`), disparado assim que o pipeline ÁGORA parar de funcionar — com o motivo provável e uma sugestão de correção. Peça central: `alerta_suporte.py`.

- **É ADITIVO, não substitui nada.** O grupo do WhatsApp (`EVOLUTION_GROUP_ID`) que já recebia "falha na execução" continua recebendo exatamente como antes — o número do admin é um destino A MAIS, configurável por ele mesmo, sem depender de um secret do GitHub.
- **Dois vigias, porque nenhum dos dois cobre tudo sozinho**: (1) de DENTRO do próprio `agora.py` — o `else: main()` final (`agora.py`, fim do arquivo) agora está em `try/except`, e qualquer exceção não tratada dispara o alerta com o erro REAL antes de repropagar, então é imediato e específico; (2) o **heartbeat** (`heartbeat.yml`/`heartbeat_check.py`), que roda sozinho a cada 15min (era 4h) e pega o que o item 1 não pode: o cron do `agora.yml` simplesmente não disparar (processo nunca chega a existir, então não tem como se auto-reportar). 15min só é viável porque o repo é **público** (Actions ilimitado e grátis nesse caso) — repo privado precisaria repensar a frequência.
- **O timeout do step "Executar ÁGORA" (mata o processo, sem chance de rodar `except`) tem um BACKSTOP no próprio `agora.yml`**: um step `if: failure()` novo chama `python alerta_suporte.py --origem agora_yml_backstop`. Ele só dispara se o marcador `.alerta_suporte_enviado` não existir — esse arquivo só é criado quando o `except` de dentro do `agora.py` já conseguiu mandar um alerta mais específico, evitando notificar o mesmo incidente duas vezes com motivos diferentes. Esse marcador nunca deve ser commitado (está no `.gitignore`).
- **Coleta vazia (0 posts) já tinha alerta para o grupo desde antes; ganhou também o alerta admin-configurável**, no mesmo bloco de `main()`. O heartbeat ainda soma um terceiro sinal, mais paranóico: **2 execuções seguidas com coleta vazia** (não 1 — um dia sem post publicado de verdade é normal; dois seguidos não é).
- **Run "success" com o modelo fora do ar também alerta (incidente de 01/08/26).** O crédito da API Anthropic esgotou no meio do dia e o run das 18:03 UTC terminou verde com TODAS as chamadas ao modelo falhando em HTTP 400 "credit balance is too low": cada falha individual é engolida de propósito (um lote perdido não pode derrubar o run), o pipeline gravou `_DEFAULTS_ANALISE` em tudo e nenhum vigia disparou — não houve exceção não tratada nem coleta vazia (a Apify funcionou). O que faltava era alguém olhar o CONJUNTO no fim: todo call site cria o cliente via `agora.py::_cliente_anthropic()` (proxy que conta sucesso/falha de cada `messages.create` — **nunca usar `Anthropic()` direto**, senão a chamada escapa da conta; `radio_analise.py` recebe o cliente pronto e já entra), e `_verificar_saude_anthropic()` roda no fim do `main()` (em `_safe`: avisa, não derruba) disparando `alerta_suporte.disparar("anthropic_sem_credito", ...)` quando **todas** as chamadas do run falharam por crédito/chave ou **>= 3** falharam mesmo com sucessos anteriores (crédito que esgota no MEIO do run). Só erro **determinístico de conta** entra na conta (`credit balance`/`billing`/`invalid x-api-key`/`authentication_error`); 529/timeout é transitório e fica de fora — alertar por instabilidade passageira viraria ruído. `sugerir_correcao` tem ramo próprio (comprar crédito em console.anthropic.com), checado ANTES do ramo de chave inválida: o erro vem como 400, não 401, e a correção certa é comprar crédito, não trocar o secret. Os contadores zeram no início de cada `main()` (no multi-tenant, por tenant).
- **`sugerir_correcao(motivo)` casa o texto do erro com os incidentes já documentados neste arquivo** (token Apify 403, crédito Apify esgotado, 429/bloqueio do Instagram, sessão/login do IG, Anthropic instável ou com chave inválida, Supabase fora do ar, timeout genérico) e só cai no "confira os logs" quando nada bate — sugestão específica de verdade, não um texto de preenchimento.
- **Dedup por `alerta_historico` (`tipo='suporte'`), janela de 60min.** Evita o mesmo incidente disparando duas vezes (ex.: o `except` do `agora.py` E o backstop do YAML rodando perto um do outro) sem deixar o admin sem notícia numa indisponibilidade longa — o heartbeat, rodando a cada 15min, ainda consegue reenviar um lembrete a cada ~60min enquanto o problema persistir.
- **Credenciais são lidas em tempo de CHAMADA, nunca em constante de módulo** — a mesma armadilha já documentada para `coletor_radio.py`/`radio_analise.py` (agora.py chama `load_dotenv()` DEPOIS de importar `alerta_suporte`; constante de módulo capturaria vazio pra sempre numa execução local).
- **SMS usa Twilio, e por enquanto é a única peça sem crédito configurado.** `_enviar_sms` degrada em log (mesmo padrão do `enviar_whatsapp` sem credencial) até os secrets existirem. **Pendência real de configuração**: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN` e `TWILIO_FROM_NUMBER` como secrets do GitHub (conta a criar em twilio.com — plano trial já permite enviar SMS para o próprio número verificado). Sem eles, a caixa SMS na UI simplesmente não entrega nada; WhatsApp funciona hoje porque reusa o Evolution API que já existe.
- **O botão "Enviar teste" da UI dispara `heartbeat.yml` via `workflow_dispatch`** (input `teste_alerta=true`), não uma Edge Function que manda a mensagem direto — o teste passa pelo MESMO caminho de código do alerta de verdade (`heartbeat_check.py` → `alerta_suporte.disparar(..., forcar=True)`, ignorando dedup e limiar de horas), então prova o mecanismo real, não uma simulação à parte. A Edge Function (`supabase/functions/testar-alerta-suporte`) só confere admin e aciona o disparo — reusa o secret `GH_DISPATCH_TOKEN` que já existia para `gravar-radio`, sem escopo adicional.
- **"Não recebi o alerta" tem várias causas e o envio devolve o MESMO HTTP 404 para quase todas** — servidor da Evolution inexistente, instância removida, instância desconectada do WhatsApp, número sem WhatsApp. Por isso existe `python alerta_suporte.py --diagnostico` (ou o `heartbeat.yml` com o input `diagnostico`): testa uma camada por vez, de fora para dentro, para na primeira que falha e diz se o problema é do **SISTEMA** ou do **NÚMERO**. Não envia mensagem nenhuma, então pode rodar à vontade enquanto se conserta o canal. **Nunca imprime a URL nem a apikey** — o veredito não precisa delas e o log do Actions é público neste repo.
- **A dedup do alerta de suporte é POR INCIDENTE desde 06/08** (`_ja_alertado_recentemente` filtra pelo prefixo `[origem]` que `_registrar_alerta` sempre gravou na mensagem — retrocompatível, sem migration). Era global por tenant: qualquer alerta calava qualquer outro pela janela inteira, então um aviso de baixa urgência que se repete (créditos da Apify) podia engolir um "pipeline parado" crítico que chegasse minutos depois. O curinga do PostgREST no filtro é `like.[origem]%` (o `%` funciona; validado contra o banco real). Origens com janela própria: `apify_sem_credito` roda com `janela_dedup_min=1440` (1x/dia — a condição dura dias e quem resolve é o dono da conta) e é usada pelos DOIS vigias (agora.py no bloco de coleta vazia e heartbeat_check.py) de propósito, para dividirem a mesma janela em vez de mandar um lembrete cada; o alerta de 80% de consumo (`apify_creditos`, dentro de `verificar_creditos_apify`) usa 720 min e agora também passa pelo alerta_suporte multi-provedor — antes ia SÓ para o grupo pela Evolution, que estava fora do ar quando o teto estourou em 27/07, e ninguém soube. `verificar_creditos_apify` passou a retornar `{uso, teto, pct}` para o chamador nomear a causa. **O CallMeBot JÁ ESTÁ configurado e entregando** (confirmado pelo Robério em 06/08 e pelo dado: `alerta_historico` mostra alertas de suporte com `canal=whatsapp` entregues 3x/dia desde 03/08 — com a Evolution morta desde 31/07, quem segura o canal é o CallMeBot). O registro genérico "whatsapp" não permitia essa auditoria, então `_enviar_whatsapp` do alerta_suporte passou a retornar o NOME do provedor e `alerta_historico.canal` grava `whatsapp:callmebot` (ou evolution/twilio) — nenhuma tela exibe esse campo, é rastro de auditoria. Consequência prática do canal estar VIVO: todo ajuste de dedup vira mensagem real no telefone do admin — foi por isso que a coleta vazia genérica (3 msgs/dia desde 03/08) precisou virar `apify_sem_credito` 1x/dia.
- **O WhatsApp do alerta de suporte é MULTI-PROVEDOR** (`_PROVEDORES_WHATSAPP`, 31/07): Evolution → CallMeBot → Twilio WhatsApp, parando no primeiro que entrega. Amarrar o canal a um único serviço reintroduz o ponto único de falha que o canal de reserva tinha acabado de tirar do sistema. Cada provedor se auto-desativa sem as credenciais dele, então **o que o admin configurar passa a valer sem mudança de código**. CallMeBot (`CALLMEBOT_APIKEY`) é o de menor atrito para destino fixo: o dono do número manda uma mensagem única de opt-in ao bot e recebe uma apikey pessoal — sem servidor, sem mensalidade, sem QR code para renovar (a fragilidade que derrubou a Evolution). A apikey é atrelada ao número que fez o opt-in, então **serve só para o alerta de suporte, nunca para mandar a terceiros** (o "Alertar Secretário" continua via `wa.me` no navegador). A resposta do CallMeBot põe o resultado no CORPO (HTML) e o status varia até no erro (apikey inválida veio com HTTP 203, medido em 31/07) — o veredito é pelo texto, não pelo status. Twilio WhatsApp reusa `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN` do SMS mais `TWILIO_WHATSAPP_FROM`. O envio ao GRUPO (`agora.yml`/`radio.yml`) continua Evolution-only: CallMeBot/Twilio não mandam para grupo.
- **Existe um canal de RESERVA: issue no próprio repositório** (`_enviar_github_issue`, rótulo `alerta-suporte`). Um alerta de suporte com um canal só tem o mesmo ponto único de falha que ele deveria estar vigiando — foi exatamente o que aconteceu em 31/07. A reserva foi escolhida por ser o único canal que **não depende de nada a mais**: usa o `GITHUB_TOKEN` que o Actions já injeta, sem conta nova, sem secret novo, sem serviço para manter no ar, e o GitHub notifica o dono do repo por e-mail e pelo app. **Só entra quando WhatsApp e SMS falharam todos** — enquanto o WhatsApp funciona, não polui o repositório. E **reaproveita a issue aberta** em vez de criar uma por disparo: numa indisponibilidade longa o heartbeat roda a cada 15 min, e isso viraria uma enxurrada de issues sobre o mesmo incidente. Exige `permissions: issues: write` no job (sem isso o token é read-only e a reserva falharia calada, que é o defeito que ela corrige) e `GITHUB_TOKEN` no env dos steps que disparam — inclusive o "Executar ÁGORA", porque o `agora.py` alerta de dentro do próprio `except`.
- **Quando nenhum canal entrega, o aviso sai por `::error::` do Actions.** O sistema que existe para avisar que algo quebrou não pode depender do canal que quebrou: em 31/07 o servidor da Evolution tinha sumido e o alerta de suporte falhava em silêncio. `::error::` sobe o texto para a interface do run e para o e-mail de falha que o GitHub já manda ao dono do repo, sem nenhuma infraestrutura nossa.
- **Incidente de 31/07/26 — o canal WhatsApp inteiro estava fora do ar, e não era o número.** O diagnóstico parou na camada 2: o host respondeu `{"status":"error","code":404,"message":"Application not found","request_id":...}`, que é a **plataforma de hospedagem** (formato de `request_id` do Railway) dizendo que a aplicação apontada por `EVOLUTION_API_URL` não está mais lá — não é a Evolution respondendo. Afeta todo envio automático (alerta de suporte, aviso de falha ao grupo no `agora.yml`/`radio.yml`, alerta de créditos Apify). **Não afeta** o card "Alertar Secretário" do dashboard, que é a função que a SECOM usa: `EnvioSecretario.tsx` abre `https://wa.me/...` no navegador do próprio usuário e registra em `message_log`, sem passar pela Evolution. Antes de dizer que "o WhatsApp caiu", separar as duas coisas.
- Ferramentas: `python alerta_suporte.py --origem <texto> --motivo <texto>` dispara manualmente (por linha de comando, fora do teste da UI); `python alerta_suporte.py --teste` força um alerta de teste local (ignora dedup); `python alerta_suporte.py --diagnostico` não envia nada e diz onde está a falha.

### Critério de relevância (não afrouxar)

- **A lista de `relevance_keywords` é do cliente. Nunca adicionar, remover ou "melhorar" as palavras cadastradas** — só o admin mexe nisso pela UI.
- **Todo perfil cadastrado passa pelo filtro, inclusive os de governo** (revisão de 25/07). Antes a conta oficial da gestão era isenta ("a fonte já é o critério"), e por isso post de agenda cultural, festa junina e afins entrava na base e formava clima sem nenhuma relação com as palavras da tela Relevância. Decisão do cliente: **o clima só pode ser formado por conteúdo que se relacione com as palavras cadastradas, nada além disso.**
- O filtro (`agora.py::_motivo_relevancia`) classifica as keywords cadastradas em **específicas** (contêm um token distintivo: `prefeito de alagoinhas`, `gustavo carmo`) e **genéricas** (só tokens que servem para qualquer município: `prefeito`, `prefeitura`, `gestao municipal`, `administracao`). Genérica só vale se o texto também citar uma **âncora do tenant** (`alagoinhas`/`gustavo`/`carmo`), derivada das próprias keywords específicas.
- A âncora é exigida **apenas da imprensa** (cobre a região e publica sobre outras cidades). Oposição são políticos locais — quando escrevem "a gestão" é a daqui; exigir âncora deles descarta crítica legítima. Governo também não precisa de âncora (a conta já é da gestão daqui), mas **precisa de alguma keyword cadastrada** no texto.
- **Os handles dos perfis políticos cadastrados na tela Fontes (governo + oposição) também valem como âncora** (`agora.py::_ancoras_de_perfis`). Veículo local não repete o nome da cidade, mas cita os políticos daqui: sem isso, matéria de cobrança à gestão que marcava `@gleysersoares` e `@jaldicenunes` era descartada como "notícia de outra cidade" (achado na limpeza de 25/07). **Handles de imprensa ficam de fora de propósito** — o nome do veículo não diz de que cidade é o fato narrado, e foi justamente por isso que a âncora passou a ser exigida da imprensa. Handles com menos de 6 caracteres são ignorados: o match é por substring e um handle curto casaria dentro de qualquer palavra.
- Efeito medido em 25/07 sobre a base real (231 posts, 9 keywords cadastradas): 116 passam e 115 são descartados; só 3 dos 29 posts de `@gustavoascarmo` sobrevivem, porque ele escreve em primeira pessoa e raramente usa as palavras da lista. **Se o cliente quiser recuperar esse volume, o caminho é cadastrar `alagoinhas` na tela Relevância** (mediu-se 188 passam / 43 fora nesse cenário) — a decisão é dele, tomada na UI dele; nunca adicionar a palavra por conta própria.
- O match é por **substring** de propósito: `@prefeituraalagoinhas` e `@gustavoascarmo` vêm colados na menção e são o sinal mais forte de que o post é local. Trocar por match de palavra inteira já quebrou isso uma vez.
- Antes de mexer nesse filtro, medir contra a base real com `python agora.py --teste-filtro` (mostra a classificação das keywords e o motivo de cada decisão). No Windows, rodar com `PYTHONIOENCODING=utf-8`.

### Critério de sentimento (revisão de 25/07 — não reintroduzir os atalhos)

O clima mede **o sentimento que o cidadão expressou sobre a gestão municipal**. Nunca deduzir polaridade por proxy: se a pessoa não avaliou a gestão, o dado certo é neutro. Cinco atalhos foram removidos por fabricarem sentimento que ninguém manifestou:

- **Apoio a opositor não é crítica à gestão.** "Parabéns vereador", "você é o próximo prefeito" = NEUTRO. Só vira negativo se o próprio comentário reprovar a gestão ou endossar a denúncia do post. A regra antiga marcava como negativo todo elogio a perfil opositor: 400 comentários (38% de todos os negativos) na base de 25/07. O lado do perfil (`OPOSITOR`/`ALIADO`) é **contexto de leitura, nunca atalho de polaridade** — vale para `PROMPT_COMENTARIOS`, `montar_prompt_comentarios::nota_lado` e `montar_prompt::lado`.
- **Risada não é prova de ironia.** 😂/kkkk aparece em deboche, mas também em concordância e no riso de quem defende a gestão. Ironia exige a contradição no texto. Risada dirigida a quem critica é defesa, não ataque.
- **Cobrança só é negativa quando há reprovação.** Pergunta ou recado sem reclamação é neutro; a demanda já é capturada no campo `pedido`.
- **Limiares simétricos.** Antes: 50% para negativo, 60% para positivo, e "misto" só descia para negativo (empate ia para o lado negativo). Isso é dedo na balança. Hoje os dois lados usam o mesmo limiar, em `agora.py` (safety net e `recalcular_sentimento_posts`) e em `data.ts` (`sentimentoReacao` e `fetchAgregadoComentarios`) — se mexer num, mexer nos quatro.
- **Sem inversão dupla por oposição.** `comentarios_pct_pos` já é medido como "favorável à gestão" na classificação de cada comentário; inverter de novo no agregado transformava aprovação em crítica.

**Os critérios valem para TODOS os prompts, não só o de comentários** (auditoria de 26/07). A revisão de 25/07 consertou `PROMPT_COMENTARIOS` e deixou `PROMPT_TRIAGEM` e `PROMPT_SISTEMA` para trás: os dois seguiram um mês mandando classificar apoio a opositor como crítica à gestão, e o `PROMPT_SISTEMA` chegava a se contradizer 150 linhas depois de si mesmo. Isso não é cosmético: a triagem produz `score_risco`, `urgencia` e `risco_crise` e decide se o post sobe para o Sonnet, e o `recalcular_sentimento_posts` **não corrige nenhum desses campos** (ele só reprojeta os percentuais). Ao mexer em critério de sentimento, varrer os quatro prompts: `PROMPT_TRIAGEM`, `PROMPT_SISTEMA`, `PROMPT_COMENTARIOS` e as notas de lado (`triar_post_rapido::nota_lado` e `montar_prompt_comentarios::nota_lado`).

Para medir mexidas na **triagem** existe `python agora.py --teste-triagem [N] [--imprensa|--governo]`: roda só o `PROMPT_TRIAGEM` numa amostra real (oposição por padrão, que é onde o atalho distorcia mais) e compara com o gravado, sem escrever nada. Custo: só Anthropic. Ele foi criado justamente porque a ausência de harness foi o que deixou a triagem escapar da revisão anterior.

Quem entra na conta do clima: **só comentário de cidadão** (perfil político não é população e não passa pelo classificador — antes herdava a média do próprio post, a média alimentando a si mesma) e **só com `confianca_tema >= 50`** (`CONFIANCA_MIN_SENTIMENTO` no agora.py, espelhado em `radar-app/src/lib/sentimento.ts`). Abaixo disso o comentário conta no total como indeterminado, nunca como crítica ou elogio.

Antes de mexer em qualquer critério de sentimento, medir com `python agora.py --teste-sentimento [N]`: reclassifica uma amostra real com os critérios atuais e compara com o gravado, **sem escrever nada**. Custo: só Anthropic, zero crédito Apify.

**A chamada de comentários (`analisar_comentarios_haiku`) roda com `temperature=0` desde 30/07 — não era a regra que estava errada, era a amostragem.** Achado ao investigar a queixa do cliente de que comentários de apoio ao opositor (sem reprovar a gestão) apareciam em "Vozes que aprovam": a chamada nunca fixava `temperature`, então rodava no default da API (1.0). Reproduzindo a MESMA entrada e o MESMO prompt do comentário "Vereador vc falou tudo" (post do vereador opositor sobre o Hospital Dantas Bião), a produção tinha gravado `positivo`, mas 4 de 4 repetições isoladas com `temperature=0` saíram `neutro` — a regra 1 do `PROMPT_COMENTARIOS` já cobre esse caso exato com exemplo quase idêntico ("Brabos, tem o meu respeito" -> NEUTRO), só que numa amostragem aleatória o modelo às vezes escapava dela. `temperature=0` não é opcional para classificação: é o que faz o modelo convergir para a resposta que a regra pede, em vez de rolar dado a cada chamada.
- `python agora.py --reparar-sentimento-oposicao [--dry-run]` conserta o que já foi gravado com o comportamento antigo: reclassifica (post inteiro, mesmo lote que a produção usaria) todo comentário de cidadão marcado `positivo` num post de perfil de Oposição, grava só o que mudou, e chama `recalcular_sentimento_posts` em seguida para os agregados dos posts acompanharem. Rodado em 30/07: 44 comentários corrigidos (a maioria `positivo`/`negativo` -> `neutro`, o padrão esperado de "elogio à pessoa não é juízo sobre a gestão").

### Texto

- **Nunca usar travessão (—) em texto gerado ou exibido.** Os prompts do `agora.py` proíbem na origem; `limparTravessoes()` (radar-app/src/lib/format.ts) limpa textos antigos na exibição. Vale também para textos novos de UI.
- **Vocabulário**: "comentários analisados" (não "vozes ouvidas"); "estabilizar/estabilizado" (não "recuperar/recuperado"); "Sugestões a serem avaliadas por especialista" (o "genéricas" saiu do título em 01/08 a pedido do cliente; nunca "o que deveria ter sido feito" — a plataforma sugere, não prescreve).
- **Sentence case, não Title Case**: usar a classe `.frase-cap` (index.css), nunca o `capitalize` do Tailwind — ele deixava "Instagram E Facebook Da Prefeitura", com preposição maiúscula no meio da frase.
- **Data em prosa é dd/mm/aa, nunca ISO cru.** `lib/format.ts::fmtDataBR("2026-07-28")` → `"28/07/26"`. Datas de gráfico continuam em `fmtDiaBR` (dd/mm, sem ano — o eixo não tem espaço para o ano). Não exibir `briefing.dia` nem qualquer coluna `*_em`/`data_*` direto do banco: é ISO e já vazou pra tela pelo menos uma vez.

### Índices e clima (auditoria de 26/07 — não reverter)

- **O risco político é normalizado pela soma dos pesos.** Antes os pesos somavam 0,65 no `agora.py` e o resultado era apresentado numa escala que diz ir de 0 a 100: o teto real era 65 e as faixas de `boletim.py` ("tempo fechando" ≥ 60, "tempestade" ≥ 80) eram inalcançáveis. Numa varredura das 9.261 combinações de (IAD, % risco alto, ICA), **69,7% caíam em "céu limpo" e "tempestade" tinha probabilidade zero** — o produto que vende alerta de crise não conseguia declarar uma. Depois da normalização: céu limpo 53,7%, tempestade 1,0%, e dia tranquilo continua céu limpo (não virou alarme falso). Se voltar a somar termo sem dividir pela soma dos pesos, o teto artificial volta junto.
- **`calc_risco()` (agora.py) e `calcRisco()` (indices.ts) são gêmeos**: mesmos termos, mesmos pesos, mesma normalização, incluindo a velocidade do negativo (pct_neg do dia menos o de 3 entradas atrás na série). Antes o backend usava 3 termos e o frontend 4, e o mesmo dia tinha dois riscos diferentes no mesmo produto. Mexeu num, mexe no outro.
- **`risco_amplificacao` fica fora do numerador E do denominador** enquanto o dado não for coletado. Ele era multiplicado por zero desde sempre; mantê-lo só no denominador recria o mesmo teto, agora disfarçado.
- **O clima tem uma fonte só: `getWeather(iad)`.** Havia um ramo `isAdmin` na ClimaPage em que o admin via o clima derivado do IAD (escala de aprovação, alto é bom) e o cliente via a condição do boletim (escala de risco, alto é ruim): duas grandezas na mesma metáfora visual, que podiam discordar no mesmo dia. `weatherFromCondicao` foi removida de propósito. Não reintroduzir uma segunda fonte de clima sem resolver a diferença de grandeza.
- **Sem comentário classificado, o IAD NÃO é exibido: a tela diz "Sem sinal"** (05/08, achado na avaliação do produto). O IAD conta neutro por `iad_neutro` (0,5), então um período sem nenhum comentário classificado converge para **exatamente 50** — e a Estação Meteorológica mostrava esse 50 em 208px com a legenda "Aprovação da gestão" e o clima "Nublado, opiniões divididas" ao lado. Ou seja, o produto **afirmava empate técnico onde ninguém tinha sido medido**, que é o pior erro possível numa tela usada para decidir. Medido na base: 166 dos 330 posts (50%) com `comentarios_pct_pos` e `_neg` zerados, e IAD 50,0 exato em 14 dos 55 dias com métrica (12 deles passam a dizer "sem sinal"; os outros 2 tinham 23 e 87 votos e continuam exibindo 50%, porque ali o empate foi **medido**). O piso é `MIN_VOTOS_IAD` (10) em `lib/indices.ts`, com `votosDeSentimento`/`temSinalIAD` — e é 10, não 1, porque com N votos um único comentário move o índice em 100/N pontos: abaixo disso um comentário sozinho troca a faixa de clima exibida. Vale nas três telas que mostram o número (ClimaPage, ApprovalPage e CommandCenter); na Aprovação **o velocímetro também some**, porque agulha ao centro lê como medição de equilíbrio. A fórmula do `calcIAD` não mudou: o que mudou é quando ela pode ir para a tela. Harness: `radar-app/clima-dev.html` + `src/dev-clima.tsx` (entrada Vite só de dev, fora do bundle) renderiza a ClimaPage real com o cache do react-query pré-populado, nos três casos (sem sinal, com sinal e fronteira de 9 votos), sem login e sem rede.
- **A porcentagem do IAD aparece pra todo papel** (revisão de 28/07, revertendo a nota antiga deste arquivo). Até então só o admin via o número grande na Estação Meteorológica; o usuário comum via só o rótulo (`wx.label`, ex. "Chuva"). O cliente pediu o número pra todo mundo — o ramo `isAdmin` que trocava entre os dois saiu do card do clima em `ClimaPage.tsx`. `isAdmin` continua vivo ali só pra decidir a fonte do boletim (`fetchBoletimByRole`).

### Medição de acurácia (rótulo humano)

- Todo o valor do produto repousa na classificação de sentimento, e até 27/07 **ninguém tinha medido a taxa de erro**. `--teste-sentimento` compara o modelo atual com o anterior: isso mede **deriva** entre versões do critério, não acerto. Acurácia exige referência externa ao sistema medido.
- **Reclassificar a amostra com um modelo (o mesmo ou outro) não resolve.** Isso mede autoconsistência: o classificador vira o próprio gabarito e todo erro sistemático passa batido, porque ele erra igual nas duas vezes. O gabarito precisa ser humano.
- Fluxo em `acuracia.py` (módulo puro, com testes no `__main__`) mais duas flags:
  1. `python agora.py --amostra-rotulagem [N]` gera `rotulagem_<data>.html` (planilha **cega**, com atalhos 1/2/3/0 e progresso salvo no navegador) e `gabarito_<data>.json` (o que o modelo respondeu). Default 100 por estrato, 300 no total.
  2. Uma pessoa rotula e exporta `rotulos.csv`.
  3. `python agora.py --medir-acuracia rotulos.csv gabarito_<data>.json` reporta precisão com IC de Wilson, revocação, F1, matriz de confusão ponderada, acurácia geral e kappa de Cohen.
- **A amostra é estratificada pela classe PREVISTA**, não aleatória simples: a base é desbalanceada (negativo 47%, neutro 42%, positivo 11%) e 300 aleatórios trariam só ~32 positivos. Estratificar pela predição dá a precisão de cada classe direto do estrato, sem ponderação; para revocação, acurácia e kappa os estratos são reponderados por `N_h/n_h`.
- **No recorte "só confiantes" o universo também encolhe** (`N_conf`, não `N`). Usar o N cheio extrapolaria a subamostra confiante para uma população maior do que ela representa e inflaria justamente o número que o relatório apresenta como o mais importante. Há asserção cobrindo isso.
- **A planilha é cega de propósito**: rotulador que vê o palpite da máquina concorda com ela por ancoragem. O gabarito fica noutro arquivo. Há um teste que gera dois cartões idênticos no texto e diferentes só no rótulo do modelo e exige que sejam indistinguíveis.
- **Os arquivos gerados nunca vão para o repo nem viram artifact publicado** (já no `.gitignore`): contêm texto e @ de cidadãos reais, o mesmo dado que a retenção da migration 009 protege.
- O guia de rotulagem embutido no HTML espelha o critério do `PROMPT_COMENTARIOS`. Se o critério mudar, mudar o guia junto: senão a medição capta divergência de definição, não erro do modelo.

### Retenção de dados pessoais (LGPD)

- Opinião política de cidadão identificado é **dado pessoal sensível** (LGPD art. 5º, II) e o controlador é órgão público. O `autor_hash` era gravado na **mesma linha** que `username` e `texto` em claro, então a pseudonimização não separava identidade de conteúdo, e nada nunca era apagado.
- `agora.py::expurgar_pii` roda a cada pipeline e apaga `texto` e `username` dos comentários fora da janela (`RETENCAO_PII_DIAS`, default 180). **Preserva** sentimento, tema, subtema, localidade, pedido, curtidas, confiança e `autor_hash` — clima, índices, Pedidos, Bairros e a série histórica ficam inteiros. Migration **009**.
- Sob demanda: `python agora.py --expurgar-pii --dry-run` (conta sem escrever) ou `--expurgar-pii 180`. É idempotente via `pii_expurgado_em`. Cobre também linhas sem `data_comentario_dia` (falha de parse de fuso) pelo `atualizado_em`, senão elas ficariam fora da retenção para sempre.

### Visual

- **Nova linguagem visual (briefing de 03/08, onda 1 — só cor e tipografia, layout intacto).** O pedido do cliente foi "modernizar a roupagem sem mexer na disposição", para um público de assessores de 25 a 60 anos que lê o painel de relance. Por isso a mudança inteira mora em `index.css` + `tailwind.config.ts`: as ~15 telas herdam de lá, e nenhuma foi editada uma a uma. Tema claro em off-white quente (`--bg-page: #F6F2EA`, com o card um degrau mais claro — é a diferença entre os dois, e não a borda, que faz o vidro existir sobre o creme); tinta na família do chumbo nos dois temas (era azul ardósia no claro); mesh do tema escuro trocado do índigo/teal/ciano para a mesma família quente, porque blobs frios sob um laranja quente faziam o escuro parecer outro produto ao lado do claro. **Verde e vermelho continuam sendo APENAS sentimento** — a paleta do briefing (laranja/chumbo/off-white) é a da interface, não a do dado —, mas desceram um degrau no tema claro (`--success: #137A3C`, `--danger: #C22626`): sobre o creme, os tons anteriores caíam para 2,95:1 e 4,33:1. **Fora do escopo da onda 1** e ainda no visual antigo: as cinco peças animadas (clima, radar, velocímetro, rádio, contadores) e os cards de superfície própria (`superficieRadio.ts`, `RadarStatusColumn`).
- **Onda 2 do redesign (03/08) — as cinco peças animadas do briefing.** (1) O ícone de clima do hero levita (`wx-flutuar`); (2) a varredura do radar e as ondas da antena ativas são **laranja da marca**, não mais verde — o verde voltou a ser exclusivo de sentimento; ocioso segue âmbar (estado de atenção). Os hex são literais (`COR_RADAR_ATIVO` em `RadarStatusBar.tsx`) porque entram em strings de conic-gradient com sufixo de alpha, onde `var()` não resolve — acompanhar o token à mão; (3) `components/ContadorAnimado.tsx` conta até o valor real na entrada (IAD e comentários da Estação Meteorológica). **É rAF e pode ser**: a proibição do velocímetro é sobre rAF dono de estado CONTÍNUO; aqui é rampa única cujo repouso é o número verdadeiro — aba oculta só adia o fim; (4) o velocímetro já vibrava; (5) `components/SinalVivo.tsx` (`LedVivo` + `OndasEq`): LED pulsante na estação ativa do cadastro, equalizador no card de estação que **gravou** na janela — nunca na que falhou (falha ≠ silêncio). Keyframes compartilhados (`wx-flutuar`, `led-pulso`, `eq-barra`) moram no `index.css` porque têm mais de um consumidor; todos transform/opacity, com `prefers-reduced-motion` desligando o movimento e mantendo o dado (o halo some, o LED fica; as barras param nas alturas fixas).
- **A Estação Meteorológica está na linguagem aprovada pela prévia de 03/08** (arquivo de aprovação em `Desktop/aprovacao-estacao-meteorologica.html`, validado pelo cliente antes da implementação). O hero do clima é **card claro de vidro**, no lugar da foto de céu com véu escuro e texto branco fixo; a camada de chuva de tela cheia (`.rain-layer`) foi removida — a chuva cai da nuvem, dentro do desenho. O céu em CSS (`components/CeuAnimado.tsx`, tokens `--ceu-*` do index.css) valeu de 03/08 até 04/08 e ficou **órfão** com a troca pelo `ClimaIconeAnimado` (bullet seguinte): componente e tokens permanecem no repo sem consumidor — remover ou reaproveitar é decisão pendente. `wx.bg`/`wx.bgApp`/`wx.bgLight` (weather.ts) ficaram **sem consumidor** — `wx.image` e `wx.heroDark` voltaram a ter consumidor em 04/08, no fade do hero (bullet adiante). Os chips neutros da página usam `FUNDO_ESCUTA` importado (os antigos `CHUMBO`/`CHUMBO_ESCURO` slate saíram); os contadores neg/pos de "Temas que merecem atenção" usam `--sent-ink-*` (cor de gráfico é preenchimento, não texto). O fluxo que funcionou aqui e deve ser repetido em redesign de página: **mockup estático de aprovação primeiro** (HTML autocontido com tema claro/escuro alternável), implementação só depois do OK.
- **O desenho do clima no hero é o `components/ClimaIconeAnimado.tsx`** (revisão de 04/08, vídeo de referência do cliente + escolha entre duas prévias no harness): um ícone animado por condição do `getWeather`, **soft e sem contorno** — só preenchimentos em degradê; a primeira versão, com linha azul-marinho fiel ao vídeo, foi preterida e removida. Sem o traço, as formas se sustentam com degradês de mais contraste interno, **sombra de base** sob cada nuvem e **brilho radial atrás do sol**. O **Severíssimo tem UM raio, maior, pela FRENTE da nuvem** (a versão de dois raios atrás foi rejeitada na prévia, e o eco translúcido em volta do raio também foi retirado a pedido). A composição do card é **número | ícone grande no centro | frase à direita** (`limparTravessoes(wx.sub)` na exibição), e o par ícone+frase usa **`flex-[1_1_300px]`, não `flex-1`** — item de base 0 nunca quebra linha num flex-wrap, então o par encolhia em vez de descer e o conteúdo vazava do card no mobile (vale também para o texto interno, `flex-[1_1_11rem]`). É SVG puro com animação CSS de transform/opacity (mesma regra do GaugeTema/AntenaSinal: roda no compositor, nunca rAF; com `prefers-reduced-motion` o desenho fica parado e COMPLETO, gotas e raio visíveis), e **grupo animado nunca carrega atributo `transform` próprio** — a animação CSS substitui o atributo e a nuvem teleporta pra origem; posicionamento no `<g>` externo, classe de animação no `<g>` interno. Harness: `radar-app/icones-dev.html` + `src/dev-icones.tsx` (entrada Vite só de dev, fora do bundle de produção) mostram as 6 condições nos dois fundos e réplicas do hero sem depender de login.
- **A foto de céu voltou ao hero como FADE lateral, visível na DIREITA e desvanecendo para a esquerda** (04/08; a 1ª versão era espelhada e a 4ª rodada inverteu a pedido): `wx.image` numa camada absoluta de **52%** de largura com `mask-image` de stops intermediários (opaca até 22%, morrendo suave até 96%) — revela o fundo do card do tema; NÃO é o fundo inteiro com véu escuro de antes do redesign. **No MOBILE (`< sm`) a foto vai para o RODAPÉ com fade vertical** (`sm:hidden` / `hidden sm:block`): no card empilhado o par ícone+título é centralizado e a foto lateral deixava a fronteira do fade no MEIO do título ("TEMPE|STADE", metade ilegível) — no rodapé o título senta inteiro sobre a foto. **A tinta do texto que senta sobre a foto (título e frase do clima) é FIXA pela luminância da foto e pelo tema (`inkFoto`, com `useThemeStore`), nunca pelos tokens**: foto ESCURA (`wx.heroDark`) = véu leve `rgba(10,12,18,0.28)` na camada + texto claro fixo nos dois temas; foto CLARA = **preto sólido `#0B0E14` no tema claro** e tokens no tema ESCURO (atrás da transição do fade o fundo é o card escuro, e o preto sumiria nele). Rótulo, número e legenda ficam na zona sem foto e usam os tokens. **`heroDark` descreve a FOTO, não a condição**: `partly` é carregada de nuvens escuras e o campo foi corrigido para `true`. **As fotos são `.jpg` limpos desde a inversão**: as `.webp` originais tinham a MARCA D'ÁGUA do gerador de imagem no canto inferior direito, invisível com o fade espelhado e exposta pelo invertido — foram cortadas em ~7% na borda direita (ver `public/sky/README.md`); ao trocar foto de céu, conferir marcas perto das bordas.
- **O bloco direito do hero tem o TÍTULO do clima em destaque** (2ª rodada de 04/08): `wx.label` em caixa alta (uppercase via CSS), extrabold 30/36px, acima da frase (`wx.sub`, que desceu para 18/20px em `text-txt-2`) — antes só a frase aparecia e o nome da condição não estava em lugar nenhum do card.
- **O número do hero usa dígitos PROPORCIONAIS: sem `tnum` e sem tracking custom** (3ª rodada de 04/08). Em caixas tabulares nenhum espaçamento fixo é harmônico — o 4 preenche a caixa inteira e o 5/2 sobram, então "44" colava com o tracking negativo herdado do peso 200 e "52" abria demais com o positivo que o corrigiu; o kerning da própria fonte é que decide o espaço por par. `tnum` continua certo onde há COLUNA de métricas para alinhar. Como o `ContadorAnimado` muda a largura durante a rampa de 0,9s, o span reserva a largura do valor FINAL (cópia invisível, `aria-hidden`) e o contador anima em camada absoluta por cima — sem isso o ícone e a frase ao lado tremem a cada carga da página.
- **A Rádio Escuta está na linguagem aprovada pela prévia de 04/08** (arquivo em `Desktop/aprovacao-radio-escuta.html`, validado antes da implementação — o mesmo fluxo mockup-primeiro da Estação Meteorológica). O que mudou foi a metade de LEITURA da página: os **cards de estação** têm superfície própria de vidro quente com borda laranja (tokens `--estacao-bg`/`--estacao-borda` no index.css, creme no claro e âmbar apagado no escuro), LED pulsante + equalizador quando a estação gravou na janela, e **estação que só falhou fica com LED apagado E borda avermelhada** — falha ≠ silêncio continua. A **citação** leva fio laranja de 3px (era cinza) e o botão de ouvir o clipe é pílula `bg-brand text-brand-ink` (`ClipeCitacao.tsx`); instante + aviso de transcrição automática seguem obrigatórios. `TOM_COR` (RadioPage) usa as tintas `--sent-ink-*` como texto, nunca o par de gráfico. KPIs com `ContadorAnimado` na display (`Kpi` recebe `valor: number`). A linha de topo (antena/cadastro/gravar) já era da onda 2 e não mudou.
- **A superfície escura dos cards é PETRÓLEO desde 26/08** (`superficieRadio.ts::FUNDO_ESCUTA = linear-gradient(165deg, #2E5560, #04242F)`; foi o chumbo quente `#55534E→#171613` de 03/08 a 26/08, e antes disso o slate azulado `#475569→#0F172A`) — contraste remedido na ponta clara: branco 6,53:1, rótulo a 78% de alpha 4,52:1. **O painel de marca do Login consumiu a constante entre 26/08 e 27/08** (ele mantinha uma segunda cópia escrita à mão da receita e ficou para trás na troca, exatamente o defeito que a constante existe para evitar); desde 27/08 aquele painel é **petróleo chapado `#04242F`**, e não mais o degradê (ver "Revisão de 27/08"), então não há mais nada a sincronizar entre ele e os cards escuros. A receita mora **só** lá: `RadarStatusColumn` e `AntenaStatusColumn` importam de `superficieRadio.ts` em vez das cópias hardcoded que tinham (eram 3 cópias do mesmo gradiente). Contraste remedido na ponta clara, o pior caso: branco 7,68:1, rótulo a 78% de alpha 4,55:1. As menções antigas ao degradê `#475569→#0F172A` neste arquivo descrevem a mesma superfície no tom antigo.
- **Marca como TEXTO tem token próprio, `--brand-text`** (`#0E6B75` no claro, 5,67:1 na página; `#7FD4DB` no escuro, 9,49:1 — família do `#62C2CA` desde 26/08). O `--sent-ink-link` acompanhou (`#0E6B75` no claro, `#7FD4DB` no escuro), e os degradês de texto (`--gradient-hero` claro e `.metric-accent`) usam os teais fechados `#12808C → #0E6B75 → #084F57`. **Exceção única e consciente: o `@perfil` do cabeçalho da Análise por Perfil usa `#62C2CA` CHAPADO** (pedido explícito do cliente em 04/08, com print, mantido na troca de paleta). Medido sobre o card: 2,00:1 no claro — abaixo do mínimo de 3:1 até para texto grande — e 8,03:1 no escuro. Fica assim por decisão de design; o nome também aparece no chip aceso do seletor logo acima, então a leitura não depende só dele. Não "consertar" para `--brand-text` sem novo pedido; se voltar a ser exigido AA, é `var(--brand-text)` (5,54:1 no claro). `--brand` é escolhido para receber tinta escura por cima; como cor de letra sobre o creme ele mede 2,56:1. A regra vive no `index.css` (`.text-brand { color: var(--brand-text) }`, declarada depois de `@tailwind utilities`, então vence por ordem) em vez de trocar a classe nos ~10 pontos que a usam. `hover:text-brand` tem pseudo-classe e continua ganhando dela, de propósito.
- **UMA família em todo o painel: a Inter** (26/08/26, pedido do Robério na mesma leva da paleta nova, com o layout mantido como está). O briefing de 03/08 tinha criado duas com papéis distintos — Space Grotesk em título, rótulo de card e número (`h2, .section-label, .metric-hero`, regra por ELEMENTO), Inter no texto corrido —, e o `h1` já tinha saído dessa regra em 26/08. Agora a segunda família saiu inteira, e em quatro lugares de uma vez, porque deixar um deles para trás faz a tela divergir sem ninguém notar: a regra de `font-family` do `index.css` (removida, os três herdam a Inter do `body`), o `display` do `tailwind.config.ts` (virou ALIAS de `sans` em vez de sumir — são ~30 usos de `font-display` em 12 telas, e trocar classe por classe só para apagar um nome é mexer em 12 arquivos sem ganho, a mesma razão que manteve `brand.2` e `skycard`), os `style={{ fontFamily }}` inline dos números (ClimaPage, Gauge, GaugeTema, harness de ícones) e a URL de fontes do `index.html`. **`font-synthesis-weight: none` saiu junto**: ele existia porque a Space Grotesk para no 700 e o navegador fabricava o 800 engordando o traço; a Inter é carregada em 400/500/600/700/800, então todo peso usado tem arquivo real — ao usar um peso novo, acrescentar na URL da fonte. `.metric-hero` continua em 700 (o 300 era da doutrina fina de 11/07, revogada em 27/07). Continua valendo o resto: hierarquia por tamanho e cor, piso tipográfico maior, e `.section-label` como título padrão de card.
- **Tipografia**: o dashboard é lido em telão/TV, então o piso é maior que o default do Tailwind — `text-xs`=14px e `text-sm`=16px (override em `tailwind.config.ts`), `.section-label`=15px/peso 700, labels de gráficos ECharts=12px. Ao criar tela nova, seguir esse piso; nunca reduzir para "caber". **`.section-label` é o título padrão de card em todo o painel** — títulos novos devem usar essa classe, não recriar `uppercase + tracking + cor` na mão.
- **Título de página (H1) é igual em toda a barra lateral** (revisão de 28/07, pedido explícito do cliente: "mesmo tamanho e espessura das fontes... em todas as páginas", usando a Estação Meteorológica como referência). Todo `<h1>` de página usa `text-[27px] font-semibold leading-tight tracking-tight` **na Inter** (26/08/26; era `text-[34px] font-extrabold` na Space Grotesk de 28/07 até lá, e o `h1` saiu da regra de display do index.css junto com a troca; pouco depois, no mesmo dia, `h2`, `.section-label` e `.metric-hero` seguiram o mesmo caminho e a Space Grotesk saiu do produto). O que a decisão de 28/07 fixou e continua valendo é o principal: o título é o MESMO em toda página, e página nova segue este padrão, não o tamanho de nenhuma tela antiga. O subtítulo fica logo abaixo em `text-base text-txt-2`; a linha que contém o H1 e o `PeriodoFilter` (quando existe) usa `items-center`. Antes disso a maioria das páginas estava em `text-2xl` (24px) — só a ClimaPage usava o tamanho grande — e a Análise por Perfil chegou a ficar em `text-xl` (20px) com subtítulo em `text-txt-3`. Página nova segue esse padrão, e não o tamanho de nenhuma tela antiga.
- **Peso da fonte segue as utilidades normais do Tailwind, sem achatamento** (revisão de 27/07). Até então havia uma doutrina oposta, documentada aqui: "hierarquia só por tamanho e cor, nunca por peso", com um override em `index.css` que rebaixava `font-bold` para 400 e `font-extrabold` para 600. O cliente pediu o oposto — letras maiores **e** mais pesadas para melhorar a leitura — e a doutrina foi revertida. Não reintroduzir o achatamento sem novo pedido explícito: ele já causou um bug documentado (a primeira versão do box "Publicações analisadas" saiu preta com texto em peso regular, porque `font-bold` apontava para 400 via `!important`).
- **Paleta**: sem identidade visual fechada, tudo que não tem cor semântica usa chumbo/grafite (`#334155` / `#1E293B`) com texto branco — nada de verde/vermelho decorativo. Verde e vermelho ficam reservados para sentimento (positivo/negativo).
- **Velocímetro por tema** (`components/GaugeTema.tsx`) — versão final: arco segmentado com escala **fixa e sempre visível, verde à esquerda (0%) em degradê até vermelho à direita (100%)**; o ponteiro aponta a **% de comentários NEGATIVOS** entre os que tomam partido (neutros fora da conta). Atenção: as rodadas anteriores oscilaram entre medir positivos e negativos — o que vale é negativos.
- **Estação Meteorológica** (landing; medidas atualizadas em 27/08, ver a revisão do dia): número do IAD em destaque (96px, até 132px no desktop; era 120/208 até a Direção A), igual pra admin e usuário comum (ver bullet "porcentagem do IAD aparece pra todo papel" mais acima). Logo abaixo do número, sempre a mesma legenda fixa — "Aprovação da gestão nos comentários analisados no período" — explicando o que ele mede; foi pedido do cliente em 28/07 depois de ver o número sozinho e não saber do que se tratava. O rótulo qualitativo ao lado (`wx.sub`, ex. "Clima ruim — predominância negativa") varia por faixa e não substitui essa legenda. O card principal não leva chips de contagem; o box laranja de engajamento tem número + selo de qualidade da amostra + ação "Ver publicações"; o radar de coleta vive na coluna do meio (ver bullet de layout mais abaixo), em degradê chumbo/cinza (`#475569 → #0F172A`).
- **Todo pop-up usa o `ModalShell`** (`components/ModalShell.tsx`, revisão de 03/08, referência visual aprovada pelo cliente): caixa azul-marinho profunda com fio de luz na aresta superior, tile de ícone, chip de contexto em caixa alta, título grande, painéis internos um degrau mais escuros (`ModalPainel`) e botão primário em pílula clara (`ModalBotaoPrimario`). A casca é **fixa nos dois temas** e aplica `theme-dark` na própria subárvore, então o conteúdo interno que usa tokens (ComentarioBox, `sent-ink-*`, bordas) resolve nos valores escuros já auditados — não reimplementar cor dentro de modal. Acessibilidade (Esc, aria-modal, trava de rolagem, foco) vive na casca, uma vez. `window.confirm` não é usado: confirmação destrutiva é o `ConfirmaModal` (botão na receita semântica: degradê claro vermelho + texto quase preto). **Botão dentro de modal precisa de cor de texto explícita** (`text-txt-1`): `<button>` não herda cor, e o default preto do navegador some no fundo escuro — foi o bug do "Copiar" na primeira rodada.
- **Previsões**: a linha do tempo não usa balões/pins (o tema do dia vive no tooltip); o status "Estabilizado" é **amarelo**, não verde.
- O modal de publicações mostra uma **introdução da legenda** de cada post (`posts.caption`, com fallback em `resumo`) para localizar a publicação sem abrir o Instagram.
- O feed **não** exibe a camada SCCT (cluster de crise, nível de responsabilização, resposta recomendada) nem o chip "Urgente" — o backend continua calculando, mas isso não vai para a leitura dos posts.

## Protocolo de depuração autônoma

Ao investigar um bug, siga esta ordem sem esperar aprovação a cada passo:

1. **Formule uma hipótese antes de editar código.** Se a causa não está clara, adicione instrumentação (log) primeiro — não altere lógica "no escuro".
2. **Rode você mesmo.** Execute `python agora.py` (ou a flag relevante) e leia o output diretamente. Não peça para o Robério colar prints a menos que o comando dependa de algo que só ele tem acesso (ex: conta do Google, WhatsApp, aprovação visual do dashboard).
3. **Isole antes de tocar produção.** Use ou crie uma flag de teste (`--teste-filtro`, `--reprocessar` ou similar) em vez de rodar contra o pipeline de produção ou gastar créditos Apify desnecessariamente. Se não existir uma flag adequada para o teste que você precisa fazer, crie uma.
4. **Descarte hipóteses uma a uma.** Se o log esperado não aparecer, isso já é informação — não repita o mesmo teste, ajuste a hipótese (ex: o pipeline pode estar saindo antes mesmo de chegar na etapa que você está logando, como aconteceu com a deduplicação mascarando o bug do filtro de relevância).
5. **Depois de confirmar a causa raiz, limpe.** Remova logs de debug temporários ou coloque-os atrás de uma flag `--debug` — não deixe `print()` soltos no código de produção.
6. **Reporte de forma concisa**: o que quebrou, por que, o que foi mudado. Sem narrar todo o processo de tentativa e erro.

## Quando escalar para o Robério

Só pare e pergunte quando:
- A correção exige uma decisão de produto (ex: qual deve ser o comportamento correto, não só o bug técnico).
- Falta uma credencial, secret ou acesso que você não tem.
- O teste isolado já confirma o fix, mas rodar contra produção vai consumir créditos Apify de forma não trivial — aí sim, confirme antes de rodar contra produção real.

Fora isso, resolva e implemente diretamente, sem pausar para aprovação passo a passo.

## Migrations no Supabase

- SQL remoto (incluindo DDL) roda direto pelo CLI já logado nesta máquina, sem senha do banco:
  `supabase db query --linked --file supabase/migrations/00X_arquivo.sql`
  (projeto linkado com `supabase link --project-ref wtlhqyqxhuchzloodoyx --yes`; o link cria `supabase/.temp/`, que não deve ser commitado).
- Migrations aplicadas até **011** (006 = histórico de envios manuais em `message_log`; 007 = Relevância/Fontes editáveis por qualquer usuário; 008 = série de seguidores em `profile_metrics`; 009 = retenção de dados pessoais em `comments`, coluna `pii_expurgado_em` + índices do expurgo; 010 = `posts.tom_publicacao` + `confianca_tom`, o tom da publicação em si; 011 = Escuta do Rádio: `radio_transcripts` + `radio_topics` com RLS admin-only, `sources.config` e as policies de `sources` estreitadas para isolar `platform='radio'`). Após DDL, o cache do PostgREST atualiza sozinho — validar com um insert/select de teste via REST, ou `select ... from pg_policies` quando a mudança for de RLS.

## Referências

- Pegadinhas de ambiente conhecidas: ver `GOTCHAS.md` neste mesmo diretório.
- Spec de arquitetura do admin: `2026-06-28-radar-comando-admin-rbac-design.md`.
- Spec da Escuta do Rádio (contrato do ator, achados do teste real, desenho): `RADAR_ESCUTA_RADIO.md`.
